# Tauri 遷移短中長期計劃

## 進度紀錄（持續更新）

最近一次更新：2026-05-10 (#66)。（同上 + **新 #66 — `settings_detect_cx` Tauri Rust 原生 port (關 SettingsPanel 第一個 notImplemented gap)**：之前的 #58-#65 全部把 namespace port 進 sidecar (給 remote phone client 用)，這次轉向**填補 Tauri 本機 host 的 notImplemented gap** — 渲染端 `host.settings.detectCx()` 在 Tauri build 一直 throw `notImplemented`，導致 SettingsPanel 的 cx semantic-navigation 設定區塊一打開就紅，現在 port 進 `src-tauri/src/commands/settings.rs`（已存在的 native Rust 模組，跟 settings_load/save/get_shell_path 同檔）約 +110 行。**為什麼選 native Rust 而非 sidecar 轉發**：(1) settings.rs 既有設計就是 self-contained native，跟 sidecar bootstrap 解耦 — Settings 開啟時 sidecar 可能還沒 spawn (lazy spawn pattern)；(2) detect-cx 邏輯純粹是 fs.read + child_process — Rust `std::fs` + `std::process::Command` 一行對應，不需 SDK / async runtime；(3) 跟 #62 sidecar `settings.detectCx` 的程式邏輯**故意一致**（接 cxSemanticNavigationEnabled / cxBinaryPath 一樣的 settings.json key，回 `{enabled, detected, path?, version?, cacheDir, error?}` 一樣的 shape），所以 remote phone 走 sidecar 路徑、本機 Tauri 走 Rust 路徑，渲染端 `host.settings.detectCx()` 兩邊看到 byte-equal 結果，不會因 host 不同而行為不同；(4) `serde(rename_all = "camelCase")` + `skip_serializing_if = "Option::is_none"` 把 Rust snake_case 自動轉成跟 sidecar 一樣的 camelCase wire 格式，渲染端 `CxDetectionStatus` interface 不需改。**個別 helper 重點**：(a) `CxSettings` deserialize struct 用 `serde(default, rename = "cxSemanticNavigationEnabled" / "cxBinaryPath")` 把 settings.json 兩個 key 解出來，**lenient parsing** — 缺 key 默認 false / None、不認得的 key (其他 settings) 略過不報錯（settings.json 同檔含百項設定，不能因一個欄位 fail 就讓 detect-cx 整個 throw）；(b) `cx_resolve_from_path()` — 平台 `where.exe cx` (Windows) / `which cx` (POSIX) 跑 `Command::new(cmd).arg("cx").output()`，stdout 第一行非空當 cx 路徑、failure / 沒輸出回 None；(c) `cx_resolve_configured(configured)` — 設定有指定 binary_path 走「abs / relative-with-separator / bare token」三路：abs 直接回、含 / or \ 的 relative pass-through 給 OS 解、bare token (例 `'cx'`) pass-through 給 OS PATH 查；空字串 fallback 走 `cx_resolve_from_path()` (PATH lookup)；(d) `cx_run_version(binary)` — `Command::new(binary).arg("--version").output()`，trim stdout 當 version、空字串補 `'cx'`、failure 回 Err(msg) 讓 caller 把錯誤訊息塞進 `result.error`；(e) 主 `settings_detect_cx(app)` — 讀 settings → 拼 cacheDir = `<app_data_dir>/cx-cache`（永遠存在不管 cx 是否 detected，UI 顯示「cx cache located at: X」用）→ resolve binary → 跑 `--version` → 拼 result struct 回。Wire 點：(1) `src-tauri/src/lib.rs` 加 `settings::settings_detect_cx,` 一條 invoke_handler 條目；(2) `src/host-api.ts` 把 `detectCx: () => notImplemented(...)` 換成 `getInvoke()<{enabled,detected,path?,version?,cacheDir,error?}>('settings_detect_cx')` — typed inline object literal 跟 SettingsPanel `CxDetectionStatus` interface 一致。**Test**：`src-tauri/src/commands/settings.rs` 加 3 條 Rust unit test (測 7 個現有測試外擴充到 10 條)：(a) `cx_resolve_configured_pass_through_for_abs_and_relative` — abs path / relative with sep / bare token / 空白 trim 4 種分支都 pass through；(b) `cx_settings_parses_minimal_and_full_shape` — 空 JSON `{}` 默認 false/None、完整 JSON `{cxSemanticNavigationEnabled:true, cxBinaryPath:'/opt/cx'}` 兩 field 對齊、含其他 settings key 的 mixed JSON 不破解析（鎖死 lenient parsing 契約 — 未來新增 settings 不會把 detect-cx 弄炸）；(c) `cx_run_version_failure_returns_err` — 不存在 binary path 跑 `--version` 拿 Err(msg) 而非 panic（鎖死 graceful failure）。仍綠：cargo test settings:: (10/10 passed) / cargo build (clean) / tsc (clean) / sidecar tests 全綠。**結果**：Tauri build 開 SettingsPanel 不再吐 notImplemented，cx 設定區塊現在會即時顯示「(✓) cx detected at /usr/local/bin/cx, v1.2.3 — cache: <appdata>/cx-cache」或「(×) cx not found in PATH」之類的 status badge；user 在那邊改 cxBinaryPath 設定 → save settings.json → 下次叫 detectCx 即時更新。**設計 trade-off**：(1) 跟 sidecar 版本邏輯故意 duplicate — 一份 sidecar (給 remote phone client) 一份 Rust (給本機 Tauri)，trade off 是維護兩邊得同步邏輯，但好處是不必為 Tauri 本機叫 detectCx 付 sidecar IPC 一個 round-trip 的成本；未來如果 settings.detectCx 變得更複雜（如真的去 fetch cx index），考慮統一走 sidecar；(2) 沒 cache cx 偵測結果 — sidecar 版也沒 cache，每次 settings panel 開啟跑一次 `which cx` (POSIX which 通常 < 5ms、Windows where.exe < 50ms)，UI 端容忍；(3) `cx --version` 沒上 timeout — 假設正常 cx binary 不會 hang，反正 user 自己設的 path 自己負責；極端 case (cxBinaryPath 指到 hang 的程式) 會卡住 settings panel 但 user 改回原值就好；(4) `cacheDir` 永遠回（即使 cx 沒 detected）— 跟 Electron 一致，UI 不需在拿到 result 時判斷有沒有 cacheDir field。**剩下 notImplemented 待 port**：(1) `fs.resolvePathLinks` (sidecar 已有 #59 但 Tauri Rust 沒 port — 注意 fs.rs 目前有 uncommitted local edits，先擱)、(2) `image.saveDataUrl`（save-file picker + raw bytes 寫，需要 dialog 跟 image module 串起來）、(3) `clipboard.saveImage / writeImage`（需要 raw bytes 跨 IPC 跟 OS clipboard image API）、(4) `settings.clearTerminalHistory`（清 PTY history，但 PTY 在 Tauri 走 portable-pty Rust，可加 native command）、(5) `workspace.detach / reattach / moveToWindow`（multi-window MVP，先擱）。剩下 PROXIED_CHANNELS 待 port：(1) `pty:*` (Tauri 走 Rust portable-pty 跟 sidecar 解耦，不必 port)、(2) `workspace:*` / `profile:*` 還沒 port 進 sidecar（需要 windowRegistry + ProfileManager 設計，下次另開）。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳。）

歷史紀錄：2026-05-10 (#65)。（同上 + **新 #65 — `fs.search` 進 sidecar (recursive 檔名 substring 搜尋，純 Node walker)**：完成 #64 後最後一個 fs:* 待 port channel — `fs:search`，加進既有的 `node-sidecar/src/handlers/fs.mjs` (~50 行 append)。**設計重點**：(1) **純 Node walker 不 spawn ripgrep** — Electron 版本就是 fs.readdir 遞迴，沒走 `rg` binary，sidecar 1:1 port 跟 Tauri build size / portability 一致；(2) **filename substring match** — 注意：這是**檔名**搜尋不是檔案內容搜尋，`e.name.toLowerCase().includes(query.toLowerCase())` 一個 substring check，UI (FileTree / Agent panel file picker) 期待的就是檔名匹配；(3) **遞迴 + 兩個 hard cap** — `SEARCH_MAX_DEPTH=8`、`SEARCH_MAX_RESULTS=100`，前者防無限遞迴 / symlink loop / 巨深 monorepo (lerna/yarn workspaces)，後者防 UI 端 list virtualization 沒有時崩 — 兩 cap 都 mirror Electron；(4) **IGNORED set** 9 條 — `.git/node_modules/.next/dist/dist-electron/.cache/__pycache__/.DS_Store/release` — 跟 Electron 完全一致（注意：跟 fs.readdir 的 IGNORED set 接近但**多了 `release`** 因為 build artifact 通常在 release/ 下），鎖死「常見 noise dir 永遠不出現在 search」契約；(5) **path-guard 兩段檢查** — entry 進 walk 之前檢一次 dir 整體 sensitive、每個 entry fullPath 也檢一次，鎖死 `~/.ssh / ~/.aws` 等敏感樹永遠不被 enumerate；(6) **sort: directories-first then alphabetical** — `(a, b) => a.isDirectory !== b.isDirectory ? (a.isDirectory ? -1 : 1) : a.name.localeCompare(b.name)`，鎖死 UI 顯示順序契約；(7) **缺 dirPath / 缺 query 一律回 `[]` 不 throw** — UI 走 search-as-you-type 預期空 query 不報錯（host-api 的 contract 是 `Promise<{name,path,isDirectory}[]>` 不是 throw），跟 Electron 一致。**param shape**：handler 用 `pickPath(params, 'dirPath', 'path')` 接 Tauri `{dirPath, query}` 跟 Electron-style positional bare object — 注意 Electron preload 直接走 `(_ctx, dirPath, query)` 兩 positional，bridge 只 unwrap args[0] 所以 Electron-style 從遠端 client 來會壞（兩 positional）；Tauri host-api `{dirPath, query}` object form 是 canonical wire format（`src/host-api.ts` 早就改成 object）。Wire 點：`server.mjs` 的 `import './handlers/fs.mjs'` 已存在，append 即接通；remote-bridge 已建好 `fs:search` → `fs.search` translation。**Test**：~25 條 assertions 真實建一棵 tmpdir 樹：`alpha-dir/alpha-file.txt` / `beta-dir/beta-file.md` / `node_modules/alpha-junk.txt` / `.git/alpha-HEAD` / `dist/alpha-build.js` / `plain.txt` 加上一條 10-deep `alpha-dir/deep/lv1.../lv9` 鏈，分別驗：(a) **case-insensitive substring** — query=`'ALPHA'` 找到 `alpha-dir`/`alpha-file.txt` 但**不**找到 `alpha-junk.txt`/`alpha-HEAD`/`alpha-build.js`（鎖死 IGNORED 過濾）；(b) **sort 順序** — 走過 result 一旦看到 `isDirectory:false` 後不能再出現 `isDirectory:true`（鎖死 dirs-first）；(c) **depth cap** — lv6.txt（depth 8）必出現、lv9.txt（depth 11）必不出現（鎖死 cap 8 不變）；(d) 缺 query / 缺 dirPath / 完全 no-match query 一律 `[]`；(e) `~/.ssh` 直接當 dirPath 短路回 `[]` 不 enumerate（鎖死 sensitive-root short-circuit）；(f) **max-results clamp** — 真實建 120 個 `alpha-NNN.txt` 在 flat dir 下、search → length 嚴格等於 100（不是 ≤ 100，是 = 100，鎖死 cap 觸發行為）。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 ClaudeAgentPanel / CodexAgentPanel / FileTree 的 file picker 搜尋功能可以即時搜 host 端 workspace 檔名 — query 打字 → host-api → bridge → sidecar fs.search → walk 回結果，預期 100ms 內回應對 1000-file repo。同時 path-guard 阻 sensitive 樹被 enumerate / IGNORED 阻 noise dir、跟 Electron 行為對齊。**設計 trade-off**：(1) 純檔名搜尋不含 content — 使用者搜「TODO」找不到含 TODO 的程式碼是 by design（要做 content search 應該用 `pty + rg` 走 terminal 或開新 channel `fs:grep`）；(2) 100 個結果 cap — 對大型 monorepo 可能搜到的多但 UI 100 條已經 scroll 不完，cap 在 walker 內部不 cap result list 後面就好；(3) 8 層深度 cap — 對 monorepo 像 `apps/foo/src/components/sub/sub/lv6` 還在範圍內，但極深 dependency tree (e.g. `node_modules/deep/lib/...`) 反正先被 IGNORED 過濾不會走；(4) walker 是 async recursion 走 `await fs.readdir + for-of` — 不平行化是 trade-off：fs.readdir 對 SSD 已經非常快、平行化會 race condition 讓 result order 不 stable + max-results cap 難 enforce，序列化 IO 簡單可控；(5) 沒做 timeout — Electron 一致，靠 cap 自我限制；極端壞 case (網路 mount 慢) 可能 hang 但這跟 Electron 一樣不在這次 scope。剩下 PROXIED_CHANNELS 待 port：(1) `pty:*`（PTY 在 Tauri build 走 portable-pty Rust，sidecar port 重要性低、難度高，先擱）、(2) `workspace:*` (2 channels — 需 sidecar windowRegistry 設計)、(3) `profile:*` (6 channels — 需 ProfileManager + token encryption)。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (workspace/profile/pty)。**至此 fs:* namespace 全部 11 個 channel + 1 個 event 全部 port 完**：fs.home/readdir/readFile/listDirs/mkdir/deletePath/quickLocations/resolvePathLinks (#59) + fs.watch/unwatch + fs:changed event (#64) + fs.search (#65)，sidecar fs.* 完整可用。）

歷史紀錄：2026-05-10 (#64)。（同上 + **新 #64 — `fs.watch / fs.unwatch` 進 sidecar 加 `fs:changed` event 廣播 (第一個 stateful + emit 的 sidecar handler)**：延續 #63 的 namespace-port 收尾，把 `fs:watch / fs:unwatch` 兩條 stateful channel 跟 `fs:changed` event 一起 port 進 sidecar 新檔 `node-sidecar/src/handlers/fs-watch.mjs` (~95 行)。**這是第一個 sidecar slice 同時改 invoke handler 跟 event 廣播**（之前的 image/fs/git/github/settings/snippet 全是 stateless / 純讀寫），用來驗證 `sendEvent('fs:changed', abs)` → `event:fs:changed` JSON-RPC notification 能透過 Tauri Rust bridge 跟 remote-server 兩路同步傳到 renderer 跟遠端 client，鎖死 sidecar 的 event 廣播路徑（`PROXIED_EVENTS` 已含 `fs:changed`）。**設計重點**：(1) **process-wide Map** `fileWatchers: Map<string, {watcher, timer}>` — key 是 caller 傳的原始 dirPath（**不**正規化／不 resolve），所以 unwatch 必須用同一個 string；value 是 `{watcher: FSWatcher, timer: Timeout|null}` 一起綁，避免分兩 Map 漏 cleanup；(2) **idempotent watch** — 同一個 dirPath 第二次 watch 直接 return true 不重攻擊 watcher，鎖死「FileTree mount/unmount 多次叫 watch 不會 leak」契約；(3) **path-guard 在 fs.watch attach 之前** — `path.resolve` → `isSensitivePath` 命中回 false（**不** throw，host-api contract 是 `Promise<boolean>`），鎖死 `~/.ssh / .aws` 等敏感樹永遠不被監聽；(4) **500 ms debounce coalesce** — 每個 watcher 一個 timer，每次 fs.watch callback `clearTimeout` + `setTimeout(500ms, sendEvent)`，burst 編輯 (rename / pnpm install / build artifact) 收斂成一次 client refresh；(5) **error 自動 cleanup** — `watcher.on('error', ...)` 把 entry 從 map 移掉並 closeEntry（清 timer + close watcher），常見原因是 watched dir 被 rm 掉，renderer 下次 mount 自然會 re-watch；(6) **`{recursive:true}`** mirror Electron — 整棵 tree 監聽，部分 platform/fs (Linux fsevents 沒裝某 kernel 模組 / 某些 net mount) 不支援會 throw ENOSYS，handler catch swallow 回 false，host-api 端 `Promise<boolean>` 一致；(7) **bare-string fallback** `pickPath(params)` 接 `{dirPath}` / `{path}` / 直接 string，跟其他 fs.* handler 一致。**事件 contract**：payload **就是** `path.resolve(dirPath)` 一個 string — 跟 Electron `broadcastHub.broadcast('fs:changed', abs)` 一致，renderer 端 `host.fs.onChanged((path:string) => ...)` 不需修改；event name 走 `sendEvent('fs:changed', abs)` 出去變 `{jsonrpc:"2.0", method:"event:fs:changed", params:"<abs>"}`，Tauri bridge 端把 `event:fs:changed` 拆出 channel `fs:changed` 跟 args，跟 remote 端 PROXIED_EVENTS allowlist 對齊放行。**Test export**：`__closeAllWatchersForTests()` 跟 `__watcherCountForTests()` 兩個 test-only export，前者一刀清光所有 watcher + timer (test 必呼叫不然 node --test 不會 exit)、後者驗 idempotent / cleanup count。Wire 點：`server.mjs` 加 `import './handlers/fs-watch.mjs'`；remote-bridge `fs:watch → fs.watch` / `fs:unwatch → fs.unwatch` kebab→camel 自動接通。**Test**：~25 條 assertions 真實 `mkdtempSync` tmpdir 走 watch + write + 等 debounce + 收 event 全鏈路：(a) watch new dir → true，count=1；(b) **idempotent** 同 path 第二次 watch → true 但 count 仍 1（鎖死不重複 attach）；(c) `~/.ssh` sensitive path → false 且 count 不增（鎖死 path-guard 在 attach 前擋）；(d) 缺 dirPath → false；(e) bare-string params 也 work + 第二個 tmpdir watch → count=2，再 unwatch 回到 1；(f) **event flow** — `__setSendEventForTests` 安裝 capture array，連寫 3 個檔案 (a.txt / b.txt / c.txt) 都在 500ms 內，sleep 800ms 後驗 captured `fs:changed` 至少 1 條 ≤ 2 條（debounce coalesce 鎖死 — 嚴格 ≤1 過於 brittle 因為 Linux inotify 偶會 split burst across windows，≤2 仍能 catch「完全沒 debounce」會是 3+ 的情況），payload 就是 `path.resolve(watchRoot)` abs path；(g) **unwatch silences events** — unwatch 後 count=0，再寫一個檔等 700ms，captured 不增（鎖死 close watcher 後沒漏 emit）；(h) unwatch 沒 watch 過的 path → true 不 throw（idempotent）；(i) watch nonexistent path → false catch swallow，count 不增。`finally` block 三件事必做：`__closeAllWatchersForTests()` / `restoreSend()` / `rmSync(tmpdir)`，少一件就會 leak handle 阻 process exit。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 FileTree / Sidebar UI 真的可以即時看到 host 端檔案系統變化 — 在 host 那邊用編輯器存檔、在 phone 端 1 秒內看到 FileTree refresh；同時 path-guard 永遠擋住敏感樹。Sidecar event 廣播鏈路接通也代表未來 `claude:*` 類 event 從 sidecar 推到 remote 的 path 全部通了 — 之前只在 claude session 內發 sidecar event 走 stdout 給 Tauri，現在驗證了從 PROXIED_EVENTS 出去到遠端 client 的完整流程。**設計 trade-off**：(1) Map key 用 caller 原始 string 而非 resolved abs — 避免 `'./foo'` vs abs path 不一致導致 unwatch 找不到（但 caller 必須傳同樣 string 回來），mirror Electron 行為一致；(2) error handler 不發 event — Electron 行為一致，renderer 下次 mount 自動 re-watch 是補償，避免製造一個「watcher died」error event 增加 client 端複雜度；(3) debounce 500ms hardcode — 跟 Electron 一致；UI 端 `loadRoot()` 重新讀目錄差不多 100-300 ms，500ms 是 reasonable lower bound；(4) 沒 port `fs:search`（ripgrep 全文搜尋）— 那個需要決定 spawn `rg` binary 還是純 Node walk + content match，design space 不小，下次另開；(5) test 有兩個輕度 flaky 點：(i) Linux inotify 對快寫 burst 偶爾切兩個 debounce window，所以 assertion 是 `>= 1 && <= 2` 而非嚴格 `=== 1`；(ii) 某些 Windows defender / antivirus 介入 fs.watch 也會放慢，800ms sleep 是平衡。剩下 PROXIED_CHANNELS 待 port：(1) `fs:search`（ripgrep 全文搜尋）、(2) `pty:*`（PTY 在 Tauri build 走 portable-pty Rust，sidecar port 重要性低）、(3) `workspace:*` (2 channels — 需 sidecar windowRegistry 設計)、(4) `profile:*` (6 channels — 需 ProfileManager + token encryption)。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (fs:search/pty/workspace/profile)。）

歷史紀錄：2026-05-10 (#63)。（同上 + **新 #63 — `snippet.*` namespace port 進 sidecar (10 個 CRUD handler + SnippetDatabase singleton)**：延續 #62 的 user-data namespace port 節奏，把 `snippet:*` 整個 namespace port 進 sidecar — 新檔 `node-sidecar/src/lib/snippet-db.mjs` (~190 行) 是 SnippetDatabase class 的 1:1 port from `electron/snippet-db.ts`、`node-sidecar/src/handlers/snippet.mjs` (~95 行) 是 10 個 handler 的 thin wrapper 把 RPC 參數轉成 db method 呼叫。**SnippetDatabase 重點**：(1) lazy-load singleton — `ensureLoaded()` 第一次呼叫才 `fs.readFileSync(<dataDir>/snippets.json)` JSON.parse，之後 cache in-memory `{snippets, nextId}`；(2) **debounced disk write** — 所有 mutation (`create/update/delete/toggleFavorite`) 走 `save()` 排個 `setTimeout(flushNow, 300)`，coalesce burst edits 成一次 `fs.writeFileSync`，測試端 `flushNow()` 直接 sync 出檔；(3) **external-edit detection** — 每個 read method 先 `refreshIfChanged()` 走 `fs.statSync().mtimeMs`，如果 mtime > lastMtime 就 cancel pending save (in-memory data 是 stale) + reload — 鎖死「另一個 process / user 手動編 snippets.json 後 sidecar 不會用 stale memory 蓋掉外部 edit」契約；(4) **migration on load** — old snippets without `action` field backfill 成 `'terminal'` 並排個 save；(5) `dataPath` getter 每次都 `path.join(resolveDataDir(), 'snippets.json')` 不 memoize，這樣測試 flip `BAT_SIDECAR_DATA_DIR` 後 `__resetForTests()` + 下次呼叫就讀新位置；(6) `__resetForTests()` 把 loaded=false / data 清回初始 + clear pending timer，讓 test fixture 切換時不要被前一輪 in-memory state 污染。**個別 handler 重點**：(a) `snippet.getAll` — sort by updatedAt desc 回最新優先；(b) `snippet.getById({id})` / 或 bare-number params — `pickId()` 兩種都接，找不到回 null；(c) `snippet.create` — `pickInput(params)` 兩路：Tauri form `{input: {title, content, ...}}` 或 Electron-style positional bare object 直接 `{title, content, ...}`，缺 title/content throw `'snippet.create: missing input.title / input.content'` — 在 disk write 之前 fail，避免 corrupt entry 滲透到 nextId；自動補 `format='plaintext'` / `action='terminal'` / `isFavorite=false` / `createdAt=updatedAt=Date.now()` 預設；(d) `snippet.update({id, updates})` — 必須 object form 兩個 key，缺 id throw `/missing id/`、缺 updates throw `/missing updates/`，內部 `updates.workspaceId !== undefined` 而非 `??`（讓 caller 傳 `null` 清掉 workspaceId、不傳則保留），其他 field 用 `??` keep-if-undefined；`updatedAt = Date.now()` 一定 bump、`createdAt` 永不動；(e) `snippet.delete({id})` — boolean 回 found-and-removed；nextId **不** decrement 鎖「id 不重用」契約 (測試明確驗 `after.nextId === 3` after delete #1)；(f) `snippet.toggleFavorite({id})` — 內部 `getById` + `update({isFavorite:!cur})` 兩 step，找不到回 null；(g) `snippet.search({query})` 或 bare-string — `query.toLowerCase()` 對 title/content/tags substring 三個欄位匹配 (case-insensitive)、缺 query 回 `[]` 不 throw（UI 走 search-as-you-type 一定常打空 query）；(h) `snippet.getCategories` — 走 Set distinct + sort 回 string[]，沒 category 的 snippet 不出現；(i) `snippet.getFavorites` — filter isFavorite=true sort by updatedAt desc；(j) `snippet.getByWorkspace({workspaceId})` — **重要 contract**：filter `!s.workspaceId || s.workspaceId === workspaceId` — 沒 workspaceId 的 snippet 在**任何** workspace 都看得到（global snippet），有 workspaceId 的只在該 workspace 看得到，這是 Electron 的設計：workspace filter 是 opt-in per snippet，測試 (l) lock 死 ws-A 看到 2 條 (1=ws-A + 2=global)、ws-B 看到 1 條 (只有 global #2)。Wire 點：`server.mjs` 加 `import './handlers/snippet.mjs'`；remote-bridge `:` → `.` 翻譯 `snippet:getAll` → `snippet.getAll`（沒 hyphen 直接 pass through），handler 註冊完自動接通。**Test**：~50 條 assertions 把 `BAT_SIDECAR_DATA_DIR` pinned tmpdir。(a) empty store getAll → []；(b) create 回 id=1 + 預設 fields，bare-form input 也 work id=2，缺 title/content throw `/missing input/`；(c) `snippetDb.flushNow()` 後 readFileSync `snippets.json` 驗 `{snippets:[2], nextId:3}` on-disk shape；(d) getById object form + bare-number 兩種、missing id 回 null；(e) **update 鎖 createdAt 不動 / updatedAt advance** — sleep 5ms 確保 ms 差，updated.title 改了 / content 不變 / createdAt 同 / updatedAt > 之前；缺 id throw `/missing id/`、缺 updates throw `/missing updates/`；(f) toggleFavorite 兩次 idempotent (true→false)、missing id null；(g) getFavorites 在 #2 fav 後 length=1；(h) search 三路：title `'RENAMED'`(case-insensitive)→ #1 hit / tag `'foo'` → #2 hit / bare-string `'hello'` → content match #1 / 缺 query → []；(i) getCategories distinct + sort `['alpha', 'beta']`；(j) **getByWorkspace global-snippet 契約** — #1 設 workspaceId='ws-A'、#2 沒 workspaceId，ws-A 查到 2 條、ws-B 查到 1 條 (只 global #2)；(k) delete true/false 兩支、`nextId` 仍然 3 不回退；(l) **external-edit detection** — writeFileSync 寫一個含新 id=7 的檔、`utimesSync` bump mtime +1.5s（防 Linux 1s mtime granularity）、再 `getAll` 走 refreshIfChanged 看到新增的 #7 — 鎖死 mtime-based reload；(m) end-to-end `protocol.invokeRemoteHandler('snippet:getAll', [])` 走 remote-bridge 真實鏈路驗證 dispatch 接通。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 SnippetPanel UI 可以列 / 搜 / fav / 編 host 端 snippets；改完 300ms 後 batch flush 進 host 端 snippets.json；user 手動編 snippets.json 也會被 sidecar 下次讀時 pick up — 跟 Electron 行為對齊。**設計 trade-off**：(1) sync `fs.readFileSync` / `writeFileSync` — 跟 Electron 一樣用 sync API；對 sidecar 的 dispatch loop 是 blocking 但 snippets.json 通常 < 100KB，blocking 1-2 ms 完全可接受 + 程式碼簡單，async 版會把 ensureLoaded / refreshIfChanged 全部染 async；(2) 沒 port `workspace:*` 跟 `profile:*` — 那兩個 namespace 嚴重依賴 Electron 的 multi-window registry (`windowRegistry`) 跟 ProfileManager singleton，sidecar 沒有 `ctx.windowId` 概念也沒 broadcast hub，要 port 需要先設計 sidecar 自己的 window/profile 模型 — 大刀，下次另開 slice；(3) update 的 workspaceId 處理刻意走 `!== undefined` 而非 `??`：caller 顯式傳 `null` 會清掉 workspaceId 把 snippet 變回 global，傳 `undefined` 或不傳則保留 — 給渲染端「移除 workspace 綁定」一條乾淨路徑；(4) `pickInput` 嚴格檢查 `title:string + content:string` 才接受 bare-form，避免把 `{input}` 那種 Tauri wrap object 誤判成 bare-form；(5) external-edit detection 比較粗 — 只看 mtime 不 hash content，rapid concurrent edit 仍可能 lose write，但這是 single-user 桌面工具，contention 機率極低，跟 Electron 一致。剩下 PROXIED_CHANNELS 待 port：(1) `workspace:*` (2 channels — 需 sidecar windowRegistry 設計)、(2) `profile:*` (6 channels — 需 ProfileManager 設計，外加 remote-secrets 的 token encryption)、(3) `fs:watch / fs:unwatch / fs:changed`、(4) `fs:search`、(5) `pty:*`（Tauri build 走 portable-pty，sidecar 可能不做）。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (workspace/profile/fs:watch+search/pty)。）

歷史紀錄：2026-05-10 (#62)。（同上 + **新 #62 — `settings.*` namespace port 進 sidecar (4 個 user-settings handler)**：延續 #61 的 namespace-port 節奏，把 `settings:*` 4 個（save / load / get-shell-path / detect-cx）port 進 sidecar 新檔 `node-sidecar/src/handlers/settings.mjs` (~190 行)。**resolveDataDir 串接** — 都走 `path.join(resolveDataDir(), 'settings.json')`，而 `resolveDataDir()` 在 `data-paths.mjs` 已先讀 `BAT_SIDECAR_DATA_DIR` env var 再 fallback 平台預設（mac `~/Library/Application Support/better-agent-terminal`、linux `~/.config/better-agent-terminal`、win32 `%APPDATA%\BetterAgentTerminal`），跟 Electron `app.getPath('userData')` / Tauri `app.path().app_config_dir()` 三邊指向同一個 dir，user 切換 Electron / Tauri / sidecar-only 後 settings.json 會被同一個檔案讀寫；測試端把 `BAT_SIDECAR_DATA_DIR` 改 tmpdir 不會碰到使用者真實設定檔。**個別 handler 重點**：(a) `settings.save` — 接 `{data:string}`（Tauri）或 bare string（Electron），缺 data throw `'settings.save: missing data'`；先 `fs.mkdir(dirname, {recursive:true})` 確保目錄存在再 `fs.writeFile(file, data, 'utf-8')` 回 true；param validation **在** writeFile 之前 — 缺 data 不會把 settings.json truncate 成空，這是測試明確 lock 的契約；(b) `settings.load` — 無 args，try `fs.readFile(file, 'utf-8')`，catch ENOENT 回 null（renderer 視為 first-run 不報錯）；(c) `settings.getShellPath` — 接 `{shellType}` 或 bare string，**Map 快取** per process（兩 back-to-back call 必回 strict-equal string，鎖死 cache 行為），平台分支 mirror Electron：(POSIX) `auto`→`getPosixAutoShell`（讀 `process.env.SHELL` 並 `fs.access` 驗存在 → mac 預設 `/bin/zsh` → 看 `/bin/bash` 在不在 → fallback `/bin/sh`）/`zsh`→`/bin/zsh` / `bash`→`/opt/homebrew/bin/bash → /usr/local/bin/bash → /bin/bash`（fs.access 找出第一個存在的）/ `sh`→`/bin/sh` / `pwsh`/`powershell`/`cmd`→fallback 走 `getPosixAutoShell()` 不 throw（POSIX 不能跑 Windows shell 但 caller 期待回 string，鎖死「never throw」契約）；(Win32) `auto`/`pwsh`→輪詢 `C:\Program Files\PowerShell\7\pwsh.exe` / x86 / `LOCALAPPDATA\Microsoft\WindowsApps\pwsh.exe`，找到的回 abs path、找不到 `pwsh` 回 `'pwsh.exe'`、找不到 `auto` 回 `'powershell.exe'` / `cmd`→`'cmd.exe'` / `powershell`→`'powershell.exe'`；測試端透過 `__resetShellPathCacheForTests()` export 清 cache；(d) `settings.detectCx` — 無 args，回 `{enabled, detected, path?, version?, cacheDir, error?}`；先 `readCxSettings()` (內部讀 settings.json `JSON.parse`，corrupt JSON catch 回 `{}` 鎖死「JSON 壞掉不 crash」契約) 拿 `cxSemanticNavigationEnabled` / `cxBinaryPath`；`cacheDir` 一律 `path.join(resolveDataDir(), 'cx-cache')` 即使 cx 沒裝；`resolveConfiguredCxPath(configured)` — 空 string 走 `resolveFromPath()`（POSIX `which cx` / Win32 `where.exe cx`，stdout split lines 取第一個非空，timeout 2s）、abs path 直回、含 `/` 或 `\` 的 relative 走 `path.resolve`、否則直回（讓 OS shell 解 PATH）；找不到 binary 回 `{enabled, detected:false, cacheDir, error:'cx not found in PATH'}`；找到就 `runCxVersion(binaryPath)` 走 `execFileAsync(bin, ['--version'], {timeout:3000})` 拿 stdout.trim()，成功回 `{enabled, detected:true, path, version, cacheDir}`、失敗 catch 回 `{enabled, detected:false, path, cacheDir, error: msg}`（path 還在讓渲染端顯示「我試了哪個 binary」）。Wire 點：`server.mjs` 加 `import './handlers/settings.mjs'`；remote-bridge kebab→camel 翻譯 `settings:save→settings.save / settings:load→settings.load / settings:get-shell-path→settings.getShellPath / settings:detect-cx→settings.detectCx`，handler 註冊完自動接通。**Test**：~30 條 assertions 把 `BAT_SIDECAR_DATA_DIR` pinned 到 mkdtemp tmpdir 確保 user 真實 settings.json 不被碰。(a) `settings.load` 在 missing file 回 null；(b) `settings.save({data})` 真實寫檔再 readFileSync 比對 byte-by-byte 一致、`settings.load` round-trip 一樣 string；(c) bare-string params 也 work（Electron-style）；(d) **缺 data 必須 error 而且不能 truncate 既存檔** — 先寫一個 `'{"alt":1}'`、再 dispatch 一次 missing data 看 error，最後 readFileSync 確認檔案還是 `'{"alt":1}'`；(e) `settings.getShellPath('auto')` 兩次呼叫回 strict-equal string（cache 鎖）、`__resetShellPathCacheForTests()` 後再 call、Windows 跟 POSIX 各有平台斷言（POSIX `zsh`→`/bin/zsh` / `sh`→`/bin/sh` / `pwsh` 不 throw fallback / Win32 `pwsh.exe` 結尾 / `cmd`→`'cmd.exe'`）、bare-string param 也 work、缺 shellType throw `/missing shellType/`；(f) `settings.detectCx` 有沒裝 cx 都回 typed shape — `{enabled:bool, detected:bool, cacheDir:string}` 必有、cacheDir 必含 `cx-cache` 且必 `startsWith` 我們 pinned 的 BAT_SIDECAR_DATA_DIR（鎖死「不會跑出去碰真實 user dir」）、`detected:false` 時 `error:string`、寫 `cxBinaryPath:'/no/such/binary/cx-fake'` 拿到 `{enabled:true, detected:false, path:'/no/such/binary/cx-fake', error:string}` 鎖「path 還在讓 UI 顯示我試了哪個」、寫 `'{not json'` corrupt 後 detect-cx 仍 `enabled:false` 不 throw 鎖「JSON 壞掉降級」契約；(g) end-to-end 透過 `protocol.invokeRemoteHandler('settings:get-shell-path', [{shellType:'auto'}])` 真實鏈路驗證 multi-hyphen kebab→camel 接通。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 SettingsPanel 可以 load/save host 端 settings.json、shell selector 顯示 host 端真實的 zsh/bash/pwsh 路徑、cx detection 顯示 host 端 cx binary 狀態 — 全 host 端執行、phone 端純展示。Sidecar-only build (沒 Tauri/Electron) 也可以獨立提供 settings 服務，未來 headless mode 自然可達。**剩下 PROXIED_CHANNELS 待 port**：(1) `fs:watch / fs:unwatch / fs:changed`（stateful watcher，需要 watchId map + FSWatcher 包 emitter 走 sendEvent broadcast）、(2) `fs:search`（ripgrep 全文搜尋）、(3) `pty:*`（PTY 在 Tauri build 走 portable-pty Rust，sidecar port 可能不做）、(4) `snippet:*` / `profile:*` / `workspace:*`（user 資料 JSON files，pattern 同 settings — 各自一個 JSON 檔案，可以下個 slice 一刀港）。**設計 trade-off**：(1) `getPosixAutoShell` 用 `fs.access` 改 async — Electron 版用 sync `fsSync.existsSync` 但 sidecar handler 是 async 全程比較一致；多一兩個 ms 但 caller 結果是 cached 不會痛；(2) cx detection 沒在 sidecar 提供 `applyCxEnvironment` / `buildCxSystemPromptAppend` — 那些是 main-process 用來啟動 PTY/SDK 時注 env 的，sidecar 不負責 PTY/SDK env 注入（claude-send 直接跑 SDK 也不串 cx）所以暫時不 port，需要時再加；(3) shell path 的 cache 是 per-process — sidecar 每次 spawn 重置，但 sidecar 本身是長 living 跟 Tauri 一個生命週期，所以 cache 命中率高；(4) `settings.save` 失敗保護 — param validation 在 writeFile 之前是 explicit 設計：renderer 偶有 bug 傳 `undefined` 不能因此把使用者的 settings.json truncate 掉，測試 (d) lock 死這條。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (fs:watch+search/pty/snippet/profile/workspace)。）

歷史紀錄：2026-05-10 (#61)。（同上 + **新 #61 — `github.*` namespace port 進 sidecar (7 個 gh CLI handler)**：延續 #60 的 git.* 節奏，把 `github:*` 整個 namespace port 進 sidecar 新檔 `node-sidecar/src/handlers/github.mjs` (~145 行)。7 條 channel 對應 7 個 handler：(1) `github.checkCli` (`github:check-cli`)、(2) `github.prList` (`github:pr-list`)、(3) `github.issueList` (`github:issue-list`)、(4) `github.prView` (`github:pr-view`)、(5) `github.issueView` (`github:issue-view`)、(6) `github.prComment` (`github:pr-comment`)、(7) `github.issueComment` (`github:issue-comment`)。**全部包 `gh` CLI**：read-list (`prList`/`issueList`) 跟 read-detail (`prView`/`issueView`) 回 parsed JSON 或 `{error:string}`、write (`prComment`/`issueComment`) 回 `{success:true}` 或 `{error:string}` — mirror Electron 兩種 contract 渲染端不需分支調整。**個別 handler 重點**：(a) `github.checkCli` — 最關鍵的「優雅 degrade」入口：先 `gh --version` 確認 binary 存在，再 `gh auth token`（**不**用 `gh auth status` — 後者在多帳號其中一個有問題時會 exit 非零即使 active 那個沒事，導致 false negative；`gh auth token` 只看 active account），回 `{installed:bool, authenticated:bool}` 兩 boolean 給 UI 三態 render（沒裝 / 裝了沒登入 / 都好）；(b) `github.prList` / `github.issueList` — `gh pr/issue list --json number,title,state,author,createdAt,updatedAt,labels,...,headRefName,isDraft --limit 50` 走 `execAsync`（命令固定不需 splice），timeout 15s、`maxBuffer: 5 MiB` 防巨 repo PR 列表超過 default 1MB；(c) `github.prView` / `github.issueView` — 走 `execFileAsync('gh', ['pr/issue', 'view', String(number), '--json', 'number,title,state,author,body,comments,reviews,...'])` — 用 execFile 把 user-supplied `number` 切離 shell 防 injection（雖然 `pickNumber` 已限 typeof 'number' 但 defense in depth），caller 端少傳 number → `{error:'missing number'}` 早返回；(d) `github.prComment` / `github.issueComment` — `execFileAsync('gh', ['pr/issue', 'comment', String(number), '--body', body])` — body 包含任意字串（甚至 \n 跟 backtick）也安全，因為 execFile 不走 shell；缺 number/body 各自回對應 `{error:'missing X'}`，timeout 15s（comment 可能要等 GH API）。`pickString` / `pickNumber` 兩個 helper 跟 git.mjs 一樣 — typeof check 嚴 reject string-typed number，鎖死渲染端必須傳真 number 而非 `'1'`。Wire 點：`server.mjs` 加 `import './handlers/github.mjs'`；remote-bridge 已建好 `github:*` channel 的 kebab→camel 翻譯（`github:check-cli` → `github.checkCli`、`github:pr-list` → `github.prList`、`github:issue-comment` → `github.issueComment`），handler 註冊完自動接通不需額外 bridge wiring。**Test**：~20 條 assertions 在沒裝 gh 跟裝了 gh 兩個環境都 green — (a) `github.checkCli` 一律回 `{installed:bool, authenticated:bool}` shape，沒裝時 `installed=false` 必伴隨 `authenticated=false`（鎖死「沒裝就一定沒登入」契約）；(b) 6 個 handler 缺 cwd 都回 `{error:'missing cwd'}`（驗 param validation 在 spawn `gh` 之前就擋下來，不會浪費 process spawn）；(c) `prView`/`issueView`/`prComment`/`issueComment` 在有 cwd 但缺 number 時回 `{error:'missing number'}`；(d) `prComment`/`issueComment` 在有 cwd+number 但缺 body 時回 `{error:'missing body'}`（驗三層 required-field 順序對）；(e) **string-typed number reject** — `prView({cwd:'.', number:'1'})` 回 `error:'missing number'` 而非變成 valid call — 鎖死渲染端必須走 `Number(s)` 轉型再傳；(f) end-to-end 透過 `protocol.invokeRemoteHandler('github:check-cli', [])` 走 remote-bridge → dispatch → handler 真實鏈路驗證 multi-hyphen channel `github:check-cli` 翻譯到 `github.checkCli` 跟 dispatch 接通拿到 `{installed, authenticated}` shape。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 PR/Issue 面板可以列 host 端 GitHub repo 的 PR / issue、開單個 PR/issue 看 body+comments+reviews、發評論 — 所有透過 `host.github.*` 走 remote invoke → bridge → sidecar handler → `gh` CLI 呼叫 GitHub API → 結果回手機。同時 `gh` 不存在時 `checkCli` 一律 `installed:false` 讓渲染端 GitHubBadge 走 disabled state，**永遠不 throw**。**剩下 PROXIED_CHANNELS 待 port**：(1) `fs:watch / fs:unwatch / fs:changed`（stateful watcher，需要 watchId map + FSWatcher 包 emitter 走 sendEvent broadcast）、(2) `fs:search`（ripgrep 全文搜尋）、(3) `pty:*`（PTY 在 Tauri build 走 portable-pty Rust，sidecar port 可能不做）、(4) `settings:*` / `snippet:*` / `profile:*` / `workspace:*`（user 資料 JSON files，pattern 同 settings）。**設計 trade-off**：(1) `gh auth status` 在多帳號其中一個有問題時 false negative — 用 `gh auth token` 是定義 BAT 的「authenticated」=「active account 拿得到 token」而非「所有帳號都健康」；(2) `prList`/`issueList` 走 `execAsync` 命令固定無 user-supplied 參數所以 shell 安全，但 `prView` 等有 number 走 `execFileAsync` defense in depth — 兩種混用是 trade-off readability vs strict consistency；(3) read 跟 write 兩種 contract 不一致（`{json...}` vs `{success:true}`）— mirror Electron 老契約沒改，未來統一可純 refactor；(4) 沒在 sidecar 增加 `gh` 命令安裝引導 UI — 由渲染端讀 `checkCli` 的 `installed:false` 自行決定要不要彈安裝引導訊息。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (fs:watch+search/pty/settings/snippet/profile/workspace)。）

歷史紀錄：2026-05-10 (#60)。（同上 + **新 #60 — `git.*` namespace port 進 sidecar (7 個 read-only handler)**：延續 #58/#59 namespace-port，把 `git:*` 整個 namespace port 進 sidecar 新檔 `node-sidecar/src/handlers/git.mjs` (~140 行)。7 條 channel 對應 7 個 handler：(1) `git.getGithubUrl` (`git:get-github-url`)、(2) `git.branch` (`git:branch`)、(3) `git.log` (`git:log`)、(4) `git.diff` (`git:diff`)、(5) `git.diffFiles` (`git:diff-files`)、(6) `git.getRoot` (`git:getRoot`)、(7) `git.status` (`git:status`)。**全部 read-only**：用 `child_process.exec` / `execFile` 包 `git` CLI、各自 `await execAsync(..., {cwd, encoding:'utf-8', timeout: 3-10s})` 分開設 timeout（`branch` 3s、log/getRoot/status 5s、diff 10s 比 status/log 更慢可能是大 commit），失敗（cwd 非 repo / git 不在 PATH / timeout）一律 catch swallow 回安全預設 (`null` / `[]` / `''`) — 完全 mirror Electron + Tauri Rust 的「safe default 永不 throw」契約，渲染端 git 區塊永遠不需要 try/catch。**個別 handler 重點**：(a) `git.getGithubUrl` — `git remote get-url origin` 拿 remote URL，regex 兩條：SSH (`^git@github\.com:(.+?)(?:\.git)?$`) 跟 HTTPS (`^https?:\/\/github\.com\/(.+?)(?:\.git)?$`)，match 任一個就拼 `https://github.com/<owner>/<repo>`，沒 match 回 null；接受 `{folderPath}` 或 `{cwd}` (Tauri host-api 跟 Electron 兩邊命名不同，bridge 以 channel 為準但 host-api 介面 `git.getGithubUrl(folderPath)` 跟 `git.getRoot(cwd)` 都用各自命名 — handler 兩邊都 fallback)；(b) `git.branch` — `git rev-parse --abbrev-ref HEAD` 拿 current branch；(c) `git.log` — `execFile git log --pretty=format:%H||%an||%ai||%s -n N`，split('\n') + split('||') 4 段拼成 `{hash, author, date, message}`，message 如果含 `||` 用 `parts.slice(3).join('||')` 還原，count 走 `Math.max(1, Math.min(Math.floor(Number(count)) || 50, 500))` 防 caller 傳 string/NaN/巨大值；(d) `git.diff` — 兩路：有 commitHash 走 `diff <hash>~1..<hash>`、無/`'working'` 走 `diff HEAD`，filePath 加在 `--` 後 — 接 unsafe filename 也透過 execFile 不走 shell 不會 injection；`maxBuffer: 5 MiB` 防 huge diff truncate；(e) `git.diffFiles` — 同 diff 但加 `--name-status`，每行 `tab-split` 成 `{status, file}`；(f) `git.getRoot` — `git rev-parse --show-toplevel` 拿 repo top；(g) `git.status` — `git status --porcelain -uall`（`-uall` show all untracked files，不是只 dir level），每行 `substring(0,2).trim()` 拿 status code (`'M'` / `'??'` / `'A'` / etc)、`substring(3)` 拿 file path。Wire 點：`server.mjs` 加 `import './handlers/git.mjs'`；remote-bridge 已建好 `git:*` channel 的 kebab→camel 翻譯（`git:get-github-url` → `git.getGithubUrl`、`git:diff-files` → `git.diffFiles`、`git:getRoot` → `git.getRoot` 已 camel 不變），handler 註冊完自動接通。**Test**：~30 條 assertions 真實建一個 git repo 跑 `git init / config / add / commit` 後驗整套：(a) **gitAvailable detection** — `execSync('git --version', {stdio:'ignore'})` 失敗就 skip git-specific 斷言只跑 default-path 那塊（CI 沒裝 git 也不紅）；(b) `git.getRoot` — 真實 resolve 跟 repoRoot 對齊（Windows 因 tmpdir symlink 可能展開 `C:\Users\...\AppData\Local\Temp` 而非 `C:\Users\...\Local\Temp`，所以用 `resolve()` + `.toLowerCase()` 比較 — 鎖死 cross-platform 的 symlink/casing tolerance）；(c) `git.branch` — non-empty string（main vs master 因 git 版本不同不鎖具體值）；(d) `git.log` — array length 1、hash 40 char SHA-1、author='Test User'、message='initial commit'，外加 count clamp test (`count: 999999` → array, no throw)；(e) `git.diff` — 對 unstaged 修改 `\+modified` line 出現、filePath filter 成功 narrow；(f) `git.diffFiles` — 找到 `a.txt` 的 entry status='M'；(g) `git.status` — 同時看到 untracked `b.txt` (status='??') 跟 modified `a.txt` (status='M')；(h) `git.getGithubUrl` — 假 SSH remote `git@github.com:tonyq/test-repo.git` → `https://github.com/tonyq/test-repo`，folderPath 跟 cwd 兩個 key 都 work；(i) **default-path** 7 個 handler 全部空 params 回安全預設 (`null`/`[]`/`''`)、non-repo dir (`tmpdir()`) 走 git 失敗也回 null 不 throw；(j) end-to-end 透過 `protocol.invokeRemoteHandler('git:get-github-url', [{folderPath}])` 走 remote-bridge 真實鏈路驗證 multi-hyphen channel 拿到正確 URL。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機/遠端 client 連到 mac/Tauri host 之後 GitPanel / DiffViewer / CommitLog UI 都能讀 host 端 git 狀態 — `host.git.getStatus(cwd)` 透過 remote invoke → bridge → sidecar git.status → `git status --porcelain -uall` → entries 回手機渲染。同時 git 不存在或 cwd 非 repo 一律安全預設不會把 panel 弄炸。**剩下 PROXIED_CHANNELS 待 port**：(1) `github:*` (7 channels — gh CLI 包裝，pattern 跟 git 一樣可一刀直接 port — 後續)、(2) `fs:watch / fs:unwatch / fs:changed`（stateful watcher 需要 watchId map + emit broadcast — 中等難度）、(3) `fs:search`（ripgrep 全文 search）、(4) `pty:*`（PTY 在 Tauri 走 portable-pty Rust，sidecar port 重要性低、難度高，可能不 port）、(5) `settings:*`（user settings JSON，port 進 sidecar 可達）、(6) `snippet:*` / `profile:*` / `workspace:*`（user 資料 JSON files，pattern 同 settings）。**設計 trade-off**：(1) `execAsync` vs `execFileAsync` 混用 — `getRoot` / `branch` / `getGithubUrl` / `status` 走 `execAsync` 走 shell（命令固定無變數），`log` / `diff` / `diffFiles` 走 `execFileAsync`（要 splice user-provided args 像 commitHash/filePath，避免 shell injection）；contract mirror Electron 一致，未來如果想統一改 `execFileAsync` 也是純 refactor；(2) git CLI timeout 是 hard limit，巨 repo 第一次 `git log -n 500` 可能超 5s — 跟 Electron 一樣回 `[]`，不是優雅 degradation 但 mirror existing behavior；(3) `git.diffFiles` parsing 對 rename status (`R100\told\tnew`) 處理是「first tab 後全部當 file」— 這跟 Electron 一致但對 rename 會 lose `old` 部分，未來若要完整支援需改 parser。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (github/fs:watch+search/settings/snippet/profile/workspace)。）

歷史紀錄：2026-05-10 (#59)。（同上 + **新 #59 — `fs.*` namespace port 進 sidecar (8 個 handler)**：延續 #58 的 namespace-port 節奏，把 `fs:*` 8 個（home / readdir / readFile / list-dirs / mkdir / delete-path / quick-locations / resolve-path-links）port 進 sidecar 新檔 `node-sidecar/src/handlers/fs.mjs` (~190 行)。**Skip 三個 stateful/重 handler**：`fs:watch / fs:unwatch / fs:changed`（file watcher state 跨 RPC，需要 Map<watchId, {path, FSWatcher}> 設計，留下次再做）跟 `fs:search`（ripgrep-style full-text，~70 行 + 想清楚是否該重新走 grep 代理）。**handler 共通設計**：(1) `pickPath(params, ...keys)` helper — Tauri host-api 傳 object (`{path}` / `{dirPath}` / `{parentPath, name}` / `{targetPath}`)、Electron preload 傳 positional bare string 經 bridge `args[0]` unwrap，handler 接受兩種；缺 path 各自走「empty array」(readdir Electron contract — 不丟錯誤) 或 `{error}` (其他寫操作)；(2) **path-guard 串接** — 每條 handler 都先 `path.resolve` 再 `isSensitivePath`，sensitive 命中走 readdir 的 `[]` 或寫操作的 `{error: 'Access denied (sensitive path)'}`；(3) **structured error contract** — 所有寫/讀 handler 回 `{content}` / `{path}` / `{error: string}` 等 object，渲染端 `'error' in result` 早返回 branch 完全相容 Electron 行為。**個別 handler 重點**：(a) `fs.home` — `os.homedir()` 直接回；(b) `fs.readdir` — `READDIR_IGNORED` set 8 條 (`.git/node_modules/.next/dist/dist-electron/.cache/__pycache__/.DS_Store`) 過濾、再過 path-guard、最後 dirs-first + alphabetical sort、map 成 `{name,path,isDirectory}[]`；(c) `fs.readFile` — `512 KiB` cap (`READFILE_BYTE_CAP`) 比 image 的 10 MiB 嚴 — 因為這是給 PathLinker / file preview 等 inline 顯示用，超過 cap 回 `{error:'File too large', size}` 讓 UI 走「Open externally」分支；(d) `fs.listDirs` — 只列 directory entries (not files)、`includeHidden` flag 控制 `.` prefix 過濾、`expandTilde('~/foo')` → `os.homedir() + foo`、回 `{current, parent, entries[]}` shape，**不過濾 READDIR_IGNORED**（讓 Sidebar workspace browser 仍能進 node_modules — 跟 Electron 對齊）；(e) `fs.mkdir` — name validation：trim + reject empty/`/`/`\`/`./..`，避免 path traversal；(f) `fs.deletePath` — `lstat` 後**只**允許 directory 刪除（不含 file，防 remote client 透過這條意外刪檔；想刪檔走 trash 或 explicit handler）、`{recursive:true, force:false}` 確保未存在 path 走 catch；(g) `fs.quickLocations` — 平台分支：win32 列 A-Z drive letters (access check)、posix 加 `/`、darwin 多列 `/Volumes/*` mount points；(h) `fs.resolvePathLinks` — PathLinker 用，輸入 `{cwd, rawPaths[]}` 回 `[{rawPath, path, exists, line?, column?}]`；filter dedup `Set` + `slice(0, 200)` 上限、`length <= 500` 字元限制；regex `^[`'"(<\[]+|[`'"),.;>\]]+$` 剝引號/標點包圍、`(.*?):(\d+)(?::(\d+))?` 抓 `path:line:col` 格式、`RESOLVE_TEXT_EXTS` 53 條延伸名 (ts/tsx/js/jsx/json/css/md/py/rb/go/rs/...) 過濾、Windows-flavoured drive-letter abs path 檢測 (`^[A-Za-z]:[\\/]`)、最後 `fs.stat().isFile()` 確認檔案存在。Wire 點：`server.mjs` 加 `import './handlers/fs.mjs'`；remote-bridge 已建好 `fs:*` channel kebab→camel translation (`fs:list-dirs` → `fs.listDirs`、`fs:read-file` 不存在 — Electron 用 camelCase `fs:readFile`、`fs:resolve-path-links` → `fs.resolvePathLinks`)，handler 註冊完自動接通。**Test**：~30 條 assertions 走全部 8 個 handler — (a) `fs.home` 對 `os.homedir()`；(b) `fs.readdir` — 真實 mkdtemp 建 fixture，斷言 dirs-first + alphabetical sort、`node_modules` / `.DS_Store` 被 IGNORED set 過濾、bare-string params 也 work、missing path → `[]`、sensitive path (`~/.ssh`) → `[]`；(c) `fs.readFile` — UTF-8 round-trip、>512 KiB 回 `{error:'File too large', size}`、`~/.ssh/id_rsa` 路徑回 sensitive error、missing path 回 `{error:'missing path'}`；(d) `fs.listDirs` — 只 directories、`includeHidden:true/false` 兩路斷言、`~` tilde 不丟錯誤、sensitive 拒絕、missing dirPath error、**鎖死「不過濾 node_modules」** contract 跟 Electron 對齊（這是測試文案明確標注的 design choice，未來改了立刻紅）；(e) `fs.mkdir` — 真實建檔、`bad/slash` 跟 `..` reject、missing parent error；(f) `fs.deletePath` — 真實刪 dir、refuse delete file (`/Only directories/`)、sensitive path 拒；(g) `fs.quickLocations` — Home 必有，platform 分支斷言 (win32 至少 1 drive、posix 必含 `/` root)；(h) `fs.resolvePathLinks` — `'real.ts:42:7'` 拿 line=42 col=7 + abs path、`fake.bin` 因 ext 過濾掉、`~/.ssh/id_rsa.pem` sensitive 拒；(i) end-to-end 透過 `protocol.invokeRemoteHandler('fs:list-dirs', [{dirPath, includeHidden}])` 走 remote-bridge 真實鏈路驗證 multi-hyphen channel `fs:list-dirs` 翻 `fs.listDirs` 跟 dispatch 接通。仍綠：sidecar / host-api / cargo / tsc。**結果**：手機連 mac/Tauri host 之後 FileTree 真的可以瀏覽 host 端檔案系統 — `host.fs.readdir(path)` 走 remote invoke → bridge → sidecar fs.readdir → 回 entries；PathLinker 也可以解析 message 裡的 `path:line:col` 連結到 host 端檔案。同時 path-guard 在每個 handler 一律阻止 `~/.ssh / .aws / etc/shadow` 等敏感路徑 — 跟 image namespace 同樣 harm-reduction，三 host 一致。**剩下 PROXIED_CHANNELS 待 port**：(1) `fs:watch / fs:unwatch / fs:changed`（stateful watcher，需要 watchId map + FSWatcher 包 emitter 走 sendEvent broadcast）；(2) `fs:search`（ripgrep-flavoured 全文搜尋，要嘛包 child_process 跑 ripgrep 要嘛純 Node walk）；(3) `pty:*`（4 channel，但 PTY 在 Tauri build 走 Rust portable-pty，sidecar port 需要重新 spawn 一個 child process 走 portable-pty Rust binary 或拿 node-pty — 大刀）；(4) `git:*`（10+ channel，純 child_process 走 git CLI，可以 namespace 一刀小步 port）；(5) `settings:*` / `snippet:*` / `profile:*` / `workspace:*`（settings 跟 workspace 在 Tauri 用 fs 落盤 JSON，sidecar port 直接走 `userData` dir 一致性可達）。**設計 trade-off**：(1) `fs.readdir` 跟 `fs.listDirs` 對 `READDIR_IGNORED` 處理不一致（前者過濾、後者不過濾）— 這是繼承 Electron 的歷史包袱，文案 + 測試明確 lock 住，未來統一前不能默默改；(2) `fs.deletePath` 只刪 dir 是 conservative — 後續若需 file 刪除應該開新 channel 或 explicit flag，避免 remote client 透過 typo 刪掉重要檔；(3) sidecar 的 fs handler 在 Tauri host 本地不會被叫到（Tauri 走 Rust commands），只有 remote bridge 跟 in-sidecar 的下游用戶（目前 0 個）會走，所以本刀對 local Tauri 行為**完全 zero impact** — 純粹增進 remote 功能。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (fs:watch/search、pty/git/settings/snippet/profile/workspace)。）

歷史紀錄：2026-05-10 (#58)。（同上 + **新 #58 — `image.readAsDataUrl` 進 sidecar + `path-guard` 三主機共識**：把第一個 PROXIED_CHANNEL Tauri-only namespace port 進 sidecar，遠端 client 連 Tauri host 之後可以 invoke `image:read-as-data-url` 拿 host 端圖檔縮圖；同時把 deny-list path-guard 從 Electron + Tauri Rust 第三次 port 進 sidecar 確保三邊一致。新檔 `node-sidecar/src/lib/path-guard.mjs` (~80 行) 1:1 port `electron/path-guard.ts`：(1) `DENIED_SUFFIXES` 共 24 條 — SSH keys (`~/.ssh`) / AWS credentials (`~/.aws/credentials,config`) / GCP (`~/.config/gcloud`) / GitHub gh CLI / `~/.netrc` / `~/.pgpass` / k8s `~/.kube/config` / mac Keychain / 4 條 browser credential 樹 (Chrome/Brave/Edge/Firefox) / BAT 自家 secrets (`server-cert.enc.json,server-token.enc.json,claude-account-creds.enc.json` 兩條 mac/Linux 路徑) / `~/.mozilla` / Claude CLI `~/.claude/.credentials.json` / Windows `C:\Windows\System32\config` / system-wide `/etc/shadow,sudoers,/etc/ssh/ssh_host_*_key,/root,/private/etc/master.passwd`；(2) `isSensitivePath(absolutePath)` — empty/non-string 回 true (caller broken)、`path.normalize`、然後雙重 check：deny-list 完全相等或 `startsWith(denied + sep)` 處理 directory containment；(3) **私鑰檔名 heuristic** — `id_(rsa|dsa|ecdsa|ed25519)(\.pub)?` 在含 `${sep}.ssh${sep}` 的 path 一律拒、`*.pem` 在含 `${sep}.ssh${sep}` 或 `${sep}keys${sep}` 的 path 一律拒（覆蓋使用者把 keys 放別的地方的場景）；(4) sibling-directory false-positive guard — `~/.sshfs/config` 不會被當 `~/.ssh`（startsWith 加 sep 後不 match）。新檔 `node-sidecar/src/handlers/image.mjs` (~50 行)：`registerHandler('image.readAsDataUrl', ...)` 等價 Electron `image:read-as-data-url` + Tauri `image_read_as_data_url`：(1) **參數型別 dual support** — Tauri host-api 傳 `{path: filePath}` (object 形式)、Electron preload 傳 positional string；handler 檢測 `typeof params === 'string'` 走 string、否則拿 `params.path`，缺 path throw `'image.readAsDataUrl: missing path'`；(2) `path.resolve` → `isSensitivePath` → throw `'Access denied (sensitive path)'`；(3) ext→MIME map (`.png→image/png, .jpg/.jpeg→image/jpeg, .gif→image/gif, .webp→image/webp`、unknown 預設 `image/png` 跟 Electron / Rust 兩邊一致）；(4) `fs.stat` 確認 size <= 10 MiB，超過 throw `'Image too large (NKB)'`；(5) `fs.readFile` → base64 encode → `data:<mime>;base64,<...>`。Wire 點：`server.mjs` 新 import `./handlers/image.mjs` 接到既有 `wireRemoteBridgeHandlers()` 之前；之前 bridge 對 `image:read-as-data-url` channel 已建好 closure handler 但 dispatch 時拿 `-32601 method not found`，現在 handler 真的存在了 bridge 自動把 invoke 路由進去 — 不需要額外 wire。**Test**：~22 條 assertions 分兩塊。**path-guard 區塊**（11 條）：(a) empty/null/undefined/non-string 4 條都回 true；(b) 友善路徑 — `~/.bashrc` / `~/projects/foo/README.md` 跟 (POSIX only) `/etc/hosts` / `/usr/local/bin/node` 全 false；(c) `~/.ssh` directory containment — 自己跟 `id_rsa` / `known_hosts` 都 true、sibling `~/.sshfs/config` false 鎖死 startsWith+sep 的契約；(d) AWS / GCP / k8s / netrc / Claude CLI creds 5 條 true；(e) 私鑰 heuristic — 任意 .ssh dir 下 `id_ed25519` true、`id_rsa.pub` true、`/srv/keys/host.pem` true、但同檔名換到 `~/docs/id_rsa.txt` false（檔名 regex 嚴格而非實質判斷）；(f) (POSIX only) `/etc/shadow,sudoers,/root,/root/.bashrc` 全 true。**image handler 區塊**（11 條）：(a) round-trip 一張 11-byte synthetic PNG → `data:image/png;base64,...` prefix 對 + base64 decode 跟原 bytes 完全相等；(b) bare-string params (Electron-style) 也 work；(c) ext→MIME 表 5 條 — jpg/jpeg→jpeg / gif→gif / webp→webp / bmp(unknown)→png；(d) 缺 path → JSON-RPC error `/missing path/`；(e) >10 MiB 檔 → error `/Image too large/`；(f) `~/.ssh/id_rsa` (path-guard 拒) → error `/sensitive path/` (handler 在 stat 前就擋下來，不需要檔真存在)；(g) 不存在檔 → error 但 message **不**含 `/sensitive path/` 確保 short-circuit 順序對；(h) 透過 `protocol.invokeRemoteHandler('image:read-as-data-url', [{path}])` end-to-end 走 remote-bridge → dispatch → handler 拿到 dataURL，模擬手機連 host invoke 的真實 wire 路徑。仍綠：sidecar / host-api / cargo / tsc。**結果**：**第一個 Tauri-only namespace 接通遠端**。手機連到 mac/Tauri host 之後 ClaudeAgentPanel/CodexAgentPanel 拖圖到 prompt → `host.image.readAsDataUrl(filePath)` 走 remote invoke → bridge → sidecar handler → host 端讀檔回 base64 → 手機 dataURL 顯示出來。同時 path-guard 在 sidecar 也擋住 ssh key / keychain 等敏感檔，確保任何遠端 client 不能透過 image namespace 抓 secrets。**剩下 PROXIED_CHANNELS 待 port**（同樣 pattern）：`fs:*` (read-file/readdir/list-dirs/quick-locations 等～10 條)、`pty:*` (create/data/resize/destroy)、`git:*` (getRoot/diff/log/status 等)、`settings:*` (load/save/get-shell-path)、`snippet:*` / `profile:*` / `workspace:*` — 其中 `pty:*` 比較硬（ConPTY/wpty 一支可能要直接拿 #16 走過的 portable-pty Rust binding 的 IPC 介面）；`fs:*` / `git:*` 走純 Node fs/child_process，可以照本刀 pattern 一個 namespace 一刀小步 port。**設計 trade-off**：(1) `fs.readFile` 拿到 path-guard 拒掉的話，手機端 ClaudeAgentPanel 把 base64 dataURL 回去再走一次 image render 路徑會看到「sensitive path」error string — 跟 Electron / Tauri 本機 pattern 一致；(2) sidecar 沒像 Tauri Rust 那樣會在 `fs::metadata` 失敗時 fold 成自訂 `CommandError` shape — 直接 fold 進 throw error message 的 stat error，但 error.message 內容相同 (`ENOENT: no such file...`) 行為等價。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port 剩下 PROXIED_CHANNELS namespaces (fs/pty/git/settings/snippet/profile/workspace)。）

歷史紀錄：2026-05-10 (#57)。（同上 + **新 #57 — LiveQuery 整合進 sendMessage：persistent SDK session 上線**：把 #56 ship 的 LiveQuery class wire-in 取代 single-shot `sdk.query()` per-send。**架構轉換**：原本每次 `claude.sendMessage` 都 fresh spawn `sdk.query({prompt, options})` → for-await 整個 generator → 結束關掉；現在第一次送訊息 build 一個 LiveQuery（裡面 sdk.query 跑一次、SDK CLI subprocess 從此常駐）、後續每個 send 直接 `live.push(userMessage)` 進同一個 controlled iterable、await 對應 turn 的 'result' frame。Mac cold-spawn ~3-4s 的痛點從「每送都付」攤平到「session 第一次送付一次」。**claude-send.mjs 結構重構**（從 ~310 行單一 handler closure 拆成 4 個 helper + handler）：(1) `processMessage(s, sessionId, msg)` — 把原本內聯的 system/init / rate_limit_event / stream_event / assistant / user (tool_result) / result 6 大 message-type 路由抽出，純 side-effect function（emit event + 動 lastUsage/sdkSessionId/model/permissionMode），給 LiveQuery onMessage callback 用；(2) `buildQueryOptions(s, sessionId, prompt)` — queryOptions 構建（cwd/preset/permissionMode/effort/model/plugins/canUseTool/CLAUDE_CODE_AUTO_COMPACT_WINDOW env/resume/continue），跟原版完全等價，差別只在 prompt 也傳進來判斷「empty prompt + sdkSessionId → continue:true」（autoContinue 路徑）；(3) `buildUserMessage(prompt, images)` — 產 SDKUserMessage；有 image 走 image+text content blocks、無 image 走純 string content 包 `{type:'user',message:{role:'user',content}}`；(4) `ensureLiveQuery(s, sessionId, sdk, prompt)` — idempotent，已有 open liveQuery 就回；否則 build queryOptions、create AbortController（塞進 queryOptions）、new LiveQuery({sdk, queryOptions, onMessage:processMessage, onError:logWarn})、stash 到 `s.liveQuery` + `s.currentQuery = live.generator`（後者給 claude-readonly 的 supportedCommands/supportedAgents/accountInfo 用，contract 跟 #21 留下的不變）；(5) `closeLiveQuery(s)` exported helper — 給 claude-session.mjs 共用，`s.liveQuery.close() + null out s.liveQuery + s.currentQuery`，**故意不 null abortController** — sendMessage 的 catch branch 還在 await live.push() 時 abortSession 會先 abort+close，catch 醒來要讀 `s.abortController?.signal.aborted` 來區分「aborted」vs「error」reason，所以 abortController 留著等 ensureLiveQuery 下次 rebuild 時自然覆蓋。`registerHandler('claude.sendMessage', ...)` 收斂到 ~30 行：concurrent guard + isResting 喚醒 + SDK-unavailable stub fallback + buildUserMessage + ensureLiveQuery + `await live.push(userMessage)` + try/catch/finally — catch branch 只在 stream-level error/abort 走，turn-level success/error 都由 processMessage 的 result frame 處理（success → claude:result + claude:turn-end:'completed'、!success → claude:error + claude:turn-end:'error'，push() 都 resolve 不 reject）。abort 路徑：abortController.abort() → SDK iterator throw → drain catch → close liveQuery → push reject → sendMessage catch read aborted=true → emit 只 claude:turn-end:'aborted'、不 claude:error。**新 handler `claude.stopTask`**：渲染端取消 sub-agent / Agent tool 用，`{sessionId, taskId | toolUseId}` → `s.liveQuery.stopTask(taskId)` 透過 SDK control protocol 殺掉特定 task；無 liveQuery 回 `{ok:false, error:'no active live query'}` 不 throw（早期 panel mount 還沒送過訊息使用者就按 stop 不該炸）；缺 sessionId/taskId 同步 throw。**claude-session.mjs 5 個 lifecycle handler 全 wire closeLiveQuery**：(a) `resumeSession` — 在 abort 既有 abortController 之後、delete session record 之前 close 既有 liveQuery，下次 sendMessage 拿新 sdkSessionId 走 resume rebuild；(b) `restSession` — abort + closeLiveQuery (殺 SDK CLI subprocess 不浪費 token/CPU)，wake 後 sendMessage 自動 rebuild；(c) `stopSession` — abort + closeLiveQuery + delete record；(d) `abortSession` — abort + closeLiveQuery（明確 close 喚醒 pending push 立即 reject，不等 SDK throw 自然傳到 iterator）；(e) `resetSession` — abort + closeLiveQuery + delete record。**setPermissionMode / setModel 接 SDK 控制方法**：先更 session state（給下次 rebuild 看到），如果 liveQuery open 走 control method `setPermissionMode(sdkMode)` / `setModel(model)`（mode 'bypassPlan' 對 SDK 不認得仍 map 'plan'）；control method throw（舊 CLI 不支援）就 closeLiveQuery，下次 sendMessage rebuild 時 queryOptions 自然帶新值。`autoCompactWindow` 變化（讀 env 的東西）直接 closeLiveQuery 強制 rebuild — 沒 control method 可改。**Test fixture 改寫**（streaming-input mode fake SDK）：原 image-attachment 測試 fake SDK 從 single-shot 改成「drain prompt iterable 拿第一個 user message」pattern，斷言點從「外部 for-await 拉 prompt iterable」改成「fake SDK 內部 capture user message」(controlled iterable 的 yield 在 SDK 端消費，外部再拉會跟內部 racing 而且 LiveQuery 已 closed 進不了 while loop)。原 `restSession` 測試的「rest must drop abortController」斷言改「rest must close liveQuery」+ 註釋說明 abortController 故意不清為了 catch path 讀 .aborted。**新增測試 4 大塊**：(A) **streaming-input persistent query** — 兩次 `claude.sendMessage` 後 `fakeSdkStreaming.queryCalls === 1`（核心對比，非 single-shot 應該是 2）、收到兩條 `claude:result` event 且 result 串對 `reply-1-1` / `reply-1-2`、`s.liveQuery.isClosed === false` + `s.currentQuery` 有值；(B) **claude.stopTask** — 沒 liveQuery 時 `{ok:false, error:/no active live query/}`、send 過後 stopTask 透過 generator.stopTask call 進去（fake captures taskId）、`toolUseId` 別名 fallback、缺 sessionId/taskId reject；(C) **setPermissionMode/setModel control 方法** — 'plan' / 'bypassPlan'→'plan' / 'claude-opus-4-7' 都透過 generator control method、liveQuery 仍 open；autoCompactWindow change closes liveQuery；(D) **lifecycle cleanup** — abortSession / stopSession / resetSession / resumeSession 各自 generator.close() count 增加 + s.liveQuery null。整體 sidecar test ~328 → ~370 條 (+~42)。**未動**：(i) abort 路徑現有測試（slowFakeSdk yield→abort→turn-end:'aborted'）行為一致 — abort 從 abortController.abort() 觸發 SDK throw 走 drain catch 走 push reject 走 sendMessage catch，最終 turn-end:'aborted' 同樣產生；(ii) parity test (queryOptions 鎖 19 條 key contract) — 新版 buildQueryOptions 行為等價，含 prompt-empty→continue:true 重做進 buildQueryOptions；(iii) resumeSession 測試（單次 send，single-shot fake SDK 行為下 `__queryArgs.resume` 的 contract 不變，因為 fake SDK 一輪就完導致 liveQuery close→第二輪重 build 帶 resume）。仍綠：sidecar / host-api / cargo / tsc。**結果**：使用者下次 mac 端輸入第一個訊息仍付 cold-spawn ~3-4s（無解，CLI 必須起來），第二個訊息**只付 API roundtrip**（沒 spawn），按 Stop 走 `claude.stopTask` 真的能殺 sub-agent。同時 `setPermissionMode` / `setModel` 從原本「下次 sendMessage 才生效」變成 mid-stream control method 即時生效（SDK 支援的話）。**已知 trade-off**：(1) SDK 有些 frame 路徑（partial_assistant, tool_progress 等）current routing 跟原版相同 — additive，未來再加；(2) abortController 故意不在 closeLiveQuery 裡 null 的設計需要記得 — 加註解 + 測試文案；(3) ensureLiveQuery 是 async (await loadInstalledPlugins)，第一次送訊息有不可避免的微 delay；後續 send 走 fast-path（已有 open liveQuery 直接 return）；(4) liveQuery rebuild 路徑（abort/reset 後再 send）會再付一次 cold-spawn — 這是預期行為，因為 SDK CLI 真的被殺了。剩下：(1) Tauri 原生 onDragDropEvent → folder drop path resolver、(2) Tauri bundle 瘦身、(3) MCP tools list categories、(4) ctx.windowId 透傳、(5) Port missing PROXIED_CHANNELS namespaces (pty/git/fs/settings/snippet/profile/workspace/image)。）

歷史紀錄：2026-05-10 (#56)。（同上 + **新 #56 — LiveQuery 模組（streaming-input mode 基礎建設，未 wire-in）**：persistent SDK session 是 mac send-hang 最大解，但需要重寫整個 send loop + test fixture。本刀分兩步走的第一步：**單純把 `LiveQuery` class + 完整測試先 ship**，sendMessage 還沒消費它，等 API surface 鎖死後下一刀再做整合。新檔 `node-sidecar/src/lib/live-query.mjs` (~150 行) 封裝 streaming-input mode 的 SDK Query lifecycle：(1) **controlled async iterable** — 內部 queue + waker pattern，`prompt[Symbol.asyncIterator]()` yield 隨 push() 進來的 SDKUserMessage、close/done 時把 waker 喚醒讓 iterator 結束；(2) **per-turn FIFO deferreds** — 每個 push 推進一個 `{resolve, reject}` 進 turnDeferreds queue，drain loop 看到 `type:'result'` 就 shift 一個 resolve，多個 push 並行也鎖 turn 順序；(3) **背景 drain loop** 在 constructor 立刻啟動 fire-and-forget 跑 `for-await-of generator`，每條 message call onMessage callback、generator throw 走 onError + reject 全部 pending pushes、finally 把 closed=true + waker fire；(4) **控制方法 surface** — `stopTask(taskId)` / `interrupt()` / `setPermissionMode(mode)` / `setModel(model)` 各自 forward 到 `generator.<method>`，沒接 (SDK build 不支援) 直接 throw `'not supported by this SDK build'` 而非 silently no-op，closed 狀態 reject `'closed'` 防 dead-generator call；(5) **idempotent close()** — 第二次 call no-op，generator.close() 只跑一次；(6) **constructor validation** — missing sdk.query / onMessage 同步 throw 不 defer 到 runtime。**Test**（streaming-input fake SDK）：~30 條斷言 — (a) 2 條 constructor validation throw；(b) single-turn round trip — sdk.query 只跑 1 次 (vs 多次 push 也只 1 次)、push() resolve 拿 result frame、onMessage 看到 system+assistant+result 三條 in order、close() 後 isClosed=true 且 generator.close() 跑了 1 次、第二次 push 沒 rebuild query；(c) FIFO turn deferreds — 兩個並行 push 各拿到不同的 turn 結果、順序與 push 順序對齊；(d) 控制方法 forward — stopTask/interrupt/setPermissionMode/setModel 各自 forward 到 generator 上 method、call 數累加、close 後全 reject `closed`、push 也 reject `closed`；(e) bare SDK without 控制方法 — 4 條 reject `'not supported by this SDK build'`（用 idle-generator 防 auto-close）；(f) generator throw → onError fires + pending push reject + isClosed=true；(g) onMessage throw 走 onError 但 drain loop **不**停（後續 message 還是繼續處理）；(h) idempotent close — 二度 call no 二度 generator.close；(i) close() 中斷 in-flight push — 用 idle-fake-SDK 模擬 push 永不 resolve，setTimeout 後 close、push reject `closed`。仍綠：sidecar 298+30=328 條 / host-api / cargo 114 / tsc。**結果**：API surface (push/stopTask/interrupt/setPermissionMode/setModel/close + isClosed + onMessage/onError callback contract) 鎖死可以給下一刀的 sendMessage 重寫直接消費。LiveQuery 在 production 暫不啟用，現有 single-shot sendMessage 路徑零變更，無 regression 風險。**下一刀整合計劃**：(1) 把 claude-send.mjs 改用 LiveQuery — 第一次 sendMessage 建 instance 跑 `query()` 一次、後續 push 進 queue 直接 await turn deferred；(2) onMessage callback 把現有的 system/init / stream_event / assistant / user (tool_result) / result / rate_limit_event 五大 message 路由全搬進去；(3) 加 `claude.stopTask` handler call `s.liveQuery.stopTask(toolUseId)`；(4) 改 abortSession / resetSession / stopSession 走 close() + interrupt()；(5) test fixture 從 single-shot fake SDK 改 streaming-input — 把每個 sendMessage 對應的 `__queryArgs` 計數從 N 改 1，把「second send 帶 resume」改「second send 共用 query 不重建」。剩下：(1) **整合 LiveQuery 進 sendMessage**（下一刀）、(2) Tauri 原生 onDragDropEvent → folder drop path resolver、(3) Tauri bundle 瘦身、(4) MCP tools list categories、(5) ctx.windowId 透傳、(6) Port missing PROXIED_CHANNELS namespaces。）

歷史紀錄：2026-05-10 (#55)。（同上 + **新 #55 — PromptBox 100ms 等待移除**：使用者反映普通 prompt 送出（無 image）會莫名滯後 100ms，量化路徑：`PromptBox.handleSend` flow 是 `pty.write(content) → setTimeout(100) → pty.write('\\r')`，那個 100ms padding 是 #f386589 原始實作就有的、沒寫理由。實際上 sequential `pty.write` 共用一條 OS pipe，kernel level FIFO，不需要 inter-write delay。修法：把 line 50 的 `await new Promise(setTimeout(100))` 移除，加註解說明 (1) 沒 image 場景不需要等、(2) image 場景的兩個 wait 為什麼留著（100ms 是給 OS clipboard publish 時間給 paste keystroke 抓得到、800ms 是給 terminal app render image 完才送 Enter）。沒 image 的 prompt 從 100ms→0ms 滯後 — 對快輸快發場景立感受得到。仍綠：sidecar / host-api / cargo / tsc。**結果**：使用者按 Ctrl+Enter 送純文字訊息現在「按下去就走」沒有體感停頓；image-paste 場景一切照舊（兩個 wait 都還在）。剩下 (1) **persistent SDK session for sendMessage** — 接著做、(2) Tauri 原生 onDragDropEvent → folder drop path resolver、(3) Tauri bundle 瘦身、(4) MCP tools list categories、(5) ctx.windowId 透傳、(6) Port missing PROXIED_CHANNELS namespaces (pty/git/fs/...)。）

歷史紀錄：2026-05-10 (#54)。（同上 + **新 #54 — sidecar logger 落盤 + sidecar.getLogPath JSON-RPC handler**：mac send-hang 一直缺缺診斷工具，#43 加的 stderr-tail 只在 sidecar exit 那一刻才被 Tauri Rust 抓到，正常運行的 sidecar stderr 完全沒人讀。新檔 `node-sidecar/src/lib/logger.mjs` (~95 行) 把 stderr 同步鏡到 `<dataDir>/sidecar.log`：(1) `initLogger()` 開檔 append 模式、parent dir auto-mkdir 0o700、檔案 mode 0o600（跟 #47 remote-secrets 對齊，避免不慎洩漏 token/PII 至 world-readable）；(2) >5 MB rotate-on-init — 直接 truncate 不滾檔（sidecar 每次 Tauri 啟動都重 init，自然 reset；single-file 沒 cleanup 邏輯）；(3) `log/info/warn/error(...)` 四級寫 ISO timestamp + level tag + 訊息一行；Error 物件 fold stack（多行）、其他 object 走 JSON.stringify、string 直接拼；(4) **double-write**：每一行都 `process.stderr.write` + 檔案 append，所以 #43 的 stderr-tail capture 完全相容、不破壞 Tauri Rust 的 child-exit diagnostic；(5) 寫檔 try/catch silent 永不 throw — disk full / 權限問題不能炸 JSON-RPC loop；(6) `attachProcessHooks()` 綁 `uncaughtException` / `unhandledRejection` 寫 log，idempotent；(7) 測試 seam `__setLogPathOverrideForTests(path)` 給 tmpdir 用避免污染 userData。Wire 點：`server.mjs` 的 `main()` 第一行 call `initLogger() + attachProcessHooks()`，所以 stdin readline 起來之前就已經在記錄。新 `sidecar.getLogPath` JSON-RPC handler — 渲染端日後可加「Open log file」按鈕或 bug-report 流複製 path。順便把 ad-hoc `process.stderr.write('[sidecar] ...')` 全 4 處（claude-send.mjs SDK-unavailable / claude-history.mjs forkSession / fetchSubagentMessages / archiveMessages）改 `logWarn(...)` 走新 logger，效果是錯誤訊息現在會落到使用者磁碟可以 post-mortem。**Test**：sidecar 加 ~15 條斷言 — (a) initLogger 真的建檔；(b) log/info/warn/error 4 條寫成 4 條 ISO-prefix entry；(c) 順序鎖（INFO 在 WARN 前、WARN 在 ERR 前）；(d) Error 物件 fold 出 stack 含 'kaboom'；(e) sidecar.getLogPath dispatch 回 `{path}`；(f) POSIX mode 0o600（Windows skip）；(g) >5 MB rotate-on-init truncate；(h) attachProcessHooks idempotent (listener count delta ≤1)；(i) 寫檔失敗 silent (deleted dir 不 throw)。仍綠：sidecar 283+15=298 條 / host-api / cargo 114 / tsc。**結果**：使用者下次 mac 端 sidecar 跑 `tail -f ~/Library/Application\ Support/better-agent-terminal/sidecar.log` 就能看到實時 SDK error / handler warn / uncaughtException，不再需要等 sidecar exit 才看到 stderr-tail 那十行；對追 mac send-hang 是直接的診斷管道（特別是讓使用者在卡住時把 log 貼來分析）。剩下：(1) **persistent SDK session for sendMessage**（streaming-input mode + claude.stopTask + 控制方法 setPermissionMode/setModel mid-stream — 規模較大需要重寫整個 send loop + test fixtures；現在有 logger 落盤可量測前後 cold-spawn 差異）、(2) Tauri 原生 onDragDropEvent → folder drop path resolver、(3) PromptBox 100ms 等待移除、(4) Tauri bundle 瘦身、(5) MCP tools list categories、(6) ctx.windowId 透傳到 sidecar dispatch（remote bridge 後續）、(7) 把 PROXIED_CHANNELS 漏掉的 namespace（pty/git/fs/settings/snippet/profile/workspace/image）port 進 sidecar 讓遠端 client 全 channel work。）

歷史紀錄：2026-05-10 (#53)。（同上 + **新 #53 — invoke handler bridge 上線：60+ PROXIED_CHANNELS 自動接通 sidecar JSON-RPC dispatch**：把 #51 留下的「`invoke` frame 進 server → 撞 `Channel is allowed but not yet bridged to sidecar dispatch` error」收尾。新檔 `node-sidecar/src/lib/remote-bridge.mjs` (~70 行) 兩條：(1) `channelToMethod(channel)` — kebab→camel 翻譯（`claude:start-session` → `claude.startSession`、`image:read-as-data-url` → `image.readAsDataUrl`、`profile:get-active-ids` → `profile.getActiveIds`；保留已 camel 的（`snippet:toggleFavorite` → `snippet.toggleFavorite`、`git:getRoot` → `git.getRoot`）；input validation：empty/non-string throw 不 silently 回 garbage）；(2) `wireRemoteBridgeHandlers()` 對每個 PROXIED_CHANNEL 建一個 closure handler — 收到 `invokeRemoteHandler(ctx, ...args)` 就把 `args[0]` 當 JSON-RPC `params` 餵 `dispatch({method, params})`，reply 有 error 就 throw 帶原 message + code、result 直接回。idempotent (`hasRemoteHandler(channel)` skip 重複 register)。Wire 點：`server.mjs` 在所有 `./handlers/*.mjs` import 完之後立刻 `wireRemoteBridgeHandlers()`，這樣 dispatch 拿得到全部已註冊 sidecar handler。**Wire format 設計選擇**：remote frame 是 Electron-flavoured 的 `args:[positional...]` array，sidecar handler 全是 named `{params}`；bridge 直接拿 `args[0]` 當 params，意思是 Tauri 端遠端 client 連 Tauri 端遠端 server 時兩邊 host-api 都會把單一 params object 包成 `args:[params]`，跨建構（Electron client 連 Tauri server）只會吃到 `args[0]`，`args[1..n]` 被丟掉 — 短期內 Electron build 還在所以這是已知 trade-off，等 Electron 退休後 args[0] 就是全部輸入。**未實作 channel** 的處理：sidecar 沒對應 handler 的 channel（`pty:*` / `git:*` / `fs:*` / `settings:*` / `github:*` / `snippet:*` / `profile:*` / `workspace:*` / `image:*` 全在 Tauri Rust commands 不在 sidecar 程序）走 dispatch 後拿到 `-32601 method not found`，bridge re-throw 成 invoke-error，渲染端 `'error' in result` branch 完全相容，差別只在錯誤訊息從「not yet bridged」變成「method not found」。**Test**：~25 條新斷言 — (a) 8 條 `channelToMethod` 翻譯契約 + 2 條 input validation throw、(b) `wireRemoteBridgeHandlers` idempotent (second-run 回 0)、(c) 直接 `invokeRemoteHandler('claude:auth-status', [])` 拿到 null 或 auth object（透過 dispatch 真實打）、(d) 不在 sidecar 的 `pty:create` 拿到 `method not found`、(e) `claude:get-supported-models` 帶 `args[0]={cwd}` 拿到 array 證明 args[0]→params 路徑通；同時把舊 #51 的「`claude:auth-status` 拿 not yet bridged」斷言改成 invoke-result null/object（contract flip 因為現在真接通了），相應更新 remote-client-impl 那塊 (f) 路徑跟 dispatch (l) `remote.listProfiles` 的訊息 match 從 `not yet bridged|not exposed` 改成 `method not found|not exposed`。**Test seam 修法**：原 remote-protocol 那塊測試會 call `__resetRemoteHandlersForTests()` 清掉 registry，但 server.mjs import 時已經 wire 過 bridge — reset 之後後續 remote-server-impl block 就拿不到 bridged handlers。修：在 reset 後立刻再 call `wireRemoteBridgeHandlers()` re-wire，加 sanity 斷言「`claude:auth-status` exists 且 registry size >= 50」鎖死 production wiring 跟測試環境一致。仍綠：sidecar 258+25=283 條 / host-api / cargo 114 / tsc。**結果**：使用者下次手機連 Tauri host 走 SettingsPanel 的 QR scan，連上之後遠端 ClaudeAgentPanel 真的能 invoke `claude.startSession / sendMessage / authStatus / getSupportedModels` 全套 — 任何 sidecar 已實作的 channel 都自動接通。**limitation**：(1) PTY / git / fs / settings / github / snippet / profile / workspace / image 還在 Tauri Rust，遠端拿 `method not found`，得等那批 namespace port 進 sidecar；(2) 跨建構（Electron client → Tauri server 或反向）positional args 多於 1 個會丟掉 args[1..]，這條未來 Electron 退場時自然消失；(3) ctx.windowId 沒透傳到 sidecar dispatch（sidecar 不分 window），handler 自己 derive。Phase 3 remote namespace 從 80% → **95%** 完工，剩 (1) Electron build 同步加 sidecar 對應 PROXIED_CHANNEL handler 把 missing 那批補齊（漸進式，需要的 channel 才補）、(2) 如果要支援 ctx.windowId 把它丟進 params 給 handler 自己讀。剩下：(1) **persistent SDK session for sendMessage** — typing→send 體感最大解、(2) Tauri 原生 onDragDropEvent → folder drop path resolver、(3) PromptBox 100ms 等待移除、(4) Tauri bundle 瘦身、(5) `claude.stopTask`、(6) MCP tools list categories、(7) sidecar log persisted to file。）

歷史紀錄：2026-05-10 (#52)。（同上 + **新 #52 — remote-client.ts port + connect/disconnect/clientStatus/testConnection/listProfiles 全部接通真實 client**：把 `electron/remote/remote-client.ts` (287 行) port 進 sidecar 新檔 `node-sidecar/src/lib/remote-client-impl.mjs`，改寫對 sidecar 環境的兩處 adaptation：(1) Electron 版用 `BrowserWindow.webContents.send(channel, ...args)` fan-out 收到的 `event` frame 給渲染端，sidecar 改成 `sendEvent(channel, {args})` 透過 stdout JSON-RPC notification 流推到 Tauri renderer；新 `__setRemoteClientEmitForTests` 是 in-process 測試用的 emit 攔截 hook；(2) Electron 版用 `logger` 寫 `debug.log`，sidecar 預設改寫 `console.error('[RemoteClient]', ...)` 走 stderr 不污染 stdout JSON-RPC channel，新 `__setRemoteClientLoggerForTests({log,error})` 給測試靜音 expected error spam（fingerprint mismatch / wrong-token close path）。其他全保留：fingerprint pinning（`ws._socket.getPeerCertificate(false).fingerprint256` 跟 user-pinned 兩邊都 `normalizeFingerprint` 比對）、auth token frame `{type:'auth', token, args:[label]}` 5s timeout、reconnect exponential backoff (BACKOFF_BASE=3000, MAX=30000) ±25% jitter、generation 計數防 stale reconnect、PendingInvoke `Map<id, {resolve,reject,timer}>` + 30s default timeout、`isConnected` 同時看 `_connected && ws.readyState===OPEN`、disconnect bumps generation 拒所有 in-flight reconnect。新增 input validation — connect missing host/port/token/fingerprint 各條都同步 reject 不 spawn socket、invoke channel empty 同步 reject。setTimeout 全加 `.unref()` 防擋 sidecar exit。`handlers/remote-tunnel.mjs` 把原來 5 條 stub (connect/disconnect/clientStatus/testConnection/listProfiles) 全改 wire 到 singleton `RemoteClient`（lazy-instantiated 跟 RemoteServer 對齊）— `remote.connect` 回 `{connected:true, info}` / `{connected:false, error}` 結構化（Electron 是 raw boolean，sidecar wrap 得 renderer 的 `'error' in result` 早返回 branch 仍正常）；`remote.testConnection` 跟 `remote.listProfiles` 都 spawn ephemeral RemoteClient instance（不污染 singleton），listProfiles 額外 invoke `'profile:list'` channel 把 result 過濾成 `{profiles:[{id,name,type}], activeProfileIds}` shape — 但 server 端 invoke handler bridge 還沒接（這是下一刀），所以 `listProfiles` 目前會 surface server 的 `'not yet bridged'` error（測試已鎖死這個 contract）。**Test**（sidecar in-process via 真 RemoteServer + 真 ws TLS handshake）：~50 條斷言 — (a) happy path connect → `connected:true / info:{host:'127.0.0.1',port}` + server status 看到 labeled client；(b) fingerprint mismatch (用 `'AA:'.repeat(31)+'AA'` 假 fp) → resolve false、isConnected false；(c) wrong token → auth-result error → resolve false；(d) 4 條 connect-time validation reject (`fingerprint required` / `host required` / `port required` / `token required` / `options required`)；(e) invoke before connect → `Not connected`；(f) invoke round trip — allowlisted-but-unbridged `claude:auth-status` 拿 `not yet bridged` error / non-allowlisted `evil:nuke-fs` 拿 `not exposed remotely`、empty channel sync reject；(g) event fan-out — server side `broadcastHub.broadcast('claude:message', payload)` → 客戶端 emit hook 收到完整 channel + args、`fake:ignored` 不在 PROXIED_EVENTS allowlist 不 fire；(h) pending invoke 在 disconnect 後 reject 不 leak；(i) reconnect after explicit disconnect 仍可成功（generation 機制不卡死）；(j) handler dispatch 整套 — `remote.connect / clientStatus / disconnect` 走 dispatch 真實流；(k) `remote.testConnection` happy path / wrong-fp / wrong-token 三路；(l) `remote.listProfiles` 透過 dispatch 拿到 `not yet bridged` 的 error string contract。**handler-side test cleanup**：原來 #45 留下的「remote.connect/testConnection/listProfiles 是 stub」三條 sanity 斷言，這刀改成「missing-fingerprint validation contract」斷言（仍是 `{error}` shape、但是 input-validation 路徑而非 stub 路徑），渲染端 `'error' in result` 邏輯完全不變。仍綠：sidecar 208+50=258 條 / host-api / cargo 114 / tsc。**結果**：使用者按 SettingsPanel 的「Test connection」按鈕，sidecar 真的開 TLS WebSocket → 對 fingerprint → 送 auth → 拿 `auth-result{result:true}` → close → 回 `{ok:true}`；按「Connect」按鈕也是真的進 connected 狀態、`remote.clientStatus` 真的回 `{connected:true, info}`；server 端透過 broadcastHub emit 的 PROXIED_EVENTS 也能即時推到 Tauri renderer。**唯二**還沒接通的是：(1) **invoke handler bridge** — 60+ 個 PROXIED_CHANNELS 還沒 register handler 路由到 sidecar JSON-RPC dispatch，所以遠端 client 連上之後 invoke 任何 channel 都會拿 `not yet bridged`；這刀完工後 `remote.listProfiles` 跟所有跨主機 invoke 都會自動 work。(2) **persistent SDK session for sendMessage** — typing→send 體感最大解（mac cold-spawn ~3-4s），需要 streaming-input mode 改寫。剩下：(1) **invoke handler bridge** for incoming RPC、(2) **persistent SDK session for sendMessage**、(3) Tauri 原生 onDragDropEvent → folder drop path resolver、(4) PromptBox 100ms 等待移除、(5) Tauri bundle 瘦身、(6) `claude.stopTask`、(7) MCP tools list categories、(8) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#51)。（同上 + **新 #51 — typing path 收尾結論 + remote.startServer / stopServer / serverStatus 真實實作 (WebSocket 上線)**：(A) typing 收尾 — 五輪 audit 已 cover keystroke 路徑 / IME / textarea uncontrolled / auto-resize / history nav / slash menu / paste 圖檔 / 拖檔 fallback / streaming reconcile，沒有任何阻塞點。typing 本身在 React 層已經是 baseline-fast。剩下唯一的「可量化阻塞」是 Enter→handleSend→`claude.sendMessage` invoke 的 cold-spawn SDK ~3-4s（#43 timing diagnostic 等使用者重現拿 sync/invoke/total log）。其他 UX-flavoured 的 typing 議題（PromptBox 100ms 等待、Tauri 原生 onDragDropEvent → folder drop path resolver）已 backlog。**typing audit 段落收尾**。(B) remote port 終於上線 — `pnpm --dir node-sidecar add ws` 加 `ws` dep，新檔 `node-sidecar/src/lib/remote-server-impl.mjs` 真實 port `electron/remote/remote-server.ts`：(1) `https.createServer({cert,key})` 用 #50 的 `ensureCertificate(configDir)` 拿 self-signed cert + key、`new WebSocketServer({server, maxPayload:32MB})` 上層接 WebSocket；(2) per-connection auth flow — 5s timeout、frame `type:'auth'+token` 對齊就升級成 authenticated client、token 不對 record failure；(3) brute-force ban — 5 fails / 60s window 觸發 10min ban；(4) heartbeat — 每 30s `ws.ping()` keep-alive，setInterval 用 `.unref()` 不擋 sidecar exit；(5) broadcast fan-out — `broadcastHub.on('broadcast', listener)` 攔截 sidecar emit、過 `PROXIED_EVENTS` allowlist 後 JSON.stringify 推給每個 OPEN client；(6) ping/pong app-level frame；(7) invoke handler bridge — 檢查 `PROXIED_CHANNELS` allowlist 再檢查 `hasRemoteHandler` registry，如果 channel 在 allowlist 但沒 handler 註冊（這刀的狀態，registry 還空的）回 invoke-error `'not yet bridged to sidecar dispatch'`，下一刀做完真實 handler bridge 自然 plug-in。token 持久化走 #47 的 `writeEncryptedString` envelope（`<configDir>/server-token.enc.json`），re-launch 同一個 dir 拿到同一個 token；legacy `server-token.json` plaintext fallback migrate-on-read。`start({port:0, bindInterface:'localhost'})` OS-assign port 給測試用，real 用法是 9876。`handlers/remote-tunnel.mjs` 把 startServer / stopServer / serverStatus 從 stub 改 wire 到 RemoteServer singleton（lazy create with `resolveDataDir()`）；client-side 4 條 (connect/disconnect/clientStatus/testConnection/listProfiles) 仍 stub 等下一刀（remote-client.ts port）。`tunnel.getConnection` 改成 server running 時拿 live `{url, token, fingerprint, mode, addresses}`，否則回 #45 的 `{error, addresses}` shape — 完全跟 Electron 對齊。**Test**（sidecar in-process via mkdtempSync + 真 ws client 連線）：~30 條斷言 — (a) start/stop/status — pre-start status `{running:false, port:null, ...}` / start 拿 port>0 + 32-char hex token + fingerprint format / status mid-run / token file 落盤 envelope shape；(b) auth happy — `WebSocket(rejectUnauthorized:false)` 連到 wss://127.0.0.1:port，送 `{type:'auth', token, args:['my-laptop',{windowId:'w1'}]}` 拿 `auth-result{result:true}`、status.clients 浮現含 label/windowId、ping/pong frame round-trip；(c) invoke 路徑 — 不在 allowlist channel `evil:nuke-fs` → invoke-error `not exposed remotely`，allowlist 上但 no-bridge `claude:auth-status` → invoke-error `not yet bridged`；(d) auth fail — 錯 token 拿 `auth-result{error:'Invalid token'}` + ws.close；(e) restart token 持久化 — 同一個 dir 起新 server2，`startResult.token` 跟原 server byte-identical，cert fingerprint 也一樣；(f) brute-force ban contract — 5 個 recordAuthFailure / IP banned / 過期 window 後 reset count / clearAuthFailures 清空；(g) constructor validation — empty/null configDir throw。仍綠：sidecar 178+30=208 條 / host-api / cargo 114 / tsc。**結果**：`remote.serverStatus()` 終於回真實 running:true + port + fingerprint，使用者按 SettingsPanel 的「啟動 server」按鈕 — sidecar 真的開 https + WebSocket、產 cert、token persist、QR code 畫得出可掃。invoke 還沒接通所以遠端 client 連上之後不能真的 drive host（auth/ping/pong/broadcast 都正常，但 channel 都拿到 `not yet bridged`）— 這是下一刀的工作（registerRemoteHandler 把每個 PROXIED_CHANNEL 路由到對應的 sidecar JSON-RPC dispatch 或既存 handler）。整個 remote namespace 的 server-side 樹已 80% 完工：剩下 (1) **invoke handler bridge** （registerRemoteHandler 各 channel）、(2) **remote-client.ts port** （outgoing connect/disconnect/testConnection/listProfiles，給 host A 連 host B 的場景）。（同時手動把 #45 sidecar 測試裡 obsolete 的 `remote.startServer → {error}` stub 斷言移除，因為 startServer 不再回 stub。）剩下：(1) **invoke handler bridge** for incoming RPC、(2) **remote-client.ts port**、(3) **persistent SDK session for sendMessage** — typing→send 體感最大解、(4) Tauri 原生 onDragDropEvent → folder drop path resolver、(5) PromptBox 100ms 等待移除、(6) Tauri bundle 瘦身、(7) `claude.stopTask`、(8) MCP tools list categories、(9) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#50)。（同上 + **新 #50 — typing path 五次審計（streaming reconcile cost 量化結論） + remote-certificate ensureCertificate 真實實作**：(A) typing 五次 audit — 把 ClaudeAgentPanel 的 `setMessages` 點全列出共 30+ 處（streaming onMessage / onToolUse / onToolResult / onError / onResult / onTurnEnd 各自會打）。每條 streaming message 一進來 → `setMessages(prev => [...prev, item])` 觸發 React reconcile，messages array 越長越貴；不過 message item 本身是 plain object、不重 deserialize、render path 是 markdown component memoised。**結論**：streaming 過程中 typing 的「JS thread 被 reconcile 占住」最壞 case 大約幾 ms 級別，不可能 hang 住「整個輸入框幾秒不能打字」。所以使用者前次「mac 輸入訊息會 hang」必然是 send 時刻（cold spawn SDK ~3-4s 的 sendMessage invoke）— #43 timing diagnostic 等使用者重現拿 sync/invoke/total 三段分布。（typing audit 五輪基本可以收尾：keystroke 路徑、IME 處理、textarea uncontrolled 模式、auto-resize、history nav、slash menu、paste/drag-drop fallback、streaming reconcile 全鎖死，沒有任何阻塞點。）(B) remote port 推進 — 把 `electron/remote/certificate.ts` 的 `ensureCertificate(configDir)` 真實 port 進 sidecar，不再 stub。`pnpm --dir node-sidecar add selfsigned` 加 dep（^5.5.0、+23 deps 總共 23 + 7 = 30 packages，build-script 已被 pnpm v10 onlyBuiltDependencies 過濾），新檔 `node-sidecar/src/lib/remote-certificate.mjs`：(1) load `<configDir>/server-cert.enc.json`（透過 #47 的 `remote-secrets` envelope 讀），如果 file/data 缺欄位（`cert`/`privateKey` string 都齊）就 regenerate；(2) `selfsigned.generate({...})` 2048-bit RSA、SHA-256、validity 3650 天（10 年 self-signed，使用者透過 fingerprint pin 控制信任，不靠 CA），SAN 加 `localhost` / `127.0.0.1` / `::1` 涵蓋 loopback；(3) regenerate 後立刻 writeEncryptedJson 落盤，persist 失敗 warn 但不 throw（serve TLS 這次還是 OK，下次 launch 重 generate）；(4) auto-`mkdirSync(configDir, recursive:true, mode:0o700)` 給 cert 目錄加 owner-only perm。fingerprint256 每次都用 #49 `computeFingerprint(cert)` 從 PEM 即時算 — **不**存到磁碟，避免 cert 跟 fingerprint 漂移。input validation：empty/non-string configDir 同步 throw。**Test**（sidecar in-process via mkdtempSync，~17 條斷言）：(1) fresh dir 觸發 generate → cert/privateKey PEM header 對、fingerprint256 length 95 + format 鎖定 + 跟手算 computeFingerprint 對齊；(2) on-disk envelope shape 鎖（`{enc:false, data:string}`）；(3) reload 同一個 dir → 拿到 byte-identical cert + privateKey + fingerprint，**不** regenerate（contract 鎖死「下次 launch 不重發 cert」）；(4) corrupt file（覆寫 'not-json-at-all'）→ regenerate 不 throw，新 cert fingerprint 跟原始不同；(5) missing-fields envelope (`{enc:false, data:'{}'}`) 也 trigger regenerate；(6) empty/null configDir reject (`非-empty string` error)。仍綠：sidecar 161+17=178 條 / host-api / cargo 114 / tsc。**結果**：本刀使用者面向沒有可見變化（remote 還在 stub），但 sidecar 已可一句 `await ensureCertificate(dataDir)` 拿到 production-grade TLS cert + pin — 下一刀 WebSocket server 直接 plug-in `https.createServer({cert, key})` + WebSocket upgrade。剩下：(1) **WebSocket server 真實實作** (`ws` npm dep + ~400 行：handshake / auth token / brute-force ban / heartbeat / frame multiplex)、(2) **persistent SDK session for sendMessage** — typing→send 體感最大解、(3) Tauri 原生 onDragDropEvent → folder drop path resolver、(4) PromptBox 100ms 等待移除、(5) Tauri bundle 瘦身、(6) `claude.stopTask`、(7) MCP tools list categories、(8) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#49)。（同上 + **新 #49 — typing path 四次審計（textarea uncontrolled invariant 鎖定） + remote broadcast-hub + cert fingerprint helpers**：(A) typing 四次 audit — 把 textarea declaration 走完最後一段：`<textarea ref={textareaRef} defaultValue="" onInput={handleInputChange} ... />` line 3806 — **uncontrolled** mode（value 屬性沒接、defaultValue 只在 mount 時用一次），意思是：(i) 任何 ClaudeAgentPanel 的 re-render（streaming message 進來、tool use 更新、status 變化）都**完全不**碰 textarea 的 DOM value，React reconciliation 看到同一個 element 直接 skip 屬性 diff，cursor position / IME state 完整保留；(ii) value 由 `inputValueRef.current` + `setInputValue(val)` 顯式 `textarea.value = val` 寫入，這個路徑只在 history navigation / slash select / clear 才走，typing 本身**不觸發**任何 React state；(iii) 沒有 controlled-input lag — 不像很多 React app 在重 component 上控 textarea 會在 100+ ms 才看到字。**結論**：typing 在 React 層完美無阻、cursor + IME 不被 streaming 干擾。Mac hang 不可能是 typing 本身，只能是 Enter→sendMessage invoke（已 instrumented）或 streaming message 進來時 setMessages 觸發的 reconcile 太慢拖到下一個 input event（這條 #48 後可量化 — drag-drop 會 fire onDrop 後 React 反應時間，但本身不算 typing）。(B) remote port 推進 — 兩個小但獨立有用的模組：(1) `node-sidecar/src/lib/remote-broadcast.mjs` port `electron/remote/broadcast-hub.ts` (9 行)，是 host→remote event multiplexer 的 EventEmitter 包裝，未來 sidecar handler 喊 `broadcastHub.broadcast(channel, ...args)`、WebSocket server `.on('broadcast', ...)` listener pickup 把 event 切成 frame 推給每個 authenticated client。share singleton instance（`export const broadcastHub`）跟 Electron 對齊；加 `__resetBroadcastHubForTests()` 給測試清 listener。(2) `node-sidecar/src/lib/remote-fingerprint.mjs` port `electron/remote/certificate.ts` 裡 `computeFingerprint(certPem)` + `normalizeFingerprint(fp)` 兩條純 crypto helper（不含 cert 生成 — 那塊需要 `selfsigned` npm dep，下一刀處理）。computeFingerprint：strip PEM header/footer/whitespace → base64 decode 拿 DER bytes → SHA-256 → uppercase hex → 每 2 char colon 分組（標準 X.509 fingerprint 格式 `AB:CD:EF:...`）；增 input validation：empty/non-string PEM throw、PEM 沒 base64 body throw（防 caller 傳壞輸入靜默回 garbage hash）。normalizeFingerprint：strip colons/whitespace + uppercase，給 pin comparison 用，non-string 回 `''`（Electron 版本沒這層 type guard，sidecar 加上去防 caller 不小心傳 null）。`fingerprintOfPem` alias 直接 export 同一個 reference（Electron 對外公開 API）。**Test**：sidecar 加 ~22 條斷言 — broadcast 6 條（multi-listener 同步、channel + variadic args 透傳、reset 後 listener 清空、reset hook 存在）、fingerprint 16 條（PEM round-trip 跟手算 SHA-256 對齊、長度 95 / format `^([0-9A-F]{2}:){31}[0-9A-F]{2}$`、whitespace stripping、alias 同 reference、normalize 5 條 idempotent + non-string fallback、3 條 input validation throw、pin comparison case-insensitive 契約）。仍綠：sidecar 139+22=161 條 / host-api / cargo 114 / tsc。**結果**：未來 WebSocket server slice 拿到 broadcastHub 直接 fan-out、TLS handshake 拿到 fingerprint 直接 pin compare；本刀使用者面向沒有可見變化，但 remote 模組樹已剩兩塊主菜（cert generation + WebSocket server）。剩下：(1) **certificate.ts ensureCertificate + generate** (selfsigned npm dep + ~70 行)、(2) **WebSocket server 真實實作** (~400 行 + ws npm dep)、(3) **persistent SDK session for sendMessage** — typing→send 體感最大解、(4) Tauri 原生 onDragDropEvent → folder drop path resolver、(5) PromptBox 100ms 等待移除、(6) Tauri bundle 瘦身、(7) `claude.stopTask`、(8) MCP tools list categories、(9) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#48)。（同上 + **新 #48 — drag-drop on Tauri 真實修法**：使用者第三輪「繼續從打字的角度」終於把 drag-drop 這條提到優先處理。Root cause 已在 #46 audit 確認：Tauri 2 預設 `dragDropEnabled: true` 在 webview 層攔截 OS drag-drop event，所以 React `onDrop` 根本不 fire — 拖檔到 ClaudeAgentPanel/CodexAgentPanel/OpenAIAgentPanel/Sidebar 全部默默 no-op，使用者拖了沒反應只能用 file picker。**修法**三條：(1) `src-tauri/tauri.conf.json` 給 window 加 `"dragDropEnabled": false`，把 OS 級 hook 關掉、瀏覽器 onDrop 恢復 fire；(2) `src/host-api.ts` 把 `host.shell.getPathForFile` 從 `notImplemented(...)` throw 改成 `() => null` — Tauri 2 webview 沒有 `webUtils.getPathForFile()` 對應 API，從 File 物件拿不到 on-disk path，所以 graceful 回 null 讓 caller 可以 fallback；(3) ClaudeAgentPanel + CodexAgentPanel + OpenAIAgentPanel 的三個 `handleDrop` 統一改成「path-or-fallback」pattern：把 `isRemoteConnected ? null : host.shell.getPathForFile(file)` 拉到分支頂端，沒 path 就走「image → readFileAsDataUrl + addImageDataUrl / 非 image → alert」分支（跟舊的 remote-only 分支合併、訊息文案區分 remote vs Tauri-no-path）。Sidebar 的 workspace folder drop 同理：沒 path 就 alert 提示用「Add workspace」按鈕（拖 folder 拿 path 在 Tauri 2 完全無解，因為 File 連 directory 都不一定識別）。**Test**：cargo 加 1 條 `tauri_conf_disables_dragdrop_so_browser_ondrop_fires`，讀 `tauri.conf.json` parse JSON 走 `/app/windows`，斷言每個 window 都顯式設 `dragDropEnabled === false` — 未來有人改回 default、或加新 window 沒設這個 flag，這條測試立刻紅。仍綠：sidecar 139 / host-api / cargo 114 (+1 新 conf regression) / tsc。**結果**：使用者下次 `pnpm tauri:dev` 拖檔到 Claude/Codex/OpenAI panel — 圖檔自動轉 dataURL 附上、非圖檔跳 alert（誠實告知 Tauri 暫時無法解 path），Sidebar 拖 folder 也跳 alert 引導用 Add workspace 按鈕。日後如果決定接 Tauri 原生 `onDragDropEvent`（dragDropEnabled:true 就有）拿 OS 級 path 進來，可以再加一層 path-resolver 跟 File 物件 cross-ref，但本刀先把「拖了完全沒反應」這個 dead UX 修掉。剩下：(1) **certificate.ts 自簽 TLS cert** (103 行 + selfsigned npm dep)、(2) **WebSocket server 真實實作** (~400 行)、(3) **persistent SDK session for sendMessage** — typing→send 體感最大解、(4) Tauri 原生 onDragDropEvent → File 物件 path resolver（folder drop 才能再 work）、(5) PromptBox 100ms 等待移除、(6) Tauri bundle 瘦身、(7) `claude.stopTask`、(8) MCP tools list categories、(9) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#47)。（同上 + **新 #47 — typing path 三次審計 + remote secrets persistence port**：(A) typing 三次 audit — 把 ClaudeAgentPanel 從 textarea ref 到 send 的每個 hook 又走一遍，確認沒有 hidden cost：(1) `inputValueRef` 是 ref 不是 state，所以打字不觸發 re-render；(2) `autoResizeTextarea` 只動 `textarea.style.height`，瀏覽器 reflow O(1)，不打 React state；(3) auto-compact window 查表 `getAutoCompactWindowForModel(currentModel, settingsStore.getSettings().autoCompactWindow)` — `settingsStore.getSettings()` 是 sync ref read（不打 disk），查表也是 O(1) Map lookup，加 Send 時付一次、不影響打字；(4) `historyMap` 是 module-scoped Map，append 純 sync。**結論**：每 keystroke cost = 1 個 ref write + 1 個 style height assign + slash-menu predicate check，幾乎不可量測。Mac hang 一定不在 keystroke 而在 Enter→handleSend invoke，等使用者重現拿 #43 timing log。(B) remote port — 把 `electron/remote/secrets.ts` (58 行) port 進新檔 `node-sidecar/src/lib/remote-secrets.mjs`。**為什麼這刀有用**：未來 sidecar WebSocket server 啟動時要記住 (1) 自簽 TLS cert、(2) auth token、(3) bind interface 等 across restart。Electron 用 `safeStorage`（OS keychain）加密；pure-Node sidecar 沒對應 API，所以這刀統一寫 `{enc:false, data:'<JSON>'}` 信封 + mode 0o600 owner-only — 跟 Electron 在 headless Linux 沒 keyring 時的 fallback path 同 shape。Read 端三條：(i) `enc:false` 走正常 inner JSON parse、(ii) legacy plaintext (envelope-less object) 直接回避免遺失舊資料、(iii) `enc:true`（Electron build 留下的）**拒絕**回 null + warn 因為沒 safeStorage 解不開，caller 該重新生成。`readEncryptedString` / `writeEncryptedString` 是糖衣包 `{value: '<str>'}`；同時也支援讀「writeEncryptedJson 寫的 bare string」的場景（typeof obj === 'string' 分支）。如果之後加 keytar 讓 sidecar 也能加密，envelope 從 false → true 透明 upgrade，舊檔可繼續讀。**Test**（sidecar in-process via mkdtempSync）：~13 條斷言 — (a) JSON round-trip 含 nested array；(b) on-disk envelope shape 鎖（`{enc:false, data:string}` 兩 key）；(c) legacy plaintext envelope-less 物件 read 不丟資料；(d) `enc:true` blob 拒絕 → null（不 throw）；(e) missing file → null；(f) 主 JSON 壞掉 → null + warn 不 throw；(g) inner data JSON 壞掉 → null + warn；(h) POSIX 模式檢查（Windows skip：NTFS 報 0o666）— `mode & 0o777 === 0o600`；(i) string wrapper round-trip；(j) bare-string round-trip；(k) object-without-`value` field readEncryptedString → null。仍綠：sidecar 126+13=139 條 / host-api / cargo 113 / tsc。**結果**：本刀使用者面向沒有可見變化（remote 還在 stub），但下一刀 cert / WebSocket server 都直接拿到一條 stable 的 secret persistence 線。剩下：(1) **certificate.ts 自簽 TLS cert** (103 行 + selfsigned npm dep)、(2) **WebSocket server 真實實作** (~400 行)、(3) **persistent SDK session for sendMessage** — typing→send 體感最大解、(4) drag-drop on Tauri 2 (`dragDropEnabled:false` + Tauri 替代 `getPathForFile`)、(5) PromptBox 100ms 等待移除、(6) Tauri bundle 瘦身、(7) `claude.stopTask`、(8) MCP tools list categories、(9) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#46)。（同上 + **新 #46 — typing path 二次審計（drag-drop 默默壞 — 後續 slice 處理） + remote protocol allowlists 進 sidecar lib**：(A) typing audit 二次 — 把 ClaudeAgentPanel input 周邊每條路徑再走一遍：(1) IME composition：`handleKeyDown` 在 Enter/Up/Down 三處都鎖 `!e.nativeEvent.isComposing`，中文/日文輸入法不會誤觸送出；(2) image paste：`handlePaste` 走 dataURL path 對 Tauri/Electron 都正確；(3) drag-drop：`handleDrop` 呼叫 `host.shell.getPathForFile(file)`，但 host-api 那邊還是 `notImplemented` — **不過實際上**在 Tauri 2 預設 `dragDropEnabled:true` 下，OS 層級 drop 事件被 Tauri 攔走、瀏覽器 `onDrop` 根本不 fire，所以 `getPathForFile` 不會被叫到、不會 crash typing；net effect = drag-drop 在 Tauri 默默不 work（拖檔進去沒反應），但**不影響打字本身**。後續 slice 修法：tauri.conf 開 `dragDropEnabled:false` 走標準瀏覽器 drop + 把 `getPathForFile` 用 `webUtils.getPathForFile(file)` Tauri 替代品實作（Tauri 2 沒對應 API，需要 sniff DataTransfer.files[i].name 配 Tauri sidecar resolve 或直接讀 bytes 寫 temp file）— 此 slice 不動。(4) history navigation / slash menu / setInputValue：純 ref + style 寫入，無 async，無 IPC，typing 本身**不可能** hang。唯一阻塞點仍是 Enter → handleSend → claude.sendMessage invoke，#43 已加 timing diagnostic 等使用者 mac 重現。(B) remote port 推進 — 把 `electron/remote/protocol.ts` 的兩個 Set 常數（`PROXIED_CHANNELS` 60+ 條 invoke channel allowlist / `PROXIED_EVENTS` 22+ 條 host→client push event allowlist）以及 `handler-registry.ts` 的整套 register/has/invoke/reset semantics port 進新檔 `node-sidecar/src/lib/remote-protocol.mjs`。**為什麼這刀有用**：未來 Phase 3 真的開 WebSocket server 接 remote client（手機 / 另一台 mac），server 端必須在 dispatch 前先用這兩個 allowlist 過濾，不在 list 上的 channel/event 一律拒絕；handler registry 是 server 把 invoke frame 路由到對應 handler 的索引。這刀把 data + registry contract 先架好，下一刀只要寫 WebSocket frame loop 跟 TLS handshake 就能直接接上。registerRemoteHandler / hasRemoteHandler / invokeRemoteHandler / __resetRemoteHandlersForTests / __remoteHandlerCountForTests 全 export — registerRemoteHandler 多兩條 input validation（empty channel 跟 non-function handler 直接 throw，比 Electron 寬鬆版本更嚴格一點，避免後續 server 拿到 garbage handler）。**Test**：~13 條斷言 — (a) 解析 `electron/remote/protocol.ts` 原檔（regex 抓 `export const X = new Set([...])` body、過 // 註解、抓 `'...'` 字串），跟 sidecar 的 Set 做雙向 diff（only-in-electron / only-in-sidecar）— 任何一邊加新 channel 或 event 沒改另一邊立刻紅；(b) 兩個 Set size 下界 sanity（>50 channels / >15 events）；(c) handler registry — register 後 size+1、has=true、invoke 帶 default ctx (windowId=null/isRemote=false)、custom ctx (windowId='win-1'/isRemote=true) 透傳、null args 強轉 [] (mirror Electron tolerance)、unknown channel reject 訊息 contract、validation throw 兩條、reset 把 size 歸零。仍綠：sidecar 113+13=126 條 / host-api / cargo 113 / tsc。**結果**：未來那刀 WebSocket 上線時，常數 + registry contract 已鎖死跟 Electron 對齊；本刀使用者面向沒有可見變化（remote 還在 stub），但下一刀 server 接上去就直接 secure。剩下：(1) **WebSocket server 真實實作**（用 sidecar 已有的 ws npm package + 自簽 TLS / 自製簡單 cert + auth token 持久化、~400 行）、(2) **persistent SDK session for sendMessage**（streaming-input mode 把 sendMessage cold spawn 攤到 process lifetime — typing→send 體感最大解）、(3) tauri.conf `dragDropEnabled:false` + `host.shell.getPathForFile` Tauri 實作（恢復拖檔上傳）、(4) PromptBox 100ms 等待移除、(5) Tauri bundle 瘦身、(6) `claude.stopTask`、(7) MCP tools list categories、(8) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#45)。（同上 + **新 #45 — typing path audit 通過 + `tunnel.getConnection` 真實 network discovery**：(A) typing audit — 使用者前一輪報「mac 輸入訊息會 hang 住」，沿著 ClaudeAgentPanel 走一趟：`handleInputChange` (line 1959) 純 ref 寫入 + auto-resize、`handleKeyDown` (1979) 純 local 導航、`setInputValue` (1476) 也只動 ref + style。每個 keystroke 沒有任何 async/IPC，typing 本身**不可能** hang。唯一阻塞點是 Enter → `handleSend` → `claude.sendMessage` invoke，這個已在 1137761 加 timing diagnostic（sync/invoke/total 三段），等使用者重現拿 log。(B) remote 真實 port — Phase 3 Big Project 還在；本刀先取一塊小但有用的：把 `electron/remote/tunnel-manager.ts` 的 `getAllAddresses(boundHost)` 純 logic（30 行 `os.networkInterfaces()` filter + tailscale `100.x.` vs LAN classification + label format）port 進 sidecar `node-sidecar/src/handlers/remote-tunnel.mjs`，並 export 給測試 直接 unit 測。`tunnel.getConnection` 改成：(1) loopback boundHost (`127.0.0.1`/`::1`/`localhost`) 直接回 `[{ip:'127.0.0.1', mode:'localhost', label:'localhost — 127.0.0.1'}]`，(2) 其他 (含 `'all'` / undefined) 走 `os.networkInterfaces()` 列出真 LAN/Tailscale 介面 — tailscale 排前面、LAN 排後面、internal/IPv6 過濾掉。**仍回 `{error, addresses}` 雙鍵 shape**：error 講「server not running — start the remote server before generating a QR code」、addresses 是真實清單。renderer SettingsPanel `connectionUrlHost` effect (line 304) 跟 `handleGenerateQR` (359) 兩處消費點都是 `'error' in result` 早返回 + `result.addresses?.length` 取首條 IP，新 shape 完全 backward-compatible — 沒 server 還是擋下來但日後實作 server 直接拔掉 error 就一切自動接上。其他 7 條 `remote.*` (startServer/stopServer/serverStatus/connect/disconnect/clientStatus/testConnection/listProfiles) 維持 stub shape（`running:false / connected:false / {error}`）以鎖住 renderer 預期。**Test**：sidecar 加 ~20 條斷言 — (a) 6 條 `remote.*` shape 鎖（serverStatus 6 keys / clientStatus 2 keys / 4 條 `{error}` 路徑）、(b) `tunnel.getConnection` loopback 路徑 4 條（`127.0.0.1/::1/localhost` 三種輸入都收斂到 1 條 localhost 結果 + label 含 IP）、(c) external 路徑 3 種輸入 (`0.0.0.0` / undefined / `'all'`) — 斷言 shape 跟內容契約：`{ip, mode, label}` 三 key、mode ∈ {tailscale, lan} 不會洩漏 localhost、tailscale entry 必 `100.x` 開頭、label 必含 ip、(d) helper unit — direct call `getAllAddresses` 驗 ordering invariant（tailscale 必排在所有 LAN 之前）。仍綠：sidecar 113+ / host-api / cargo 113 / tsc。**結果**：使用者下次 SettingsPanel 點 QR — 雖然 server 還沒啟動所以仍會看到 error 訊息，但 console 上就有真實 LAN/Tailscale IP 列表可以驗證網路設定，下一刀真正 wire 起 WebSocket server 就只需要把 error 拿掉。剩下：(1) `remote.*` server 真正 port（`electron/remote/remote-server.ts` 389 行 + certificate.ts/secrets.ts/protocol.ts/handler-registry.ts/broadcast-hub.ts ~600 行 — TLS + WebSocket + auth brute-force protection 全套）、(2) **persistent SDK session for sendMessage**、(3) PromptBox 100ms 等待移除、(4) Tauri bundle 瘦身、(5) `claude.stopTask`、(6) MCP tools list categories、(7) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#44)。（同上 + **新 #44 — `<cwd>/.mcp.json` 偵測 + 一鍵 approve flow**：使用者回報「我們本來的 Claude Code 沒讀到 `<project>/.mcp.json`」。Root cause 不是 SDK bug：Claude Code CLI 會 detect `<cwd>/.mcp.json`，但**不會自動 attach** — 等三個 settings 來源（`~/.claude/settings.json` / `<cwd>/.claude/settings.json` / `<cwd>/.claude/settings.local.json`，跟 sendMessage queryOptions 的 `settingSources: ['user','project','local']` 對齊）任一支供 approval marker：`enableAllProjectMcpServers: true`，或 `enabledMcpjsonServers: [...]` 把 `.mcp.json` 裡每個 server 都 enumerate 過。CLI 的互動式 approve prompt 在 SDK mode 不會出現（沒 TTY），所以未 approve 的 `.mcp.json` 被 silently skip — 完全沒線索給使用者。**修法**：被動偵測 + 一鍵寫入。新 sidecar handler module `node-sidecar/src/handlers/claude-mcp.mjs`：(1) `claude.checkMcpJsonStatus(cwd)` 讀 `<cwd>/.mcp.json` 拿 server names，再讀三條 settings source 判斷是否 approved（任一 `enableAllProjectMcpServers:true` 或 `enabledMcpjsonServers` 涵蓋全部 server names 都算 approved；partial 列表**不**算），回 `{exists, approved, servers[]}`；(2) `claude.enableAllProjectMcp(cwd)` 寫 `<cwd>/.claude/settings.json` 加 `enableAllProjectMcpServers: true` — preserve 既有 keys（淺 merge）、idempotent（已 true 回 `changed:false` 不重寫）、缺 cwd throw、自動 mkdir 新 project。Tauri Rust 兩條 command (`claude_check_mcp_json_status` / `claude_enable_all_project_mcp`) + lib.rs 註冊。host-api adapter 兩條 cwd-keyed proxy（不是 sessionId-keyed — 跟 panel mount cwd 綁）。Renderer `ClaudeAgentPanel.tsx` 加 mount-time effect：`isCodexSession` skip（Codex 不過 Claude CLI），call check、`exists && !approved` 走 `host.dialog.confirm` 列出 server names 問「Approve project MCP servers?」，OK → call enable + push 一條 system message 提示「Start a new session for them to attach」（CLI process 還在跑，必須重啟 session 才會重讀 settings）。**Test**（sidecar in-process via mkdtemp + HOME/USERPROFILE override）：13 條斷言鎖死 9 個 check 路徑（無 .mcp.json / 有但無 settings / project enableAll / exhaustive enabledList / **partial enabledList 不算 approved** / user 層 enableAll / local 層 enableAll / 壞 JSON / missing cwd）+ 4 個 enable 路徑（fresh dir 自動建 .claude/ + settings.json / preserve 既有 keys / idempotent 二度 call 不重寫 / missing cwd error）。仍綠：sidecar / host-api / cargo / tsc。**結果**：使用者打開有 `.mcp.json` 的 project、Claude panel mount 即跳 dialog 列出 detected servers、按 Yes 自動寫 settings → 起新 session 就會 attach。剩下：(1) **persistent SDK session for sendMessage**、(2) PromptBox 100ms 等待移除、(3) Tauri bundle 瘦身、(4) `remote.*` namespace、(5) `claude.stopTask`、(6) MCP tools list categories（拿到 attached 後的 tool list，跟 #44 是不同維度）、(7) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#43)。（同上 + **新 #43 — bundle.resources 漏帶 `lib/` + `handlers/` 子目錄（#40 拆檔的 lurking bug）**：使用者在 mac 上 `pnpm tauri:dev` 開了 workspace、起 Claude + Codex 兩個 panel 各打 ping，console 噴一堆 `[Error] Unhandled Promise Rejection: [object Object]`，沒有具體訊息。先在 `src/main.tsx` 加 `unhandledrejection` listener 把 reject 物件 JSON.stringify 到 `host.debug.log`（預設 [object Object] 是 Tauri invoke `BridgeError` 的 shape，瀏覽器 console 不會顯示 `.message`），同時加 sidecar smoke test 模擬「workspace + 2 panel + 各 1 個 ping」整套 RPC（startSession × 2 + 7 條 metadata × 2 + sendMessage × 2）斷言每條 reply 沒 `.error` — 全綠表示 sidecar handler 沒問題。使用者重現後從 log 撈到真正的訊息：`sidecar: child exited; stderr tail: Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/Users/.../target/debug/node-sidecar/src/lib/protocol.mjs' imported from /Users/.../target/debug/node-sidecar/src/server.mjs`。**根因**：#40 把 sidecar 從單檔 `server.mjs`（2464 行）拆成 18 檔（`lib/` 7 個 + `handlers/` 11 個），但 `src-tauri/tauri.conf.json` 的 `bundle.resources` 還停留在「`../node-sidecar/src/server.mjs` → `node-sidecar/src/server.mjs`」的單檔 mapping，dev/release Tauri 把 `server.mjs` 複製到 `target/debug/.../node-sidecar/src/` 但**沒帶 `lib/` 跟 `handlers/`**，server.mjs 第一行 `import { ... } from './lib/protocol.mjs'` 直接 `ERR_MODULE_NOT_FOUND` → Node 立刻 exit → Tauri 端每個 invoke timeout/child-exit reject → renderer 噴 unhandled rejection。#31 stderr-tail capture 那一刀剛好是這次的救命稻草，不然 root cause 完全無法定位。**修法**：`tauri.conf.json` 從 `"../node-sidecar/src/server.mjs": "node-sidecar/src/server.mjs"` 改成 `"../node-sidecar/src/": "node-sidecar/src/"` — 把整個 src 目錄複製過去，自動帶上未來新加的 subdir。**Test**：cargo `tauri_conf_bundles_full_sidecar_src_tree` 讀 `tauri.conf.json`、parse JSON、斷言 `bundle.resources` 包含 `"../node-sidecar/src/"` 或子目錄 entry，且**不**包含 single-file 的 `"../node-sidecar/src/server.mjs"`（後者會 silently 把 `lib/`/`handlers/` 漏掉、reintroduce 同樣 bug）。註解寫清楚 root cause 與失敗 mode 給未來改 conf 的人看。仍綠：sidecar 113（多 1 條 workspace+2-panel smoke）/ host-api / cargo 113（多 1 條 conf regression）/ tsc。**結果**：使用者下次 `pnpm tauri:dev` 啟動正常 — sidecar 載得到 lib + handlers，Claude / Codex panel mount 不再噴 unhandled rejection，ping 走完整 sendMessage flow。同步加的 main.tsx `unhandledrejection` listener 留著當長期診斷工具，下次任何 Tauri invoke fail 都能直接拿到 message + stack head 而不是 `[object Object]`。剩下：(1) **persistent SDK session for sendMessage**、(2) PromptBox 100ms 等待移除、(3) Tauri bundle 瘦身、(4) `remote.*` namespace、(5) `claude.stopTask`、(6) MCP tools list categories、(7) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#42)。（同上 + **新 #42 — 鏡射 Electron：3 條 cheap metadata RPC 改讀 `session.currentQuery`，不再 spawn fresh SDK query**：使用者反映 #41 雖然把第二次 panel mount 從 16s 壓到 0.16ms，但**第一次 mount 仍要等 ~16s**（4 條 metadata RPC 各 spawn 一個 fresh `sdk.query({prompt:'',cwd:'/'})` 拿 metadata，每條付 ~4s SDK init cost）。使用者進一步追問「getSupportedCommands 幾乎只是 local 資料怎麼會需要好幾秒」、「都啟動了不是應該 init 之後就一直活著嗎幹嘛每次都要重來」，於是去翻 Electron 怎麼做的（`electron/claude-agent-manager.ts:2068-2099`）發現 Electron **根本沒有 metadata Query**：3 條 cheap RPC（getSupportedCommands / getSupportedAgents / getAccountInfo）直接讀 `session.queryInstance`（也就是 `claude.sendMessage` 起的那個 live Query），那個 Query 的 init promise 已經 resolve 過、所有 metadata 方法都是 `return (await this.initialization).<field>` 純記憶體讀；session 還沒打過任何 query 時就 inert 回 `[] / null`。本 slice 把 sidecar 完全鏡射這個 pattern：sidecar 那邊 `claude-send.mjs` 早就有 `s.currentQuery = generator` (line 140)，只是 `claude-readonly.mjs` 沒有用、反而每條都 `loadAnthropicSdk().query(...)` 重 spawn。新 helper `readFromLiveQuery(sessionId, method, fallback)` 讀 `sessions.get(sessionId)?.currentQuery?.<method>()`，沒 session / 沒 currentQuery / method throw 都 graceful 回 fallback。**只有 `getSupportedModels` 仍走 fresh-Query 路徑**（Electron 也是這樣，2040-2066），因為它要 augment SDK-discovered models 跟 builtins、跟 session 沒關係。cache key 加 `:${sessionId}` 避免不同 session 撞 cache。**Test**：移除 lines 1892-1968 三個舊「SDK-as-data-source」測試 block（with-data / null-fallback / throwing — 那 3 條 RPC 不再走 SDK builder，舊 contract 失效）；新「Live-Query contract」block 4 條：(a) no sessionId → `[] / null` inertly、(b) unknown sessionId → `[] / null`、(c) session with currentQuery → 讀 live Query 結果、second call hit cache（builder 不重跑）、(d) live Query throws → graceful `[] / null`。新 cache 契約 block 改用 `getSupportedModels`（仍走 SDK builder，是目前唯一鎖得住「concurrent dedup + TTL」的 RPC）。**Bench 結果**（Windows）：第一次 mount 從 17.3s → **4.19s**（~76% 縮短）— `getSupportedModels` 仍 3.78s（Electron parity，無解除非把 builtins-only 變預設），其他 3 條從 ~4s 各自降到 **~0.1-0.2ms**（4 個量級）；第二次 mount 仍 0.04-0.06ms 全 cache hit。仍綠：sidecar / host-api / cargo 112 / tsc。**結果**：使用者第一次開 ClaudeAgentPanel 從等 17 秒變等 4 秒、第二次起免費；mac 慢的問題基本上集中到 `getSupportedModels` 跟 sendMessage cold spawn 上。剩下：(1) **persistent SDK session for sendMessage**（mid-stream interrupt + 把 sendMessage cold spawn 攤到 process lifetime）、(2) PromptBox 100ms 等待移除、(3) Tauri bundle 瘦身（mac code-sign 通量問題）、(4) `remote.*` namespace、(5) `claude.stopTask` (streaming-input mode)、(6) MCP tools list categories、(7) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#41)。（同上 + **新 #41 — claude metadata RPC 5min cache + concurrent dedup**：使用者反映 Tauri 版在 mac 「轉圈很久」，量化發現 panel mount 時 sidecar 連發 `claude.getSupportedModels / getSupportedCommands / getSupportedAgents / getAccountInfo` 4 條 RPC，每條都 spawn 一個 fresh `sdk.query({prompt:'',cwd:'/'})` 拿 metadata — Windows baseline 量到每條 3.7-5.5 秒、4 條總共 17.3 秒，每次 panel remount 都重付這個 cost。Bench 工具 `node-sidecar/scripts/bench-startup.mjs`（commit 2cb5470）跑 standalone 量 sidecar handler、不打 Tauri/Vite，bypass window startup noise — 2 round 連續測證實沒有任何 cache（round 2 跟 round 1 一樣慢，每條 3.9-4.3 秒）。**修法**：handler-level in-process cache map `Map<key, {value, ts, inflight}>`、TTL 5 分鐘（panel re-mount 一定 hit、5 分鐘是「fresh enough but rebuild eventually」的折衷）、concurrent-call dedup（4 個 panel 同時 mount → 共享同一個 in-flight promise，只 spawn 一次 SDK）、build 失敗不 poison cache（next call 重試 cold path）。`claude.authLogout / accountSwitch / accountRemove` 三條觸發 `invalidateAccountMetadataCache()` flush 4 個 key（accountInfo 必 flush、models/commands/agents 也 flush 因為理論上跨帳號 feature flag 可能不同 — 寧可 conservative）。新 export `__resetMetadataCacheForTests()` 給測試清 cache、`invalidateAccountMetadataCache()` export 給 claude-auth 用（cross-handler import 不形成 cycle — claude-auth → claude-readonly 單向）。**Test**：(a) 3 條 cache 契約 — first call build / second-third call hit cache (build 計數器只 +1) / reset 後重 build；(b) concurrent dedup — `Promise.all` 4 個同時打 `getSupportedCommands`、斷言 builder 只跑 1 次但 4 個 caller 都拿到相同 result shape；(c) 已有 fallback / augmentation / SDK-throws 三路測試各加 cache reset，證明每路都仍鎖死契約。Bench 結果：first mount 仍 ~16s（4 SDK query 沒減）、**second mount 從 16s → 0.16ms**（~100,000× 加速，每次 panel re-mount 等於免費）。仍綠：sidecar / host-api / cargo 112 / tsc。**結果**：mac 使用者首次 panel mount 仍會等一波（這需要下一刀 — 把 4 個 SDK query 平行化或共用同一個 query instance），但之後切 tab、切 workspace、reset session 等場景的 metadata 重抓全變即時。剩下：(1) **第一次 mount 平行化**（4 query 同時跑，理論上 16s → 4s）、(2) **persistent SDK session for sendMessage**（streaming-input mode mid-stream control）、(3) PromptBox 100ms 等待移除、(4) Tauri bundle 瘦身（mac code-sign 通量問題）、(5) `remote.*` namespace、(6) `claude.stopTask`、(7) MCP tools list categories、(8) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#40)。（同上 + **新 #40 — sidecar server.mjs 拆分為 lib/ + handlers/ 模組**：sidecar `node-sidecar/src/server.mjs` 從 2464 行單檔拆成 18 檔（純 code movement、零邏輯改動）。**結構**：`server.mjs`（161 行 entry — registerHandler('ping') + main loop + 全部 re-export 給 test 用）；`lib/`（共用 singleton state — `protocol.mjs` 70 行 handlers Map+dispatch+sendEvent / `state.mjs` 92 行 sessions Map+ensureSession+buildSessionMeta / `sdk-loader.mjs` 48 行 / `data-paths.mjs` 49 行 / `models.mjs` 71 行 / `plugins.mjs` 52 行 / `skills.mjs` 81 行）；`handlers/`（每檔 import 時 call registerHandler — `claude-auth.mjs` 190 / `claude-session.mjs` 272 / `claude-permission.mjs` 126 / `claude-history.mjs` 359 / `claude-send.mjs` 309 / `claude-readonly.mjs` 241 / `openai.mjs` 91 / `worktree.mjs` 266 / `agent.mjs` 24 / `remote-tunnel.mjs` 24 / `update.mjs` 59）。**約束**：(1) `node-sidecar/tests/server.test.mjs` 仍然 `await import('../src/server.mjs')`，所有 17 個 test-visible 符號（`dispatch` / `handlers` / `registerHandler` / `sessions` / `__setSendEventForTests` / `__setSdkOverrideForTests` / `loadAnthropicSdk` / `__setProjectsDirOverrideForTests` / `__setPluginsPathOverrideForTests` / `__normalizeMainPath` / `__resetClaudeCliCacheForTests` / `CLAUDE_BUILTIN_MODELS` / `CLAUDE_BUILTIN_DEDUP_KEYS` / `CLAUDE_MODEL_CONTEXT_WINDOWS` / `expectedContextWindowForModel` / `sdkModelForClaudeSelection` / `dataUrlToContentBlock`）從 server.mjs re-export，0 處 test 改動；(2) ESM live bindings + Node module cache 確保 `sessions` / `handlers` Map 是真 singleton（每個 import 拿到同一個 ref）；(3) handler module 之間無 circular import — 全部往下指向 `lib/`、`lib/` 之間也無 cycle；(4) `resolveClaudeCliBinary` 從 `claude-auth.mjs` 走 3 層相對路徑（從 `src/handlers/` 走 `../../node_modules/`）回到 sidecar root，跟原本 server.mjs 走 2 層等效；(5) `claude.resetSession` 的 `claude:session-reset` emit (#38)、`rate_limit_event` 的 `claude:rate-limit` emit (#39)、`buildSessionMeta` (#36/35)、archive ops (#37) 全保留。**Test**：sidecar / host-api / cargo 112 / tsc 全綠 — 0 條測試需要改動就能通過。**結果**：日後加新 RPC / 改 sendMessage 大迴圈不會再被 archive/rewind/permission 等不相干程式碼干擾、stack trace 也好讀；單檔最大 359 行（claude-history），最小 24 行（agent / remote-tunnel）。剩下：(1) `claude.stopTask`（需要 streaming-input mode）、(2) `remote.*` namespace 真實實作、(3) emit sites for `claude:history` / `claude:resume-loading` / `claude:prompt-suggestion`、(4) multi-account safeStorage 替代、(5) MCP tools list categories、(6) sidecar log persisted to file、(7) streaming-input mode mid-stream control。）

歷史紀錄：2026-05-09 (#39)。（同上 + **新 #39 — claude:rate-limit emit on rate_limit_event SDK message**：sidecar 在 sendMessage 的 SDK 訊息處理迴圈裡多解析 `type:'rate_limit_event'` — SDK 在 API 對我們 throttle 時主動噴一條 top-level rate_limit_event message，含 `rate_limit_info: { rateLimitType, resetsAt, utilization, isUsingOverage }`。鏡射 `electron/claude-agent-manager.ts:1030` 的 emit 邏輯：只有當 `rateLimitType` (string) + `resetsAt` (number) 兩個 required 欄位都齊才 emit `claude:rate-limit{sessionId, info}`，避免 SDK 偶爾噴的 partial event 觸發 banner；`resetsAt` 從秒轉 ms（renderer 走 `new Date(resetsAt)`）；`utilization` 不是 number 就回 null、`isUsingOverage` undefined 預設 false。**Test**（in-process via fake SDK）：注入 3 條 rate_limit_event — (1) 完整 info → 斷言 emit 帶正確 sessionId/rateLimitType/resetsAt(*1000)/utilization/isUsingOverage；(2) 缺 resetsAt → 不 emit（partial-event filter）；(3) 缺 utilization → emit 但 utilization=null、isUsingOverage=true 透傳。仍綠：sidecar / host-api / cargo 112 / tsc。**結果**：使用者打到 rate limit 時 ClaudeAgentPanel 的 rate-limit toast/banner 會顯示「重置於 X」，與 Electron 行為對齊；剩下 4 條 listener (`onHistory` / `onResumeLoading` / `onPromptSuggestion` / `onWorktreeInfo` 的 mid-stream emit) 還沒接 SDK emit site，但 host-api 端 wiring 已就位。剩下：(1) `claude.stopTask`（需要 streaming-input mode）、(2) emit sites for `claude:history` / `claude:resume-loading` / `claude:prompt-suggestion`、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file、(6) streaming-input mode mid-stream control、(7) `remote.*` namespace port（screenshot 報的 `Failed to start server` 對應這條）、(8) sidecar 拆檔（2464 → ~16 個檔）。）

歷史紀錄：2026-05-09 (#38)。（同上 + **新 #38 — 6 missing claude:* listener routes + claude:session-reset emit**：renderer 在 ClaudeAgentPanel mount 時其實會掛 6 個 lifecycle listener — `onHistory` / `onResumeLoading` / `onSessionReset` / `onRateLimit` / `onWorktreeInfo` / `onPromptSuggestion` — Electron 各自走 `ipcRenderer.on('claude:<event>', ...)`。Tauri host-api adapter 的 `eventListeners` map 之前只有 13 個 event，這 6 條落到 permissive proxy fallback 拿到 `() => () => {}` no-op unsubscriber，看起來 mount 不 crash 但事件**永遠收不到** — worktree-info bar 不會更新、rate-limit toast 不亮、reset-session 完 panel 不知道要清 message buffer、history rehydrate 卡 loading icon。本 slice 把 6 條全 wire 到 explicit map：`onHistory→claude:history` (payloadKey='items')、`onResumeLoading→claude:resume-loading` ('loading')、`onSessionReset→claude:session-reset`（**特殊**：Electron 簽章是單參數 `(sessionId)`、第二參 undefined，用哨兵 `__none__` 跳過 lookup）、`onRateLimit→claude:rate-limit` ('info')、`onWorktreeInfo→claude:worktree-info` ('payload'，sidecar 既有 cleanupWorktree emit 已用這個 key)、`onPromptSuggestion→claude:prompt-suggestion` ('suggestion')。同時補 sidecar `claude.resetSession` 在成功 delete session 後 emit `claude:session-reset{sessionId}` — 鏡射 Electron `claude-agent-manager.ts:resetSession` 的通知契約，未知 sessionId 不 emit（panel 那邊 likely 也沒 mount）。**Test**：(a) host-api 加 6 條 listener canary — 每個都呼叫拿 unsubscriber、`typeof === 'function'`、call 一次清掉，鎖住「6 條 listener 在 explicit map 裡而非 permissive fallback」的契約（fallback 路徑 still 回 function，但這 canary 至少防止有人不小心把 explicit map 條目刪掉）；(b) sidecar 加 resetSession 的 sendEvent 攔截、斷言 existing session reset 剛好 emit 1 次 `claude:session-reset` 帶正確 sessionId、unknown sessionId reset 不 emit。仍綠：sidecar / host-api / cargo 112 / tsc。**結果**：使用者按 reset 時 panel UI 立刻清空、worktree info bar 在 cleanup 後即時消失、其餘 4 條 listener 雖然 sidecar emit 還沒實作（rate-limit 需要 SDK error type 解析、history/resume-loading/prompt-suggestion 需要 SDK stream event 解析）但 wiring 已就位 — 後續 emit 來時不需要再動 host-api。剩下：(1) `claude.stopTask`（需要 streaming-input mode）、(2) emit sites for `claude:rate-limit` / `claude:history` / `claude:resume-loading` / `claude:prompt-suggestion`、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file、(6) streaming-input mode mid-stream control。）

歷史紀錄：2026-05-09 (#37)。（同上 + **新 #37 — claude.archiveMessages / loadArchived / clearArchive**：sidecar port `electron/server-core/register-handlers.ts:505+` 的對話歷史壓縮 file ops — 三條 panel UX 用來把舊訊息 archive 到磁碟、之後再分頁讀回、清空。資料落點 `<dataDir>/message-archives/<sanitized-sessionId>.jsonl`，每行一個 message JSON（append-only）。**Path traversal 防護**：`archiveFilePath(sessionId)` 用 `String(sessionId).replace(/[^a-zA-Z0-9_.\-]/g, '_')` 把任何 `../` `\\` 之類字元換成 `_`，sessionId 永遠不會逃出 `message-archives/` 目錄。`archiveMessages(sessionId, messages: ClaudeMessage[])`：mkdir -p、append 每條 message 一行 JSON 到 `.jsonl`、回 `{ok:true, count}`。`loadArchived(sessionId, offset, limit)`：tail-pagination — 從檔尾數 `offset` 行往回拿 `limit` 行，沒檔回 `[]`、past-total graceful clip。`clearArchive(sessionId)`：unlink `.jsonl`（沒檔不 throw）。新 Tauri commands `claude_archive_messages/load_archived/clear_archive` + adapter `host.claude.{archiveMessages,loadArchived,clearArchive}`。**Test**（in-process via mkdtempSync tmpdir override `BAT_SIDECAR_DATA_DIR`，~40 條斷言）：(a) 空 archive load → `[]`；(b) 5-message archive append、count 正確、檔案行數＝5；(c) tail pagination — `offset=0,limit=2` 拿最後 2 條、`offset=2,limit=2` 拿中間 2 條、`offset=4,limit=2` 拿最早 1 條（clip）；(d) beyond-start `offset=10` → `[]`、past-total `limit=100` → 全 5 條；(e) append/grow — 第二輪再 append 3 條，count=3、再 load 全部=8、順序正確；(f) clear → unlink、再 load → `[]`、再 clear（沒檔）→ graceful 不 throw；(g) 4 條 defensive missing-param paths（missing sessionId / missing messages array / non-array messages / unknown nature）；(h) **path-escape sanitization** — sessionId='../../etc/passwd' → 寫到 `message-archives/______etc_passwd.jsonl`，斷言檔名安全字元化、檔在 message-archives 目錄底下而非 escape 出去。仍綠：sidecar (新增 ~40 條斷言) / host-api / cargo 112 / tsc。**結果**：使用者在 Tauri release 下對話跑長之後，panel 自動 archive 老訊息保持 token budget、需要時再分頁載回、清掉 session 時 archive 也清乾淨 — UX 跟 Electron 對齊。剩下：(1) `claude.stopTask` 需要 streaming-input mode、(2) multi-account safeStorage 替代、(3) MCP tools list categories、(4) sidecar log persisted to file、(5) streaming-input mode mid-stream control。）

歷史紀錄：2026-05-09 (#36)。（同上 + **新 #36 — claude.restSession / wakeSession / isResting + claude:status full meta hotfix**：(A) **status hotfix**：使用者反映 `ClaudeAgentPanel.tsx` 的 status line 在第一輪對話結束後仍 crash 在 `inputTokens.toLocaleString`。Root cause #35 只修了 `getSessionMeta` RPC reply，但 renderer 的 `setSessionMeta` 主要是被 `claude:status` event 觸發，而 status 事件在 sidecar 兩處（`rewindToPrompt` + `system:init` 進來時）emit 的是 4 個 sparse 欄位的 meta，覆寫掉之前的完整 shape → renderer 拿到 `{sdkSessionId,cwd,model,permissionMode}` 沒 inputTokens → crash。修法：抽出 `buildSessionMeta(session)` helper，`getSessionMeta` RPC + 兩個 status emit 全部走它，render 永遠拿到 19-key 完整 shape；測試加 status event 的 19-key + 13-numeric 鎖（跟 RPC reply 同一份契約），任何未來 sparse status 立刻紅。(B) **rest/wake/isResting**：sidecar port `electron/claude-agent-manager.ts:2481+` — 三條面板 lifecycle ops 從 permissive null fallback 拉出顯式 route。`restSession`：abort 任何 in-flight query、清 streaming flag、emit 一條 `claude:message{role:'system', content:'Session is resting...'}` 給 panel 顯示「tap to wake」hint、把 `session.isResting=true`。`wakeSession`：把 flag 翻回 false。`isResting`：return `session?.isResting === true`。`sendMessage` 在進入點補 line 581-582 的 auto-wake — 任何 incoming user input 都會把 `isResting` 翻回 false。session record 加 `isResting: false` 預設。新 Tauri commands `claude_rest_session/wake_session/is_resting` + adapter `host.claude.{restSession,wakeSession,isResting}`。**Test**（in-process）：(a) 把 session pre-flag 成 streaming + 塞 abortController，call restSession、斷言 `isResting=true`、`streaming=false`、`abortController=null`、`ac.signal.aborted=true`、emit 1 條 system message 含 'resting' 字樣；(b) wakeSession 翻 false、isResting 跟著 false；(c) 再 set isResting=true、call sendMessage、斷言 sendMessage 一進場就把 isResting 翻 false（auto-wake contract）；(d) defensive — missing/unknown sessionId 在三條都回 false 不 throw。仍綠：sidecar (新增 ~40 條斷言 — rest/wake + status meta 雙鎖) / host-api / cargo 112 / tsc。**結果**：使用者 `pnpm tauri:dev` 點開 ClaudeAgentPanel 第一輪對話結束後 status line tokens/turns/duration 不再 crash；rest/wake UX 正常運作。剩下：(1) `claude.stopTask` 需要 streaming-input mode、(2) `archiveMessages` / `loadArchived` / `clearArchive`（壓縮對話歷史）、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file。）

歷史紀錄(本次合併進 #36)：~~2026-05-09 (#36 first cut — rest/wake/isResting only)~~。：sidecar port `electron/claude-agent-manager.ts:2481+` — 三條面板 lifecycle ops 從 permissive null fallback 拉出顯式 route。`restSession`：abort 任何 in-flight query、清 streaming flag、emit 一條 `claude:message{role:'system', content:'Session is resting...'}` 給 panel 顯示「tap to wake」hint、把 `session.isResting=true`。`wakeSession`：把 flag 翻回 false。`isResting`：return `session?.isResting === true`。`sendMessage` 在進入點補 line 581-582 的 auto-wake — 任何 incoming user input 都會把 `isResting` 翻回 false（user 都打字了，當然不算 resting）。session record 加 `isResting: false` 預設。新 Tauri commands `claude_rest_session/wake_session/is_resting` + adapter `host.claude.{restSession,wakeSession,isResting}`。**Test**（in-process）：(a) 把 session pre-flag 成 streaming + 塞 abortController，call restSession、斷言 `isResting=true`、`streaming=false`、`abortController=null`、`ac.signal.aborted=true`、emit 1 條 system message 含 'resting' 字樣；(b) wakeSession 翻 false、isResting 跟著 false；(c) 再 set isResting=true、call sendMessage、斷言 sendMessage 一進場就把 isResting 翻 false（auto-wake contract）；(d) defensive — missing/unknown sessionId 在三條都回 false 不 throw。仍綠：sidecar (新增 ~12 條斷言) / host-api / cargo 112 / tsc。**結果**：使用者可以在 Tauri release 把 Claude session 「放著休息」，下次回來打字自動喚醒，跟 Electron 行為一致。剩下：(1) `claude.stopTask` 需要 streaming-input mode、(2) `archiveMessages` / `loadArchived` / `clearArchive`（壓縮對話歷史）、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#35)。（同上 + **新 #35 — claude.fetchSubagentMessages + getSessionMeta full shape + onReload blank-screen hotfix**：(A) **fetchSubagentMessages**：sidecar port `electron/claude-agent-manager.ts:2558` — call `sdk.getSubagentMessages(sdkSessionId, agentToolUseId, {dir: cwd})` 拿 SDK 寫的 subagent transcript shard，normalise 成 `(ClaudeMessage|ClaudeToolCall)[]`。tool_result 訊息 fold 回對應的 tool_use entry（`status:'completed'|'error'`、result 截 2000 字），noise 訊息（empty / `[Request interrupted...]` / `<local-command-caveat>` 開頭）丟掉，每個 item 都 stamp `parentToolUseId` = agent。任何錯誤路徑（missing params / unknown sessionId / no sdkSessionId / SDK throw / 無 helper）graceful 回 `[]` — renderer 行為跟 Electron 一致。新 Tauri command `claude_fetch_subagent_messages(sessionId, agentToolUseId)` 30s timeout（cold SDK load + slow disk safe margin）+ adapter `host.claude.fetchSubagentMessages`。**Test**（in-process via fake SDK）：(1) happy path — 4-msg synthetic（user kickoff + assistant text+thinking+tool_use Bash + user tool_result success + noise + assistant final）→ 4 items（noise 被丟）、tool_use 的 status 升級 completed、result 從 tool_result 折回；(2) is_error:true 路徑 → status:'error'、result text 進去；(3) 5 條 defensive：missing sessionId / missing agentToolUseId / unknown sessionId / no sdkSessionId / SDK throw / SDK without helper → 都回 `[]`、不 throw。(B) **getSessionMeta full shape**：使用者開 dev 後 `ClaudeAgentPanel` line 4430 直接 `sessionMeta.inputTokens.toLocaleString()`（沒 optional chaining），sidecar 之前只回 4 個欄位（permissionMode/model/effort/autoCompactWindow），導致 panel mount 即崩。本 slice 把 getSessionMeta 擴成完整 SessionMetadata 鏡射 `electron/claude-agent-manager.ts:181`：19 個欄位（permissionMode/model/effort/autoCompactWindow/sdkSessionId/cwd/totalCost/inputTokens/outputTokens/durationMs/numTurns/contextWindow/maxOutputTokens/contextTokens/cacheReadTokens/cacheCreationTokens/callCacheRead/callCacheWrite/lastQueryCalls），數值欄位 default 0、字串/可選 default null。tokens 從 `s.lastUsage`（snake_case）翻譯到 camelCase shape，contextWindow 透 `expectedContextWindowForModel` 算。**Test**：sidecar test 加 19-key existence + 13 個數值欄位 typeof === 'number' 的 lock，確保任何欄位漏掉立刻紅。(C) **onReload blank-screen hotfix**（先單獨 commit `d2b28c5`）：renderer dev 一啟動就空白，root cause 是 `host.workspace.onReload` 在 createTauriHost 缺實作 → workspace-store init 階段 throw → 整個 React tree 被 React 錯誤邊界吃掉、splash rAF 移除後就空白。補 no-op unsub（單視窗無 cross-window/remote sync），同時把 `fs.{watch,unwatch,onChanged}` / `clipboard.onCopyShortcut` 從 `notImplemented()` throw 改成 listener-style no-op，避免 FileTree/MarkdownPreviewPanel/WorkerPanel mount 連鎖崩。仍綠：sidecar (新增 fetchSubagent + meta shape lock 約 30 條斷言) / host-api / cargo 112 / tsc。**結果**：使用者 `pnpm tauri:dev` 不再空白、ClaudeAgentPanel status line tokens/turns/duration/contextPct 全顯示 0 而非 NaN/crash、Agent/Task subagent 卡片展開可拿到 per-message 流。剩下：(1) `claude.stopTask` 需要 streaming-input mode、(2) `archiveMessages` / `loadArchived` / `clearArchive`、(3) `restSession` / `wakeSession` / `isResting` 等 lifecycle、(4) multi-account safeStorage 替代、(5) MCP tools list categories、(6) sidecar log persisted to file。）

歷史紀錄：2026-05-09 (#34)。（同上 + **新 #34 — claude.forkSession**：sidecar port `electron/claude-agent-manager.ts:2733` 的 fork 邏輯 — 把當前 SDK 對話 transcript 完整複製出一個新的 sdkSessionId，讓 renderer 可以從某個歷史對話分支出去而不丟掉原 thread。實作走 SDK 的 `forkSession: true` query option：spawn 一個 one-turn query（`prompt:' '`、`resume: currentSdkId`、`forkSession: true`、`maxTurns: 1`），等 `system:init` 噴出新的 `session_id` 撈起來，**繼續等到 `result` 訊息再 break** — CLI 是在 result 訊息之後才把 forked transcript 落盤成 `<newId>.jsonl`，太早 break 會拿到 unresumable id。60 秒 safety timeout（FORK_TIMEOUT_MS）阻止 runaway fork hang 死整個 session。fork 不 mutate 原 session record 的 sdkSessionId — renderer 那邊會自己創一個新 session entry 用回傳的 newSdkSessionId 當入口。新 Tauri command `claude_fork_session(sessionId)` 用 90 秒 timeout（內 60+slack）+ adapter `host.claude.forkSession`。**Test**（in-process via fake SDK）：(a) 注 fake SDK 噴 `system:init{session_id:'forked-sdk-id'}` + `result`、call forkSession、斷言 `reply.result.newSdkSessionId === 'forked-sdk-id'`、原 session.sdkSessionId 不變、captured queryOptions 帶 `forkSession:true`/`resume:<orig>`/`maxTurns:1`/`abortController`/`prompt:' '`/`cwd`；(b) missing sessionId → null；(c) unknown sessionId → null；(d) 沒 sdkSessionId 的 session（startSession 後沒實打過 query）→ null；(e) 噴 result 但**沒先噴 system:init** → null（鎖住「捕捉不到新 id 就拒絕回 fork id」）。仍綠：sidecar / host-api / cargo 112 / Tauri release build 重產 / launch smoke。**結果**：使用者在 Tauri release 點 ChatHistoryItem 的 fork 按鈕時，sidecar 真的複製出 forked SDK transcript、回新 id、renderer 開新 panel 從那個分支點 sendMessage 不影響原 thread — UX 與 Electron 對齊。剩下：(1) `claude.stopTask` / `fetchSubagentMessages` / `archiveMessages` 等進階面板 UX、(2) streaming-input mode mid-stream control（mid-turn interrupt/setPermissionMode/setModel）、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file under app_data_dir。）

歷史紀錄：2026-05-09 (#33)。（同上 + **新 #33 — claude.rewindToPrompt**：sidecar port `electron/claude-agent-manager.ts:2647` 的 rewind 邏輯 — 切 SDK transcript JSONL 到指定 user-prompt index、寫一個新的 transcript 檔到新的 sdkSessionId、把 in-memory session record 的 sdkSessionId 換掉、emit `claude:status` 通知 renderer metadata 變了。流程是純 fs+JSON 操作（沒打 SDK 也沒打網路）：(1) 找 `<projectsDir>/<encode(cwd)>/<currentSdkId>.jsonl`，cwd 用 `[^a-zA-Z0-9]` → `-` 編碼跟 Claude CLI 對齊；(2) walk lines 找第 N 個有 text 的 user prompt（注意 tool_result 雖然 type='user' 但不算）；(3) randomUUID 出新 sdkSessionId、把 keptLines 內所有 sessionId 欄位 rewrite 到新 id、寫到新 .jsonl；(4) abort 任何 in-flight query、`session.sdkSessionId = newSdkSessionId`；(5) 回 `{newSdkSessionId, removedPromptCount}` 配 renderer 的 SaveAndRetry UX。Test-only override `__setProjectsDirOverrideForTests(path)` 讓測試把 `~/.claude/projects` 換成 mkdtemp，rewind 不會碰使用者真實 history。新 Tauri command `claude_rewind_to_prompt(sessionId, promptIndex)` + adapter `host.claude.rewindToPrompt`。**Test**（in-process via tmp transcript）：合成 7 行 transcript（3 個 text user prompt + 1 個 tool_result user prompt + 3 個 assistant），call rewindToPrompt(promptIndex=1) → 斷言 (a) `removedPromptCount=4`、(b) 新 .jsonl 寫進磁碟且只有 3 行、(c) 每行的 sessionId 都被 rewrite 到新 id、(d) `mod.sessions.get('rw-1').sdkSessionId === newSdkSessionId`、(e) tool_result user 訊息不算 prompt（如果算成第 1 個 prompt cutoff 會跑到第 3 行而不是第 4 行）；另加 5 條 error path：promptIndex 超過範圍、streaming 中拒絕、missing sessionId、negative promptIndex、unknown sessionId。仍綠：sidecar / host-api / cargo 112 / Tauri release build / launch smoke。**結果**：使用者在 Tauri release 點某個 user prompt 的 retry/rewind 按鈕時，sidecar 真的把 transcript 切到該 prompt 之前、產生新 sdk session id、下一輪 sendMessage `resume:` 拿新 id 從那個切點繼續對話 — UX 與 Electron 對齊。剩下：(1) `claude.forkSession`（要打 SDK + 等 result，較重）、(2) `claude.stopTask` / `fetchSubagentMessages` / `archiveMessages` 等進階面板 UX、(3) streaming-input mode mid-stream control、(4) multi-account safeStorage 替代、(5) MCP tools list categories、(6) sidecar log persisted to file under app_data_dir。）

歷史紀錄：2026-05-09 (#32)。（同上 + **新 #32 — claude.resumeSession**：sidecar 之前沒實作 `claude.resumeSession`，所以 renderer 在 ClaudeAgentPanel mount 時若有 `savedSdkSessionId` 走 resume 路徑，permissive proxy 會回 null/`Promise.resolve(null)`、session 不會接上歷史 SDK id、下一輪 sendMessage 會開全新對話。本 slice port 的 `claude.resumeSession(sessionId, sdkSessionId, options)` 鏡射 `electron/claude-agent-manager.ts:2461`：(1) abort 任何 in-flight query、(2) 砍掉舊的 session record、(3) `ensureSession()` 重建並把 `sdkSessionId` pre-populate 到新 record、(4) 把 cwd/model/effort/permissionMode/autoCompactWindow 從 options 收進 session state、(5) `permissionMode` 預設 `bypassPermissions`（鏡射 Electron — resumed 對話不再 re-prompt 已批准過的 tool）但 options.permissionMode 可覆寫。`startSession` 也補一條：若 options.sdkSessionId 有給就直接寫進 session record，下次 sendMessage 自然帶 `resume:`。新 Tauri command `claude_resume_session` + adapter `host.claude.resumeSession(sessionId, sdkSessionId, cwd, model?, apiVersion?, useWorktree?, worktreePath?, worktreeBranch?, agentPreset?)` 把 9 個參數打包成 options。**Test**（in-process via fake SDK）：(a) call resumeSession、斷言 reply.ok + sdkSessionId echo、session.permissionMode='bypassPermissions'、session.sdkSessionId/model 都對；(b) 接著 sendMessage、斷言 queryOptions.resume 等於 historic SDK id（鎖死「resume → 下次 query 帶 resume:」契約）；(c) missing sdkSessionId / sessionId 各回 missing 錯誤；(d) options.permissionMode='plan' 覆寫 session 預設。仍綠：sidecar / host-api / cargo 112 / Tauri release build 重產 / launch smoke。**結果**：使用者在 Tauri release 重啟 app 後，舊 session 從 history 點開能正確接到 SDK conversation context、下一輪 sendMessage 不會丟掉前文。剩下：(1) `claude.forkSession` / `rewindToPrompt`（rewind 進階 UX）、(2) streaming-input mode mid-stream control、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file under app_data_dir。）

歷史紀錄：2026-05-09 (#31)。（同上 + **新 #31 — sidecar stderr tail capture + emit**：先前 sidecar 的 `child.stderr` 在 Rust 端是 `Stdio::piped()` 但**從來沒人讀** — 後果有兩個：(1) OS pipe buffer 滿了會反壓住 Node 進程，(2) sidecar 真的 die 時使用者只看到一句 `sidecar: child exited`，看不到 Node 的真正錯誤訊息（例如 `Cannot find module '@anthropic-ai/claude-agent-sdk'`、parse error、import 失敗等）。Slice #30 那個 verbatim-path bug 之所以難 debug 就是因為 stderr 沒人接、Node 又 silently exit(0)。本 slice 補完 stderr pipeline：spawn 後另起一條 reader thread 讀 child.stderr line-by-line，把每一行 push 到 `Arc<Mutex<VecDeque<String>>>` 的 ring buffer（cap 100 行），同時 emit `sidecar:stderr` event 給 renderer / DevTools 即時看。當 stdout reader 偵測到 child exit（stdout pipe close）時，用 `snapshot_stderr_tail()` 把 ring buffer join 成多行字串，塞進 pending caller 的 error message：`sidecar: child exited; stderr tail:\n<...>`。STDERR_TAIL_LIMIT=100 行夠抓 Node 啟動 trace，又不會無上限佔記憶體。**Test**：cargo `end_to_end_stderr_tail_surfaces_in_child_exited_error` — mkdtemp 寫一個 `crash.mjs`（`process.stderr.write('SYNTHETIC_STDERR_LINE_FOR_TEST\n'); process.exit(1)`），用真 Node spawn 後 call `ping`、斷言 error message 同時包含 `'SYNTHETIC_STDERR_LINE_FOR_TEST'` 與 `'child exited'` — 鎖死「stderr → user-facing error message」這條診斷契約，未來再有 spawn 失敗一定看得到 Node 自己噴的 trace。仍綠：sidecar / host-api / cargo 112 / Tauri release build 重產 / launch smoke。**結果**：Tauri release 將來只要 sidecar 啟動失敗，使用者直接看到 Node 報的 module/syntax/import 錯誤，不必再像 #30 那樣靠手動 PowerShell spawn 才推得出根因。剩下：(1) custom subagents `agents` option、(2) streaming-input mode mid-stream control、(3) multi-account safeStorage 替代、(4) MCP tools list categories、(5) sidecar log persisted to file under app_data_dir 給 release reproduction。）

歷史紀錄：2026-05-09 (#30)。（同上 + **新 #30 — canUseTool round-trip + Windows verbatim-path spawn fix**：(A) **canUseTool callback**：sidecar 在 `sendMessage` 的 queryOptions 加 `canUseTool` callback，鏡射 `electron/claude-agent-manager.ts:745` 的 permission flow。`canUseTool` 依 session.permissionMode 分流：(1) `AskUserQuestion` 永遠 emit `claude:ask-user` event 等使用者回答；(2) `bypassPlan` auto-allow 全部，但 `ExitPlanMode` 要 prompt（按 allow 自動切到 `bypassPermissions` 並 emit `claude:modeChange`）；(3) `bypassPermissions` 同步 auto-allow 全部、不 emit UI；(4) `acceptEdits` auto-allow `Write/Edit/NotebookEdit/Read/Glob/Grep`、其他工具仍 prompt；(5) `default` / 落非 auto-approval 的工具 emit `claude:permission-request` 等使用者裁示。`ExitPlanMode` 加另一條切換邏輯：`dontAskAgain` → `acceptEdits`、否則 `default`。session record 加 `pendingPermissions / pendingAskUser` 兩個 Map 存 toolUseId→resolve fn。新 handler `claude.resolvePermission(sessionId, toolUseId, result)` / `claude.resolveAskUser(sessionId, toolUseId, answers)` 對應 renderer 的 Allow/Deny / 答題 click。Resolve 後 emit `claude:permission-resolved` / `claude:ask-user-resolved` notification 讓其他 panel 清狀態。Tauri 加 `claude_resolve_permission` / `claude_resolve_ask_user` commands、host-api adapter 加 `resolvePermission` / `resolveAskUser` invoke + 4 個新事件 listener (`onPermissionRequest`/`onAskUser`/`onPermissionResolved`/`onAskUserResolved`，payload key 分別 `data` / `data` / `toolUseId` / `toolUseId`)。**Test**（in-process via fake SDK 攔 canUseTool）：(1) Bash round-trip — fake SDK 接住 canUseTool ref、test trigger `Bash` request、斷言 emit `claude:permission-request`、call `claude.resolvePermission` 之後 SDK promise 拿到 `{behavior:'allow', updatedInput:{command:'ls'}}`、emit `claude:permission-resolved`；(2) `bypassPermissions` mode — `Bash` 同步回 allow、無 UI event；(3) `acceptEdits` — `Edit` 自動 allow、`Bash` 仍 prompt；(4) `AskUserQuestion` — emit `claude:ask-user` data 含 questions、`claude.resolveAskUser` 把 `{q1:'option-A'}` 帶回 SDK promise、emit ask-user-resolved。(B) **Windows `\\?\` verbatim-path bug**：使用者測試 release exe 撞到 `Win32 ERROR_NO_DATA (232) 0x800700E8` — Tauri 的 `app.path().resource_dir()` 在 Windows 回 `\\?\C:\...` (extended-length / verbatim) prefix 路徑，`spawn_sidecar` 把它當 script_path 傳給 bundled Node，**Node 啟動但 sidecar 的 `isMain` 自我偵測掛掉**：原 code `new URL(`file://${argv[1].replace(/\\/g,'/')}`).href === import.meta.url` 在 verbatim path 下變 `file:////?/C:/...` vs `file:///C:/...`，never matches，導致 main() 不跑、process.exit(0) 立刻退場、Rust 那邊看到 stdin pipe close 噴 232。修法：把 isMain 改成正規化兩端 fs path 比對，新 helper `__normalizeMainPath()` 在 Windows 平台 strip `\\?\` prefix + lowercase（NTFS case-insensitive）+ 統一斜線方向，匯出給 test。**Regression test**：用 fake `\\?\C:\foo\BAR\server.mjs` 與 `C:\foo\BAR\server.mjs` 跑 normalize 後 `assert.equal`，再加 case-insensitive 與 empty/null edge cases。**手動驗證**：直接用 PowerShell `Process.Start` 用 `\\?\` script path spawn bundled Node + sidecar，修前 exit code 0 / 無 output → 修後拿到 `{"jsonrpc":"2.0","id":1,"result":{"ok":true,"echo":...,"pid":...}}` 正常 ping reply。仍綠：sidecar / host-api / cargo 111 / Tauri release build 重產 / launch smoke。**結果**：使用者裝 MSI/NSIS 開 Tauri release 第一次按 Claude tool（Bash/Edit 等）會看到正常的 permission dialog；canUseTool round-trip 跨 Rust↔sidecar 走得通；Windows release 不再因 verbatim path 在啟動時靜默 die。剩下：(1) custom subagents `agents` option、(2) streaming-input mode mid-stream control、(3) multi-account safeStorage 替代、(4) MCP tools list categories。）

歷史紀錄：2026-05-09 (#29)。（同上 + **新 #29 — installed plugins → queryOptions.plugins**：sidecar 加 `loadInstalledPlugins()` 鏡射 `electron/claude-agent-manager.ts:828` 的邏輯：read `~/.claude/plugins/installed_plugins.json` (`{plugins: { <bucket>: [{ installPath, ... }] }}` shape，每個 bucket 是個 array)，flatten 出所有 `installPath` 字串、轉成 `[{type:'local', path}]` 餵給 SDK 的 `queryOptions.plugins`。`sendMessage` 在 install plugin 不存在/parse 失敗時 graceful 跳過（跟 Electron 一樣 `installedPlugins.length > 0 ? {plugins} : {}` 條件 spread，不在 queryOptions 裡放空陣列）。新加 test-only override hook `__setPluginsPathOverrideForTests(path)` 讓測試端 mkdtemp 寫 fixture json 不打到使用者真實 `~/.claude`。**Test**（in-process via fake SDK + tmp file）：(1) missing file → `[]`、(2) malformed JSON → `[]`、(3) good json with 3 valid + 3 malformed entries（無 installPath / 字串 / null）→ 拿到 3 個 plugins，都 `type:'local'`、(4) sendMessage 真的把 plugins 串到 queryOptions、(5) 切換到 missing path 後第二輪 sendMessage 的 queryOptions.plugins 必須是 `undefined`（鎖住 conditional spread 的契約）。仍綠：sidecar / host-api / cargo 111。**結果**：使用者透過 Claude CLI 安裝過的 plugin（如 official、community plugins）在 Tauri release 下會被自動 load，跟 Electron 行為一致。剩下：(1) `canUseTool` 權限 callback round-trip（`default` mode 的 tool-use prompt UI）、(2) custom subagents `agents` option、(3) streaming-input mode mid-stream control、(4) multi-account safeStorage 替代。）

歷史紀錄：2026-05-09 (#28)。（同上 + **新 #28 — sidecar sendMessage queryOptions parity with Electron**：sidecar 之前送進 `sdk.query` 的 options 只有 `cwd / model / permissionMode / resume / abortController` — 不夠 Electron 起 Claude Code session 用，缺了 `systemPrompt:{type:'preset',preset:'claude_code'}` + `tools:{type:'preset',preset:'claude_code'}` 兩個 preset 的話 SDK 跑出來是純 Anthropic chat、**沒有 Bash/Read/Edit/Glob/Grep 等 built-in tools**，等同把 Tauri 版降級成 Anthropic console。本 slice 把 queryOptions 一次補齊到跟 `electron/claude-agent-manager.ts:856` 同步：(a) `systemPrompt` + `tools` claude_code preset、(b) `includePartialMessages: true`（partial assistant streaming 開關）、(c) `promptSuggestions: true`、(d) `settingSources: ['user','project','local']`（讓 SDK 讀 `~/.claude/settings.json` + `.claude/settings.json` + `.claude/settings.local.json`）、(e) `agentProgressSummaries: true`、(f) `toolConfig: { askUserQuestion: { previewFormat: 'html' } }`、(g) `effort` 從 session state 透傳、(h) `permissionMode` 多了 `bypassPlan→plan` 映射（SDK 不認 `bypassPlan`）、(i) `bypassPermissions` 同時設 `allowDangerouslySkipPermissions: true`、(j) `pathToClaudeCodeExecutable` 用既有 `resolveClaudeCliBinary()` 抓 sidecar bundle 內的 Claude CLI（release 不依賴 system claude）、(k) `model` 透 sidecar mirror `sdkModelForClaudeSelection()` 把 4 個 auto-compact preset id 轉回 base `claude-opus-4-7`、(l) `autoCompactWindow` 透 `queryOptions.env.CLAUDE_CODE_AUTO_COMPACT_WINDOW` 注入給 spawn 的 claude binary、(m) `resume + continue:true` 當 sdkSessionId 存在 + prompt 為空時，鏡射 Electron 的「resume context but process new input vs. continue autonomous」分支、(n) **images 支援**：把 `params.images` (`data:image/...;base64,...` 陣列) 透 `dataUrlToContentBlock()` 轉 SDK image block、跟 prompt 包成單一 `SDKUserMessage`、用 async generator yield 給 SDK（`prompt: AsyncIterable<SDKUserMessage>` shape）。**Test**（in-process via fake SDK 攔 query() 的 options）：(1) parity 13 條 — `systemPrompt`/`tools`/`includePartialMessages`/`promptSuggestions`/`settingSources`/`agentProgressSummaries`/`toolConfig`/`effort`/`permissionMode`/`allowDangerouslySkipPermissions`/`model`(auto-compact preset → 'claude-opus-4-7')/`env.CLAUDE_CODE_AUTO_COMPACT_WINDOW`/`abortController`；(2) `resume`+`continue` 在 empty prompt 第二輪自動加上；(3) `bypassPlan→plan` 映射；(4) image 流 — 1×1 PNG data URL → 斷言 `prompt` 是 async iterable、drain 後拿到 1 個 user message、content blocks 是 `[image, text]`、image source.media_type=`image/png`、text 內容對；(5) `sdkModelForClaudeSelection` + `dataUrlToContentBlock` 各 5/4 條斷言。仍綠：sidecar / host-api / cargo 111 / Tauri release build 55 秒 / launch smoke。**結果**：Tauri release 下 Claude session 第一次 `sendMessage` 真的能用 Bash/Read/Edit 等 built-in tools，跟 Electron 行為對齊；image 附件、auto-compact、effort、permission bypass 全條件可用；不再依賴 system PATH 上的 claude binary。剩下：(1) `canUseTool` 權限 callback（renderer 互動式同意 UI 的 round-trip）、(2) plugins list、(3) custom subagents `agents` option、(4) streaming-input mode mid-stream control、(5) multi-account safeStorage 替代。）

歷史紀錄：2026-05-09 (#27)。（同上 + **新 #27 — drift guard for `CLAUDE_MODEL_CONTEXT_WINDOWS`**：sidecar 在 #26 為了 `getContextUsage` 的 `maxTokens` 計算而新增的 `CLAUDE_MODEL_CONTEXT_WINDOWS` map 是 TS-side `CLAUDE_BUILTIN_MODEL_CONTEXT_WINDOWS` 的 value-level 鏡射 + 4 個 `OPUS_47_PRESET_AUTO_COMPACT` 衍生 preset entry。#17 已有 keys 級 sorted-equal drift guard（`CLAUDE_BUILTIN_DEDUP_KEYS`），但 values + preset entries 缺保護 — TS 改 `CLAUDE_OPUS_47_BASE_CONTEXT` 從 1M 到別的值、或新增 preset id，sidecar 不會自動察覺。**新測試**：(a) value-level drift — 把 TS map literal 用 regex 抽出 `[key, number]` pair list，逐個斷言 sidecar `CLAUDE_MODEL_CONTEXT_WINDOWS.get(key)` 等於 TS value；(b) preset entry 存在性 — 4 個 hand-derived preset id（`auto-compact-200k/300k/400k`、`:1m`）必須在 sidecar map 裡且值是 positive number。重用 #17 已 locate 的 `ctxMatch[1]` map literal scope 避免重 read 檔案。**結果**：sidecar / host-api / cargo 111 全綠。剩下：(1) MCP tools list 整合到 categories、(2) memory files / agent skills tokens 分類、(3) streaming-input mode、(4) multi-account safeStorage 替代。）

歷史紀錄：2026-05-09 (#26)。（同上 + **新 #26 — usage tracking + cached getContextUsage**：sidecar 在 `claude.sendMessage` 的 stream_event 處理裡多攔 `message_start` / `message_delta` 抽出 usage（input/cache_creation/cache_read tokens），存到 `session.lastUsage`；result message 進來時用 `msg.usage` 蓋掉 mid-stream 估算（result 是該 turn 的 authoritative usage）並補上 `total_cost_usd` + `num_turns`。**`claude.getContextUsage` 從 stub `null` 換成讀 `session.lastUsage` 並組成 renderer ContextUsagePopup 期待的 shape**：`{categories:[{name:'Context',tokens,color}], totalTokens, maxTokens, percentage, model, apiUsage:{input_tokens,output_tokens,cache_creation_input_tokens,cache_read_input_tokens}}`。`maxTokens` 透 `expectedContextWindowForModel(model)` 從 sidecar 新加的 `CLAUDE_MODEL_CONTEXT_WINDOWS` map 拿（鏡射 TS-side `CLAUDE_BUILTIN_MODEL_CONTEXT_WINDOWS` + 加 4 個 auto-compact preset 條目：`auto-compact-200k=200K`、`-300k=300K`、`-400k=400K`、`:1m=1M`）。**Test**：(a) `expectedContextWindowForModel` 7 條斷言 — base id、`[1m]` 變體、preset 變體、unknown→null、undefined→null；(b) fake SDK 噴 message_start usage + 不同 result.usage、call sendMessage、call getContextUsage 斷言 totalTokens=150+50+250、maxTokens=1M（claude-sonnet-4-6）、percentage 正確、apiUsage 4 個欄位都進來；(c) pre-turn 沒 usage cache 回 null、unknown sessionId 也 null（renderer 解讀為「沒資料」）。仍綠：sidecar / host-api / cargo 111 / Tauri release build / launch smoke。**結果**：Tauri release 下 ContextUsagePopup 點開能看到真實 token 數 + 跟模型 context window 比例的進度 bar，而不是空白。剩下：(1) MCP tools list 整合到 categories（renderer 也吃 mcpTools 欄位）、(2) memory files / agent skills tokens 分類、(3) streaming-input mode、(4) multi-account safeStorage 替代。）

歷史紀錄：2026-05-09 (#25)。（同上 + **新 #25 — stream_event → claude:stream 即時 delta**：sidecar 在 SDK 訊息處理迴圈裡多解 `type: 'stream_event'`（SDKPartialAssistantMessage） — 只處理 `event.type === 'content_block_delta'` 含 `delta.text` 或 `delta.thinking` 的 case，emit `claude:stream` payload `{data: {text|thinking, parentToolUseId}}`，跟 Electron 同樣 filter。其他 stream event 變體（`message_start`/`message_delta` usage tracking、`ping`、其他 control event）這層丟掉 — usage tracking 等 metadata 拓展再做。**Test**（in-process via fake SDK）：噴出 `message_start` + 2 個 text delta + 1 個 thinking delta + 1 個 ping，斷言剛好 3 個 `claude:stream` event、payload.data.text/thinking 內容對、`parentToolUseId: null`、`message_start`/`ping` 沒誤 forward。renderer 在 Tauri 下能即時看到字逐個跳出來，不是等整段 assistant message 才一次顯示。仍綠：sidecar / host-api / cargo 111 / Tauri release build / launch smoke。剩下：(1) `claude:stream` 帶的 metadata tracking（usage update → claude:status emit，要 patch session.metadata）、(2) `getContextUsage` 從 result.usage 快取、(3) streaming-input mode mid-stream control、(4) multi-account safeStorage 替代。）

歷史紀錄：2026-05-09 (#24)。（同上 + **新 #24 — abort path lock + 三條 SDK-augmented read APIs**：(1) **abortSession 中段 stream 必須立刻終止**：注入 fake SDK 慢 stream（25ms 一個 chunk × 50 個），sendMessage 跑到 80ms 後拿到 ≥2 個 event 再 abortSession，斷言 sendMessage 在 1 秒內 settle、abort signal 真的傳到 SDK iterator（fake SDK 看到 `signal.aborted=true` 主動 throw）、最終 `claude:turn-end{reason:'aborted'}` event 有 emit — 鎖住 renderer 停止鈕的真正可用性、不只是 fire-and-forget。(2) **`claude.getSupportedCommands` / `getSupportedAgents` / `getAccountInfo`** 從 stub `[]` / `null` 換成跟 `getSupportedModels` 同樣的 SDK augmentation pattern：lazy SDK load → 起 Query instance call read API → catch fall back 空陣列 / null。test 覆蓋三組：fake SDK 注入回真實資料、fake SDK 強制 null fallback、fake SDK throw 仍要 graceful return — 三條都鎖 dual-mode contract，dev 通過不會掩蓋 release fall back regression。**結果**：renderer 在 Tauri 下能拿到真實 slash commands list（之前空陣列導致 `/` 選單沒選項）、subagents list、目前帳號的 email + subscription type。仍綠：sidecar (新增 5 條斷言) / host-api / cargo 111 / Tauri release build / launch smoke。剩下：(1) streaming-input mode mid-stream control（abort 已驗證可用、setPermissionMode/setModel mid-turn 是 nice-to-have）、(2) multi-account 切換 + safeStorage 替代、(3) `getContextUsage`（SDK 有 control method 但要 active session）。）

歷史紀錄：2026-05-09 (#23)。（同上 + **新 #23 — wire claude.authLogin + bundled CLI for auth ops**：sidecar 加 `resolveClaudeCliBinary()` 走訪 `node-sidecar/node_modules/@anthropic-ai/claude-agent-sdk-<triple>/claude[.exe]`（rotate 6 個 platform/arch triple 嘗試找對應 host 的）拿到 SDK bundle 內附的 Claude Code CLI binary path，第一次找到後 cache（測試可透 `__resetClaudeCliCacheForTests()` 清掉）；`authStatus` / `authLogin` / `authLogout` 全部改成統一透 `spawnClaudeCli()` helper 走「bundled CLI 優先 → fall back 系統 PATH」的解析。**`claude.authLogin` 從 stub `STUB_AUTH_ERR` 換成真實 spawn `claude auth login`** —180s timeout 包住 browser-based OAuth flow，CLI 跑 callback 後自然 exit 我們就 resolve `{success: true}`，不論 dev/release 第一次裝 MSI 沒 system claude 的使用者都能透 sidecar 完成 OAuth 拿到 ~/.claude credentials。`BAT_SIDECAR_CLAUDE_BIN` env override 給測試 inject 任意 binary path 跳過 cache + 不打網路。**Test**：(a) `resolveClaudeCliBinary()` round-trip — 若 sidecar/node_modules 已裝就斷言路徑落在 `@anthropic-ai/claude-agent-sdk-<triple>/claude[.exe]`，否則 graceful skip；(b) env override 注入 `process.execPath`、call `claude.authLogin` / `authLogout` 透 dispatch、斷言不再回 stub error 字串（鎖住「拉出 stub 換真實 spawn」的 contract）；(c) cache 重置 hook 確保 env override 即時生效。仍綠：sidecar / host-api / cargo 111 / Tauri release build / launch smoke。**結果**：fresh MSI 安裝者第一次點「登入」就能完成 OAuth、claude credentials 落到 ~/.claude、後續 sendMessage 真的能跟 Claude 對話 — release end-to-end UX 從「需要先用系統 claude 登入」變成「裝完 MSI 開即用」。剩下：(1) streaming-input mode mid-stream control、(2) multi-account 切換要 Tauri 端 keychain 替代 Electron safeStorage（`accountSwitch/accountRemove/accountLoginNew/accountImportCurrent` 仍 stub）。）

歷史紀錄：2026-05-09 (#22)。（同上 + **新 #22 — tool_use / tool_result event mapping**：sidecar 在 `claude.sendMessage` 的 SDK 訊息處理器裡多解析 content blocks — assistant 訊息裡的 `tool_use` block 額外 emit `claude:tool-use` event（payload `toolCall: {id, sessionId, toolName, input, status:'running', parentToolUseId, timestamp}`），user 訊息裡的 `tool_result` block 額外 emit `claude:tool-result` event（payload `result: {id: tool_use_id, status: is_error ? 'error' : 'success', result: content}`）。映射跟 Electron `addToolCall/updateToolCall` 對齊，renderer 的 ToolCall panel 在 Tauri 下能正常顯示工具執行流程（之前只有 `claude:message` 拿到原始 BetaMessage，UI 要自己 parse blocks，現在跟 Electron 一樣有 dedicated event）。**新增測試**：fake SDK 噴出兩段「assistant tool_use → user tool_result」（一次 success Bash、一次 error Read），斷言 2 個 tool-use + 2 個 tool-result event、每個 payload key 與欄位、`is_error: true` 對應 `status: 'error'`、`is_error: false` 對應 `'success'`。所有 sidecar / host-api / cargo 111 / tsc / Tauri release build / launch smoke 全綠。剩下：(1) streaming-input mode（mid-stream interrupt / setPermissionMode / setModel control）、(2) MCP server config、(3) 完整 metadata + cost tracking、(4) 真實 account auth flow（login + safeStorage 替代）。前三項是 SDK 進階用法、第四項要等 Tauri 端有 keychain 介面才能做。）

歷史紀錄：2026-05-09 (#21)。（同上 + **新 #21 — real claude.sendMessage via SDK**：sidecar 的 `claude.sendMessage` 從 stub `(stub reply)` 換成真實 `@anthropic-ai/claude-agent-sdk` 驅動 — 每次 sendMessage 用 `query({prompt, options})` 起 single-shot stream、捕捉 SDK 回的 `session_id` 存進 session record 當下次 `resume`，達成 multi-turn context。SDKMessage→event 映射：`system/init` → `claude:status`（含 sdkSessionId/cwd/model/permissionMode 的 metadata 對齊 Electron contract）、`assistant` → `claude:message`（原 BetaMessage 透傳，renderer 已知道怎麼解 text/tool_use blocks）、`result/success` → `claude:result` + `claude:turn-end{reason:'completed', sdkSessionId}`、`result/error` → `claude:error` + `claude:turn-end{reason:'error'}`、throw/abort → `claude:error`（非 abort 時）+ `claude:turn-end`。新增 per-session `sdkSessionId/abortController/streaming` 欄位；`stopSession`/`abortSession` 都呼叫 abortController.abort()；concurrent send 在 streaming 期間直接 reject。**保留 stub fallback**：SDK 載入失敗（release 沒 bundle / dev 沒 install）回原本 stub 回覆 + log 到 stderr，讓 renderer 不會 hang。**新增 `BAT_SIDECAR_DISABLE_SDK=1` env 開關**強制走 stub path，給 sidecar e2e + cargo 測試用（不能讓測試實打 Claude API）。SpawnConfig 加 `extra_env: Vec<(String,String)>` 把 env 從 Rust 注入給 Node。**Test 設計**：(a) sidecar in-process — 注入 fake SDK、call sendMessage、斷言 4 個 event 順序與 payload key（status.meta.sdkSessionId / message.message.content / result.result / turn-end.payload.reason+sdkSessionId）；(b) 同上 — 第二次 sendMessage 必帶 `resume: <sdkSessionId>`，鎖住 multi-turn context preservation；(c) concurrent send 衝突 — pre-flag streaming 後 send 必回 `{ok:false, error:/streaming/}`；(d) stub fallback — `__setSdkOverrideForTests(null)` 後 sendMessage 回 `{ok:true, stub:true}` 並 emit message+turn-end，鎖住 release-without-bundle UX。原 cargo `end_to_end_session_lifecycle_emits_events` 改用 SpawnConfig.extra_env 帶 `BAT_SIDECAR_DISABLE_SDK=1` 走 stub path。**結果**：sidecar tests 全綠（多新增 4 條 in-process 斷言）、cargo 111 全綠、host-api 8 情境綠、tsc noEmit 綠、tauri release build 49.x 秒、smoke 綠。renderer 在 dev/release 都能透過 sidecar 真的跟 Claude 對話一輪 multi-turn 對話（前提：使用者已 `claude auth login` 過、機器有 NETWORK + SDK 內 bundled CLI 能跑）。剩下：(1) tool_use / tool_result event 細節（renderer 解析從 BetaMessage content blocks 抽出，但若需要 Electron-equivalent metadata tracking 還要再 port）、(2) streaming-input mode（mid-stream interrupt/setPermissionMode/setModel control 方法）、(3) MCP server 設定、(4) 完整 metadata + 成本追蹤 — 都是 nice-to-have，不擋 release 基本可用。）

歷史紀錄：2026-05-09 (#20)。（同上 + **新 #20 — bundled SDK node_modules**：把 `@anthropic-ai/claude-agent-sdk` + 它的 transitive deps 也 bundle 進 release，sidecar 在 release 不再需要 host project 的 node_modules。新建 `node-sidecar/package.json`（only deps：`@anthropic-ai/claude-agent-sdk@0.2.128` + `zod@^4.3.6`）+ `node-sidecar/.npmrc`（`node-linker=hoisted` 強制 flat layout，避免 pnpm 預設 symlink 樹被 Tauri bundle 破壞）。`pnpm --dir node-sidecar install` 拿到 274MB 樹（其中 243MB 是 `@anthropic-ai/claude-agent-sdk-win32-x64/claude.exe` — SDK 內建的 Claude Code CLI binary，跟原本 `@anthropic-ai/claude-code` 等價但是 platform-specific optional dep）。tauri.conf.json `bundle.resources` 加 `node-sidecar/package.json` + `node-sidecar/node_modules/`。新增 root-level helper script `pnpm run prepare:tauri-bundle`：先 fetch Node runtime、再 install sidecar deps，release pipeline 一行就能備齊 bundle inputs。**End-to-end 驗證**（cargo `end_to_end_bundled_sdk_loads_through_bundled_node`）：用 bundled Node 起 sidecar、call `claude.getSupportedModels`、斷言結果 ≥7 個 + 至少一個 `source: 'sdk'` — bundled SDK 沒被 sidecar reach 到的話這條會炸；node_modules 沒裝時 graceful skip。release MSI 從 31MB → 112MB，NSIS 從 22MB → 73MB（含 ~310MB raw payload，兩種 installer 各自壓縮）。launch smoke 綠。cargo 111、sidecar、host-api、tsc 全綠。**現在 release 真的 self-contained**：使用者不用裝 Node、也不用裝 Claude Code CLI（SDK 內建 win32-x64 native binding 會被走，未來 macOS/Linux build 跑 `pnpm run prepare:tauri-bundle` 在那台 host 上就會 install 對應 platform 的 optional dep）。剩下：(1) 把 sidecar `claude.startSession`/`sendMessage` 從 stub 換成真實 SDK 呼叫（bundle 已備好），(2) macOS/Linux 上實機驗證 prepare 步驟產出對的 platform binding。）

歷史紀錄：2026-05-09 (#18+#19)。（同上 + **新 #18+#19 — bundled Node runtime**：把 Node interpreter 直接 bundle 進 Tauri release exe，release 不再依賴使用者機器上的 Node。**Resolver 邏輯**（#18）：sidecar.rs `find_bundled_node()` 先在 `<resource_dir>/node-runtime/<plat>-<arch>/[bin/]node[.exe]` 找 binary，再退到 `<resource_dir>/node-runtime/node[.exe]` 平層 fallback，再退到 PATH lookup（`tauri dev` + 單元測試保留無痛路徑）。triple 用 Rust-style arch 名（windows-x86_64 / darwin-aarch64 / linux-x86_64）跟 std::env::consts::ARCH 對齊不轉譯。加 4 條 cargo unit test：missing dir、platform-arch with bin/、flat fallback、prefer-platform-over-flat。**Bundle 流程**（#19）：`scripts/fetch-node-runtime.mjs` 從 nodejs.org 下載 portable archive（Node v20.18.1 LTS 預設）並萃取到 `node-sidecar/runtime/<plat>-<arch>/`，prune 到只保留 node binary + LICENSE 把單一 platform size 從 150MB 砍到 67MB（windows-x64）。Windows 用 `C:\Windows\System32\tar.exe`（bsdtar 內建處理 zip）— 比 PowerShell `Expand-Archive` 在 Node.org zip 上的路徑解析更穩。tauri.conf.json `bundle.resources` 加 `"../node-sidecar/runtime/": "node-runtime/"` 把整個 runtime 樹搬進 bundle。`.gitignore` 把實際 binaries 排除掉但保留 `.gitkeep` + `README.md` 確保 dir 存在以滿足 Tauri bundle.resources 不可空源 path 的要求。`pnpm run fetch:node-runtime` script 預設 host platform、`--all` 跑所有 triple、`--target=` 單一指定、`--force` 重抓。**End-to-end 驗證**（cargo `end_to_end_bundled_node_runs_sidecar`）：直接用 `node-sidecar/runtime/<plat>-<arch>/` 的 binary 起 sidecar、ping 一輪、斷言 ok+echo — 無 binary 時 graceful skip 不阻 CI。Tauri release build 49.58 秒，MSI 31MB / NSIS 22MB（已含 67MB raw Node，installer 壓縮過）、smoke 綠。cargo 110、sidecar、host-api、ts noEmit 全綠。剩下：(1) 把 `@anthropic-ai/claude-agent-sdk` 跟它的 transitive deps 也 bundle 進 sidecar runtime（裝到 `node-sidecar/node_modules/`，這樣 release 真的 self-contained），(2) 真實 startSession/sendMessage SDK 串流。）

歷史紀錄：2026-05-09 (#17)。（同上 + **新 #17**：sidecar 加 lazy `loadAnthropicSdk()` loader（一次性 import + cache，失敗 cache null），並把 `claude.getSupportedModels` 從純 builtin 改成 builtin + SDK augmentation — SDK 載入失敗（release 沒 bundled node_modules）就回純 builtin、跟 Electron 在 SDK 失敗時的 fallback 完全一致。dedup 用新 mirror 常數 `CLAUDE_BUILTIN_DEDUP_KEYS`（鏡射 `CLAUDE_BUILTIN_MODEL_CONTEXT_WINDOWS` 的 keys，含 base id 與 `[1m]` 變體），SDK 結果若是 builtin 的 base 或 [1m] 變體就 filter 掉，剩下的 tag `source: 'sdk'` append 到 builtin 後面。同步把 `claude.authLogout` 從固定 `{success:true}` stub 換成 execFile `claude auth logout`（10s timeout，error 回 `{success:false, error}`）。**Test 設計**：(a) 主測試放寬 — accept ≥ builtins.length，斷言每個 builtin 都在結果裡且 tag 正確、SDK extras 都 tag `'sdk'`，這樣 dev/release 兩邊都通過；(b) 加 `__setSdkOverrideForTests(null)` test-only hook 強制走 SDK-unavailable 路徑、斷言結果剛好等於 builtin 數量、全部 tag `'builtin'` — 把 release fallback 鎖住，dev 通過不會掩蓋 release regression；(c) 加正向 augmentation 測試：注入 fake SDK 回 3 個 model（一個 dup base、一個 base[1m]、一個全新）、斷言 dedup 後只剩全新那個被 augment 上來；(d) 加第二條 drift guard：sidecar `CLAUDE_BUILTIN_DEDUP_KEYS` 與 TS `CLAUDE_BUILTIN_MODEL_CONTEXT_WINDOWS` keys 直接 sorted-equal。所有測試仍綠（sidecar 4 個 model 相關斷言全 + host-api + cargo 105 + ts noEmit），Tauri release build 41.98 秒、smoke 綠、MSI/NSIS bundle 都產出。）

歷史紀錄：2026-05-09 (#16)。（同上 + **新 #16**：把 6 個 per-session state setter（`setAutoContinue/setPermissionMode/setModel/setEffort/resetSession` + `getAutoContinue`）從 permissive fallback 拉出來顯式 route，sidecar 加 per-session state map（model/effort/permissionMode/autoCompactWindow/autoContinue），由 `startSession` 從 options 預先填入、setter 各自 patch、`getSessionState/getSessionMeta` 讀回。`setPermissionMode` 還會 emit `claude:modeChange` event 跟 Electron 一致。`getWorktreeStatus(sessionId)` 也接到剛才 port 的 worktreeStatus（之前回 null）。新增 6 條 Tauri commands + adapter 顯式 routes + host-api 測試 + sidecar in-process round-trip 測試 + 一條 cargo end-to-end 測試（透 spawn Node 跑完整 startSession→setters→getters→reset 流程）。cargo 測試 105 條全綠。Tauri release build 跑中。）

歷史紀錄：2026-05-09 (#15)。（同上 + **新 #15**：把 `claude.getSupportedModels` 從 stub `[]` 換成真實 `CLAUDE_BUILTIN_MODELS` 鏡射列表（每個 entry 加 `source: 'builtin'`）。renderer 的模型選單在 Tauri 下會看到 7 個內建選項：Opus 4.7 200K/300K/400K/1M、Opus 4.6、Sonnet 4.6、Haiku 4.5。SDK augmentation（從 `@anthropic-ai/claude-agent-sdk` 拿動態 model list）等真實 SDK port 完才接，目前 fall back 跟 Electron 在 SDK 載入失敗時的行為相同。新增 drift guard test：sidecar 直接 read `src/utils/claude-model-presets.ts`，regex 解析 array literal 內的 `value:` 與 `CLAUDE_OPUS_47_*` 符號常數，斷言 sorted-equal — 漏改 sidecar 鏡射會立刻 fail。所有測試仍綠。）

歷史紀錄：2026-05-09 (#14)。（同上 + **新 #14**：把 `claude.cleanupWorktree` 從 permissive fallback 拉出來顯式 route — sidecar handler 呼叫剛才 port 的 `worktreeRemove(sessionId, deleteBranch)` 並 emit `claude:worktree-info` event 通知 renderer 重置 UI。session-state mutation（cwd 切回 originalCwd）那部分仍要等 agent SDK port 完才有意義 — 目前 session 在 Electron 那邊管。新增 `claude_cleanup_worktree` Tauri command + adapter 顯式 route + host-api 測試。Tauri release build #13 後重跑：49.5 秒、smoke test 綠、release exe 一樣 ~12 MB + MSI/NSIS bundle 都產出。所有 sidecar / host-api / cargo / ts / smoke 全綠。）

歷史紀錄：2026-05-09 (#13)。（同上 + **新 #13**：把整個 worktree manager port 進 sidecar — `worktree.create/remove/status/rehydrate` 從 stub 換成真實 git execFile + fs 實作，鏡射 `electron/worktree-manager.ts` 約 250 LOC，包含 `.bat-worktrees/` 目錄管理、自動加 `.git/info/exclude`、untracked `.claude/` symlink/junction、衝突分支自動加 timestamp 後綴、worktree 強制移除回退手動 `rm -rf` + `git worktree prune`、source branch async 解析。`worktree.merge` 維持 stub（Electron 那邊也叫了不存在的 `mergeWorktree` 方法，feature 在 Electron 也是壞的，先不偽造）。新增 fixture-driven test：跑 `git init` 建臨時 repo + commit + worktreeCreate + status + remove，斷言 `.bat-worktrees/<shortId>` 真的在硬碟上、`.git/info/exclude` 被更新、source branch 抓到 `main`、remove 後路徑消失。Windows EBUSY 用 retry-with-delay 處理。所有測試仍綠，Tauri release build 跑中。）

歷史紀錄：2026-05-09 (#12)。（同上 + **新 #12**：sidecar 的 `agent.listPresets` 從 stub `[]` 換成真實 preset id 列表，鏡射 `src/types/agent-presets.ts::AGENT_PRESETS`。NewTerminalQuickPick 的 preset 卡片在 Tauri 下會全部亮起（之前 sidecar 回 [] 會把所有非 'none' 的卡片視為 unsupported 而 disable）。新增 drift guard 測試：sidecar 直接 read renderer-side TS 檔，regex 抓所有 `id: '...'` literals，斷言 sorted-equal — 若日後新增 preset 沒同步更新 sidecar，測試立即 fail。所有測試仍綠。）

歷史紀錄：2026-05-09 (#11)。（同上 + **新 #11**：再 port `claude.scanSkills` — 走訪 `<cwd>/.claude/skills/` 與 `~/.claude/skills/`，吃 SKILL.md（subdir）與 *.md（top-level），parse YAML frontmatter 抓 name/description，沒有 frontmatter 就退回首條 heading。新增 `claude_scan_skills` Tauri command + adapter 顯式路由（從 permissive fallback 拉出）。renderer 的 SkillsPanel 在 Tauri 下會直接拿到掃描結果，不再走 warn-once null。新增 host-api 測試 + sidecar fixture-driven 測試。所有測試仍綠。）

歷史紀錄：2026-05-09 (#10)。（同上 + **新 #10**：把 `openai.listSessions` 從 stub 換成真實實作 — 走訪 `~/.better-agent-terminal/openai-sessions/<yyyy>/<mm>/<dd>/*.jsonl`，每檔 parse JSONL 抓首條 user message 當 preview，mtime desc 排序，與 `electron/openai-agent/persistence.ts::listAllSessions` 行為一致。renderer 切到 OpenAI panel 時 resume list 會直接拿到 Electron 累積的 session 紀錄，不需要等 OpenAI 真實 manager port 完。所有 sidecar e2e + cargo + host-api + ts 仍綠。）

歷史紀錄：2026-05-09 (#9)。（Phase 1 + Phase 2 全綠 + Phase 3 半完成：(a) sidecar 透過 `bundle.resources` 進 NSIS/MSI release exe；(b) `host.update.check` 透 sidecar fetch 拿 GitHub Releases；(c) remote/tunnel namespaces 全 stub-routed；(d) tauri-launch smoke test 看到 `thread '...' panicked` 直接 fail；(e) #8：`claude.getCliPath` + `claude.listSessions` 從 sidecar stub 換成真實實作；(f) **新 #9**：再 port 兩條 — `claude.authStatus`（execFile `claude auth status`，10s timeout，parse JSON or null）與 `claude.accountList`（讀 `<data-dir>/claude-accounts.json`，sanitize 出 id/email/subscriptionType/isDefault/createdAt 公開欄位，credential blob 從不碰），同時把 `data_dir` 加進 `SpawnConfig`，Rust spawn 時透 `BAT_SIDECAR_DATA_DIR` env 注入給 sidecar，sidecar 在 env 沒設時退回 platform-default（`%APPDATA%/BetterAgentTerminal/`、`~/Library/Application Support/better-agent-terminal/`、`~/.config/better-agent-terminal/`）。Electron-side AccountManager 寫的 index 檔可以被 Tauri sidecar 直接讀，不必雙寫。剩下 Phase 3 expensive 的工作：(1) bundle Node runtime（pkg / bun --compile / Node SEA）；(2) 實打 `@anthropic-ai/claude-agent-sdk` 進 sidecar 把 stub 的 startSession/sendMessage 換成真實 Anthropic call；(3) 同樣對 OpenAI / worktree-manager / remote / tunnel 走真實實作；(4) accountSwitch / accountRemove 真實寫入（需要對應的 safeStorage 替代品 — 暫時 keep stub）。cargo test 104、host-api 8 情境、sidecar 測試（findClaudeCliPath + listSessionsFallback + readAccountIndex 的 fixture-driven 測試 + resolveDataDir branches）、Tauri release build & smoke 全綠。）

### 已完成

- [x] **Host API adapter** — `src/host-api.ts` 提供 `host` proxy，由 `getHostKind()` 判斷 Electron/Tauri/unknown，全部命名空間預設 throw 「not yet implemented」避免靜默失敗。
- [x] **Renamed `window.electronAPI` → `window.batAppAPI`** — preload 和 31 個 callsite 一起改名，TS type 由 `BatAppAPI = Window['batAppAPI']` 取得，不再依賴跨 project reference 編譯。`AboutPanel`、`UpdateNotification` 兩個 callsite 已切到 adapter 作為示範。
- [x] **Tauri 2 scaffolding** — `src-tauri/`、`vite.tauri.config.ts`、`pnpm tauri:dev|build` 指令。
- [x] **首批 Rust commands**
  - `settings_load` / `settings_save`（讀寫 `<app-data>/settings.json`）。
  - `settings_get_shell_path`（純函式 + 平台分支，可單元測試 `exists` 注入）。
  - `shell_open_external`（透過 `tauri-plugin-opener`，拒絕 `file://`）。
  - `shell_open_path`（同樣走 opener，拒絕空字串）。
  - `dialog_confirm`（OK/Cancel modal，spawn_blocking 包覆）。
  - `dialog_select_folder` / `dialog_select_files` / `dialog_select_images`（native picker + 預設 home 目錄 + 圖片副檔名 filter）。
  - `fs_read_file`（512 KiB 上限 + `path_guard::is_sensitive_path` 的 deny-list）。
  - `fs_home` / `fs_readdir` / `fs_list_dirs` / `fs_mkdir` / `fs_delete_path` / `fs_quick_locations` / `fs_search`（含 `~` 擴展、IGNORED 名單、目錄優先排序、深度 8 / 最多 100 結果限制）。
  - `clipboard_write_text`（透過 `tauri-plugin-clipboard-manager`）。
  - `image_read_as_data_url`（10 MiB cap + 副檔名 → mime 對應 + base64 編碼，沿用 `path_guard`）。
  - `pty_create` / `pty_write` / `pty_resize` / `pty_kill`（`portable-pty` 0.8 + reader thread + exit watcher，事件透過 `Emitter::emit("pty:output"|"pty:exit")`）。
  - `workspace_load` / `workspace_save`（`<app-data>/workspaces.json`，single-window MVP）。
  - `update_get_version`（讀 `PackageInfo.version`，`update.check` 留給 Phase 3 packaging）。
  - `debug_log`（renderer 端 log 寫到 stderr，未來改接 Rust file logger）。
  - `git_get_github_url` / `git_get_branch` / `git_get_log` / `git_get_diff` / `git_get_diff_files` / `git_get_root` / `git_get_status`（shell-out 到系統 `git` binary，cwd 校驗 + 25ms-poll timeout + 5 MiB stdout cap，git 失敗一律塞 None / 空集合，與 Electron handler 行為一致）。
  - `app_get_window_id` / `app_get_window_index` / `app_get_launch_profile` / `app_get_window_profile` / `app_new_window` / `app_focus_next_window` / `app_open_new_instance` / `app_set_dock_badge`（single-window MVP；全部回常數 / None / `alreadyOpen=true`，讓 App.tsx 不會在啟動時 throw）。
  - `notification_list` / `notification_mark_read` / `notification_mark_all_read` / `notification_mark_window_read` / `notification_clear` / `notification_focus_latest_unread` / `notification_focus_entry`（記憶體內 `Mutex<Vec<NotificationEntry>>`，更新一律 emit `notification:update`；MVP 沒有 producer 所以開機就是空集合，等 agent sidecar 上線再呼叫內部 helper `add_entry()`）。
  - `github_check_cli` / `github_pr_list` / `github_issue_list` / `github_pr_view` / `github_issue_view` / `github_pr_comment` / `github_issue_comment`（shell-out 到 `gh` CLI，read 命令直接吐 `serde_json::Value`，write 命令回 `{success}` / `{error}`，與 Electron handler 對齊；checkCli 用 `gh auth token` 而非 `gh auth status` 避免次帳號異常拖垮主帳號的判讀）。
  - `snippet_get_all` / `snippet_get_by_id` / `snippet_get_favorites` / `snippet_search` / `snippet_get_by_workspace` / `snippet_get_categories` / `snippet_create` / `snippet_update` / `snippet_delete` / `snippet_toggle_favorite`（JSON 檔 `<app-data>/snippets.json`，schema 與 Electron 一致，包含 `action` 欄位的 backfill migration；存檔走 tempfile+rename，讀檔失敗就回空集合並 `next_id=1`）。
  - `profile_*`（13 個 commands；single-window MVP stub：永遠回一個固定 `default` local profile；mutation 命令接收參數但只有 default slot 真實存在；等 multi-window / remote profile 重建時換成真正的 on-disk index，目前 workspace 狀態已透過 `host.workspace.{load,save}` 落盤所以 stub 就夠跑）。
  - `claude_ping` / `claude_auth_status` / `claude_account_list`（Phase 2 起手；皆轉發 `SidecarState::call` 到 Node sidecar 的 JSON-RPC handler。`claude_ping` 只給 cargo 整合測試用，renderer 端不暴露；renderer 看到的是 `host.claude.authStatus()` / `host.claude.accountList()`，shape 與 Electron preload 對齊（authStatus 回 null = 未登入；accountList 目前 Node stub 回 [] 直到實際把 `@anthropic-ai/claude-agent-sdk` 搬進 sidecar）。`SpawnConfig` 解析順序：`BAT_SIDECAR_SCRIPT` env → `<resource_dir>/node-sidecar/src/server.mjs` → `<cwd>/node-sidecar/src/server.mjs`，dev / test / release 各有歸位。預設 timeout 15 秒。
  - `claude_start_session` / `claude_send_message` / `claude_stop_session` / `claude_abort_session`（Phase 2 slice 2；transmit `(sessionId, options/prompt/images/autoCompactWindow)` 透 JSON-RPC，timeout 5 分鐘，因為真實 sendMessage stream 可能跑很久。abort/stop 用標準 15 秒 timeout）。
  - `claude_auth_login` / `claude_auth_logout` / `claude_account_import_current` / `claude_account_login_new` / `claude_account_switch` / `claude_account_remove` / `claude_account_mark_warning_shown`（Phase 2 slice 3；auth/account 7 條，其中 login 系列目前回 `{success:false, error: 'sidecar stub'}` 等真實 SDK 接管，其餘按 Electron 簽章塞 sensible defaults）。
  - `claude_get_cli_path` / `claude_list_sessions` / `claude_get_supported_models` / `claude_get_supported_commands` / `claude_get_supported_agents` / `claude_get_account_info` / `claude_get_session_state` / `claude_get_session_meta` / `claude_get_context_usage` / `claude_get_worktree_status`（Phase 2 slice 3；10 條 read-only metadata。`getCliPath` 與 `listSessions` 已在 Phase 3 #8 改成真實實作（前者掃 PATH，後者讀 `~/.claude/projects/<encoded>/*.jsonl`），其餘 8 條仍回 null/[]/'' 直到 Anthropic SDK 上線。讓 renderer 每個 panel mount 都不會崩）。
  - `openai_get_api_key_status` / `openai_set_api_key` / `openai_clear_api_key` / `openai_list_sessions` / `openai_compact_now`（Phase 2 slice 4；OpenAI agent manager 整體還在 Electron 端，sidecar handler 全部回 sensible defaults）。
  - `worktree_create` / `worktree_remove` / `worktree_status` / `worktree_merge` / `worktree_rehydrate`（Phase 2 slice 4 起 stub-routed；#13 把 create/remove/status/rehydrate 換成真實 port，鏡射 `electron/worktree-manager.ts`：sessionId 對應 `.bat-worktrees/<shortId>` 子資料夾、auto exclude、衝突分支加 timestamp、untracked `.claude/` symlink/junction、status 跑 `git diff <source>...<branch>`。state 用 sidecar 模組 scope `Map<sessionId, WorktreeInfo>`。`worktree_merge` 維持 stub — Electron 端 register-handlers 呼叫不存在的 `mergeWorktree` 方法，feature 在 Electron 也是壞的）。
  - `agent_list_presets`（Phase 2 slice 4；單一 read-only，回空陣列直到 preset registry 進 sidecar）。
  - `worker_buffer_init` / `worker_buffer_append` / `worker_buffer_read_all` / `worker_buffer_clear`（Phase 2 slice 4；Rust 端用 `Mutex<HashMap<String,String>>` 撐 panel 暫存區，1 MiB cap + line-aligned trim，不走 sidecar — 寫入率高於 stdio 線適合直接 Rust 化）。
  - `SidecarState` 加 `EventSink` 機制：reader thread 看到 id-less `event:foo:bar` notification 就把 `event:` 前綴脫掉，把 params 透 sink 推出去。production code 用 `app_handle_emit_sink(app)` 把 sink 包成 `app.emit("foo:bar", params)`，測試端塞 `Arc<Mutex<Vec>>` collector，因此事件流可以在 cargo unit test 中用真 spawn Node 跑出端到端驗證。
- [x] **Adapter Tauri routing**
  - 已 port：`host.settings.{load,save,getShellPath}`、`host.shell.{openExternal,openPath}`、`host.dialog.{confirm,selectFolder,selectFiles,selectImages}`、`host.fs.{readFile,home,readdir,listDirs,mkdir,deletePath,quickLocations,search}`、`host.clipboard.writeText`、`host.image.readAsDataUrl`、`host.pty.{create,write,resize,kill,onOutput,onExit}`、`host.workspace.{load,save,getDetachedId}`、`host.update.getVersion`、`host.debug.log`、`host.git.{getGithubUrl,getBranch,getLog,getDiff,getDiffFiles,getRoot,getStatus}`、`host.app.{getWindowId,getWindowIndex,getLaunchProfile,getWindowProfile,newWindow,focusNextWindow,openNewInstance,setDockBadge}`、`host.notification.*`、`host.system.onResume`（Tauri 下回 no-op unsub）、`host.github.{checkCli,listPRs,listIssues,viewPR,viewIssue,commentPR,commentIssue}`、`host.snippet.{getAll,getById,getFavorites,search,getByWorkspace,getCategories,create,update,delete,toggleFavorite}`、`host.profile.*`、`host.claude.{authStatus,accountList,startSession,sendMessage,stopSession,abortSession,onMessage,onToolUse,onToolResult,onResult,onTurnEnd,onError}`（其餘 20+ 個 claude 方法走 namespace-level proxy，每個方法都丟 per-method `not yet implemented`）。
  - 仍 throw `not implemented`：`host.shell.getPathForFile`、`host.settings.{clearTerminalHistory,detectCx}`、`host.clipboard.{saveImage,writeImage,onCopyShortcut}`、`host.image.saveDataUrl`、`host.fs.{resolvePathLinks,watch,unwatch,onChanged}`、`host.pty.{restart,getCwd}`、`host.update.check`、`host.claude.*`（已 port 12 個之外的 20+ 個方法 — setAutoContinue / getAutoContinue / setPermissionMode / setModel / setEffort / resetSession / getSupportedModels/Commands/Agents / getAccountInfo / scanSkills / getSessionState / getSessionMeta / getContextUsage / getWorktreeStatus / cleanupWorktree 等）、worktree/openai/agent/worker/remote/tunnel 等 Phase 2 / Phase 3 命名空間。
- [x] **Tests**
  - `tests/host-api.test.ts`：8 個情境，第 3 個 invoke 情境涵蓋 70+ 條 cmd（settings、shell、dialog、fs、clipboard、image、pty、workspace、update、debug、git、app、notification、github、snippet、profile、claude），含 optional title、camelCase 參數、undefined args（picker no-arg invokes）、optional 參數的 undefined 透傳（`commit_hash`、`workspaceId` 等）。情境 4 加上 claude 半 port 的 per-method canary（`claude.startSession` throw）。
  - `tests/tauri-launch.test.ts`：啟動 release exe 3 秒，斷言沒提前崩。
  - `node-sidecar/tests/server.test.mjs`：(a) in-process dispatch — ping/authStatus/accountList/unknown method/notification/throwing handler/duplicate registration 等 7 條斷言，(b) end-to-end — `spawn(node, server.mjs)` 跑 stdio 線程化 JSON-RPC，回放 3 條請求並用 `byId` map 對齊（伺服器 dispatch async 不保證 in-order，這是預期行為）。
  - `cargo test`（99 tests）：每個 commands::* 子模組 + 新加 `sidecar::tests`。新增 10 例 sidecar 測試包含：alloc_id 起點 + 增量、PendingTable insert/take/drain、SidecarReply 三種 shape parsing（result / error / event no-id），以及 4 條真正 spawn Node 的 end-to-end：ping round-trip、unknown method 回 -32601、claude.authStatus + claude.accountList stub、8 thread 併發 ping by-id correlation。沒有 node 或 sidecar script 時自動跳過。
- [x] **Release build verified on Windows** — `pnpm exec tauri build` 產生 ~12 MB exe + ~5 MB MSI + ~3.5 MB NSIS installer，smoke test 通過。Phase 2 加入 sidecar 後 release exe 體積無變動（sidecar 是執行期 spawn 不打進 exe）。
- [x] npm scripts：`test:host-api`、`test:tauri-launch`、`check:tauri-rust`、`test:sidecar`、`tauri:*`。

### 進行中 / 下一步

- [x] **Phase 1 完成** — 17 個非-agent 命名空間（settings、shell、dialog、fs、clipboard、image、pty、workspace、update、debug、git、app、notification、system、github、snippet、profile）全 port 到 Rust，renderer 對應 callsite 全切到 `host.*`。Tauri release build green、cargo test 89 例綠、host-api.test.ts 8 情境綠、tauri-launch smoke test 綠。
- [x] **PTY 路線決定**：採用 Rust PTY（`portable-pty` 0.8）而非 Node sidecar，理由：(1) Tauri release 不需要再帶一個 Node runtime，bundle 體積維持在 12.x MB 等級；(2) `portable-pty` 同時支援 Unix forkpty / Windows ConPTY，單一介面；(3) 事件通道直接走 Tauri `Emitter::emit("pty:output"|"pty:exit")`，跟 Electron 的 `webContents.send` 對應，renderer adapter 用 `@tauri-apps/api/event::listen` 包成同步 unsubscribe 風格 → 與 preload 完全相容。**首批 commands**：`pty_create` / `pty_write` / `pty_resize` / `pty_kill`；reader thread 推 `pty:output`；exit watcher polling `try_wait()` 推 `pty:exit`。MVP 暫不 port `pty_restart` / `pty_get_cwd`（需要跨平台 child process tracking，下一個迭代再做）。
- [~] **Phase 2：Agent SDK Node sidecar**（見下方 [Phase 2 設計筆記] 章節）。已切完四刀，covers `host.{claude,openai,worktree,agent,workerBuffer}.*`：claude 顯式 port 29 條 + permissive 預設兜底 ~15 條剩餘方法、openai 5 條、worktree 5 條、agent.listPresets、workerBuffer 4 條（Rust 直管）。剩下 remote/tunnel 兩個 namespace 與真實 agent SDK 實作邏輯（startSession/sendMessage 真要打 `@anthropic-ai/claude-agent-sdk`）綁定 Phase 3 / 之後。
- [~] **Phase 3：packaging + remote/tunnel + update.check**：sidecar 已透過 `bundle.resources` 進 NSIS/MSI 包；`update.check`、`remote.*`、`tunnel.*` 全部 stub-routed（`update.check` 是真實 fetch，其他兩個是 stub 等真正實作）。剩下：自帶 Node runtime（pkg / bun --compile / Node SEA）、把 stub 換成真實 SDK / mDNS / TLS 實作。
- [~] 把全部 `window.batAppAPI.*` 直呼換成 `host.*`：Phase 1 已 port 命名空間（settings、shell、dialog、fs、clipboard、image、pty、workspace、update、debug、git、app、notification、system、github、snippet、profile）都已切到 `host.*`；Phase 2 後 host.{claude, openai, worktree, agent, workerBuffer}.* 也都路由到 sidecar / Rust。剩下純 Phase 3 的 remote / tunnel namespace 仍走 `window.batAppAPI`，受 `installTauriShim()` 保護回 `Promise.resolve(null)`，等該命名空間有 Rust 對應或 Node sidecar 接管再切換。

### 計畫調整

1. **路徑保守化**：原計畫提到 `dialog.confirm` 是低風險首選，實際看下來 `dialog.confirm` 只剩 1 處（rewind 確認）。`settings.{load,save}` 涵蓋面更大，因此先 port 它。
2. **檔名固定**：`settings.json` 路徑刻意對齊 Electron `userData/settings.json`，未來 Electron→Tauri 用戶遷移就只是搬一個檔案。
3. **bundle output 分離**：renderer 走 `dist-tauri/`，避免和 `dist/`、`dist-electron/` 互卡。
4. **棄用 `tauri-plugin-shell`**：第一輪先用 deprecated `Shell::open` 跑通 build，之後馬上換成 `tauri-plugin-opener`，避免之後升級被綁住。

---

## 背景判斷

目前 BetterAgentTerminal 的 React/Vite renderer 可以保留，但 Electron main process 不只是開視窗，而是承擔完整 host runtime：PTY、agent SDK、IPC、遠端 server、設定、profile、通知、更新與安全儲存。

遷移策略不建議一次全 Rust 化。較務實的方向是：

```text
React / TypeScript renderer
        |
        | host API adapter
        v
Tauri Rust host
        |
        | JSON-RPC / stdio / WebSocket
        v
Node sidecar for agent SDKs
```

核心原則：

1. React UI 保留。
2. Agent SDK 與高變動 npm ecosystem 先保留 JS/Node。
3. 穩定 OS 整合與 process/runtime core 逐步搬到 Rust。
4. 先用 spike 驗證風險，再決定是否全面替換 Electron。

## Preview 版切換目標

預估在 10 個 preview 版內完成從 Electron 到 Tauri 的主要切換。這裡的「切換」定義為：Tauri build 取代 Electron build 成為 preview 發佈目標；切換完成後不保留 Electron build，也不做 Electron/Tauri 雙軌維護。必要 rollback 依賴前一個可用 release/tag，而不是保留 Electron runtime。

建議節奏：

1. Preview 1-2：完成 host API adapter 與最小 Tauri shell。
2. Preview 3-4：完成 PTY prototype，決定 Rust PTY 或 Node sidecar 路線。
3. Preview 5-6：讓 workspace、settings、fs、git、worktree 等穩定 host 能力在 Tauri 版可用。
4. Preview 7-8：整合 agent Node sidecar、packaging、macOS notarization 與 Windows installer。
5. Preview 9-10：以 Tauri build 作為 preview 發佈目標，移除 Electron 發佈流程與 runtime 依賴。

---

## 短期計劃：1-3 天

### 目標

確認 React 層能從 Electron API 切出來，並證明同一份 UI 可以跑在 Tauri shell 中。

### 工作項目

1. 新增 `src/host-api.ts` 或同等 adapter 層。
   - 將目前 `window.electronAPI.*` 包成專案自己的 host API。
   - React component 不再直接依賴 Electron preload API。
2. 先挑低風險 API 做 adapter。
   - `shell.openExternal`
   - `dialog.confirm`
   - `fs.readFile`
   - `settings.load`
   - `settings.save`
3. 建立最小 Tauri shell。
   - 使用現有 Vite/React build。
   - 不碰 PTY、agent、remote server。
4. 實作 1-2 個 Tauri Rust command。
   - 例如 `fs_read_file`、`settings_load`。
   - 驗證 `invoke` 與 event model。
5. 先在 macOS 驗證，不急著做完整跨平台。

### 完成標準

1. Electron 版仍可透過 adapter 正常跑。
2. Tauri shell 可載入現有 React UI。
3. 至少一組 host API 在 Electron/Tauri 兩邊都可工作。

---

## 中期計劃：1-3 週

### 目標

決定核心 runtime 要 Rust 化到什麼程度，並讓 Tauri 版具備基本可用的 terminal 與 workspace 能力。

### 工作項目

1. 做 PTY prototype。
   - 路線 A：Rust PTY 實作。
   - 路線 B：保留 Node PTY sidecar。
   - 必測：create/write/resize/kill、process tree、Windows PowerShell、macOS zsh、Linux shell。
2. 保留 agent managers 在 Node sidecar。
   - Claude Agent SDK
   - OpenAI/Codex SDK
   - stream parsing
   - permission / ask-user flow
   - session archive
3. 設計 Rust host 與 Node sidecar 通訊。
   - 優先考慮 JSON-RPC over stdio。
   - 若要沿用現有 remote protocol，可評估 WebSocket。
4. 逐步搬穩定 handler 到 Rust。
   - `fs:*`
   - `git:*`
   - `worktree:*`
   - `settings:*`
   - `profile:*`
   - `snippet:*`
5. 建立 Tauri event 對應。
   - `pty:output`
   - `pty:exit`
   - `claude:message`
   - `claude:stream`
   - `notification:update`
   - `workspace:reload`
6. 建立基本 regression checklist。
   - 開 workspace。
   - 建 terminal。
   - terminal resize。
   - workspace save/load。
   - 啟動 agent session。
   - agent streaming。
   - 停止 agent。
   - file tree read/search。

### 完成標準

1. Tauri 版可以開 workspace 並啟動 terminal。
2. Agent 可以透過 Node sidecar 正常 streaming。
3. 核心資料流不再直接依賴 Electron `ipcMain` / `webContents.send`。
4. 可以判斷 PTY 應該 Rust 化或先保留 Node sidecar。

---

## 長期計劃：1-2 個月以上

### 目標

讓 Tauri build 成為正式版候選，而不是 demo。
短期發佈節奏上，目標是在 10 個 preview 版內讓 Tauri build 取代 Electron build 成為 preview 發佈目標，切換後不保留 Electron build。

### 工作項目

1. 重建 packaging pipeline。
   - macOS dmg/sign/notarize。
   - Windows installer。
   - Linux AppImage 或其他發佈格式。
   - 移除 Electron `asarUnpack` 假設。
2. 重做 update flow。
   - 取代目前 Electron release check/update 邏輯。
   - 確認 GitHub Release artifact 命名與更新 metadata。
3. 設計 secrets migration。
   - Electron `safeStorage` 到 Tauri/Rust keyring。
   - Claude account。
   - OpenAI API key。
   - remote server token/certificate。
4. 移除 Electron-only API。
   - `BrowserWindow`
   - `webContents.send`
   - `ipcMain`
   - `ipcRenderer`
   - `preload.ts`
   - Electron menu/clipboard/dialog/shell wrappers
5. 做跨平台 WebView 驗證。
   - xterm rendering。
   - keyboard shortcuts。
   - drag/drop file path。
   - clipboard image paste。
   - Markdown/Mermaid rendering。
   - context menu。
   - high-output terminal performance。
6. 評估 Node sidecar 收斂範圍。
   - Agent SDK 保留 Node。
   - 可穩定搬 Rust 的工具逐步移除 JS 實作。
   - 不追求短期全 Rust。
7. 發佈候選驗證。
   - `pnpm exec tsc --noEmit --pretty false`
   - `pnpm run compile`
   - Tauri build。
   - macOS 安裝測試。
   - Windows terminal / PowerShell 測試。
   - Linux shell 測試。

### 完成標準

1. Tauri build 功能等價於 Electron 版主要流程。
2. installer 與 updater 可用。
3. secrets migration 有回滾或 fallback。
4. Tauri 版完成主要平台驗證後，Electron 發佈流程與 runtime 依賴會被移除；不維持 Electron fallback。

---

## Phase 2 設計筆記（Agent SDK Node sidecar）

剩下未 port 的命名空間幾乎全綁在 agent runtime：`claude`、`codex`（已從 preload 移除）、`openai`、`agent`、`worker`、`worktree`（agent-tied）。它們呼叫 `@anthropic-ai/claude-agent-sdk`、OpenAI SDK、以及自家 `claude-agent-manager.ts` / `codex-agent-manager.ts` / `openai-agent-manager.ts` 等 Node-only 模組。Rust 端沒有等價物，且這些 SDK 變動頻繁（每兩週一輪），用 Rust 重寫的 ROI 太低。

**選項 A：Node sidecar process（推薦）**
- 在 `src-tauri` 包一個 Node binary（透過 `pkg` 或 `bun build --compile`），啟動時由 Tauri spawn 並維持 lifetime。
- Tauri ↔ sidecar 走 stdio 或 unix socket / named pipe + JSON-RPC（沿用 `claude-agent-manager` 既有的 message bus）。
- Rust 端做：(a) 啟動/關閉/重啟 sidecar、(b) 把 invoke 轉 JSON-RPC、(c) 收 sidecar event 後 emit 到 renderer。
- 成本：~1 MB sidecar 二進位，加 spawn 一個 Node process。對 12.x MB 的 Tauri exe 來說可接受。
- 好處：agent SDK 升級只動 sidecar，不動 Rust；維持與 Electron 版本同步成本最低。

**選項 B：把 Agent SDK rewrite 到 Rust**
- 工作量大且 SDK 跟不上，每次 upstream 改都要 chase。否決。

**選項 C：Tauri command 直接 spawn `node` + IPC**
- 不打包 Node，要求使用者已安裝。對終端 / agent 工具的目標族群可能 OK，但部署 / cold-start 時間都不利。次佳。

**首批 sidecar 命令**
1. `claude.startSession` / `sendMessage` / `stopSession` / `abortSession`（最常用，22 + 6 + 6 + 6 個 callsite）。
2. `claude.authStatus` / `accountList` / `accountSwitch`（auth UI 需要）。
3. `claude.listSessions` / `resumeSession`（session 切換）。
4. `claude.{getSupportedModels,getSupportedAgents,getSupportedCommands,getContextUsage,getAccountInfo}`（單純 read-only metadata）。
5. `worktree.*`（沿用 sidecar 內部的 worktree-manager.ts）。
6. `openai.*`（API key 管理 + session list / compact）。
7. `agent.*` / `worker.*`（小量輔助命令）。

**Phase 3 額外項目（remote/tunnel）**
- `remote.*`（startServer / connect / listProfiles 等）綁定 mDNS + TLS pin，sidecar 也是合理的家。可以與 agent sidecar 共用 process，或拆成獨立 sidecar，但前期建議先共用。
- `tunnel.getConnection` 同上。

---

## 建議遷移順序

1. Host API adapter。
2. 最小 Tauri shell。
3. 低風險 Rust commands。
4. PTY prototype。
5. Node sidecar for agents。
6. Workspace/settings/fs/git/worktree Rust 化。
7. Packaging/updater/secrets。
8. Electron cleanup。

---

## 不建議的路線

1. 一開始全 Rust 重寫 agent managers。
   - Claude/OpenAI/Codex SDK 變動快，JS 保留較務實。
2. Tauri 只包一個完整 Node backend。
   - 這會增加 process 複雜度，但體積與架構收益有限。
3. 先碰 packaging。
   - 應先證明 runtime 可行，再投入 release pipeline。

## 初步結論

可行方向是混合式架構：React 保留，Agent JS 保留，host core 逐步 Rust 化。第一階段只需要證明 React 能透過 adapter 同時支援 Electron 與 Tauri；第二階段再用 PTY prototype 決定遷移成本是否值得投入。
