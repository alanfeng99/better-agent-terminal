# Flat (hoisted) layout so the bundled node_modules tree contains
# real package directories instead of pnpm content-addressable
# symlinks. Tauri bundle.resources copies files; symlinks would
# either fail to resolve at install time or get duplicated wholesale.
node-linker=hoisted
